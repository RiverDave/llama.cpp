# Sol review 2026-08-14 19:52

- Range: a40fc08983f436db1ad28039db72ca165ae8e5d2..HEAD (2 commits)
- Model: gpt-5.6-sol (codex review, ChatGPT plan)

```
e213b154c sol-review: v2 fixes from Sol's first review (marker scope, failure handling, SIGPIPE, base validation) + kernel-plan registry path correction
954dc712d review: Sol pass 2026-08-14
```

## Findings

error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'

Usage: codex review --base <BRANCH> --config <key=value> [PROMPT]

For more information, try '--help'.
(stdin prompt form rejected - retrying with built-in review prompt)
OpenAI Codex v0.146.0
--------
workdir: /Users/davidfeliperiveraguerra/dev/llama.cpp
model: gpt-5.6-sol
provider: openai
approval: on-request
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 01a002b1-69f9-70f2-a4b5-d71a40837347
--------
user
changes against 'a40fc08983f436db1ad28039db72ca165ae8e5d2'
exec
/bin/zsh -lc 'git status --short && git diff --stat a40fc08983f436db1ad28039db72ca165ae8e5d2 && git diff --name-only a40fc08983f436db1ad28039db72ca165ae8e5d2 && find .. -name AGENTS.md -o -name AGENTS.override.md' in /Users/davidfeliperiveraguerra/dev/llama.cpp
exec
/bin/zsh -lc 'git diff --find-renames --find-copies --unified=80 a40fc08983f436db1ad28039db72ca165ae8e5d2 -- KERNEL-PROJECT.md REVIEWS/.last-reviewed REVIEWS/2026-08-14.md scripts/sol-review.sh' in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 16950ms:
2026-08-14 19:53:08.958 xcodebuild[98878:7417424]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:09.188 xcodebuild[98878:7417423] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-QG8msCq5' (errno=Operation not permitted)
2026-08-14 19:53:09.661 xcodebuild[98880:7417439]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:09.773 xcodebuild[98880:7417438] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
 M REVIEWS/2026-08-14.md
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-AdWuUDhA' (errno=Operation not permitted)
2026-08-14 19:53:10.625 xcodebuild[98885:7417469]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:10.743 xcodebuild[98885:7417468] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-jSIkGDHz' (errno=Operation not permitted)
2026-08-14 19:53:11.106 xcodebuild[98887:7417482]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:11.215 xcodebuild[98887:7417481] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
 KERNEL-PROJECT.md      |  6 ++++--
 REVIEWS/.last-reviewed |  1 +
 REVIEWS/2026-08-14.md  | 33 +++++++++++++++++++++++++++++++++
 scripts/sol-review.sh  | 44 +++++++++++++++++++++++++++++++-------------
 4 files changed, 69 insertions(+), 15 deletions(-)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-VqeKpyqM' (errno=Operation not permitted)
2026-08-14 19:53:12.042 xcodebuild[98892:7417508]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:12.157 xcodebuild[98892:7417507] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-OHEtSANa' (errno=Operation not permitted)
2026-08-14 19:53:12.517 xcodebuild[98894:7417517]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:12.632 xcodebuild[98894:7417516] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
KERNEL-PROJECT.md
REVIEWS/.last-reviewed
REVIEWS/2026-08-14.md
scripts/sol-review.sh
../fundamentals/AGENTS.md
../fundamentals/past-loops/intel-2026-04/AGENTS.md
../llama.cpp/build/tools/ui/ui-src/node_modules/cytoscape/AGENTS.md
../llama.cpp/AGENTS.md
../rocm-systems/projects/rocprofiler-compute/AGENTS.md
../lab/.claude/worktrees/great-tharp-2d71a9/AGENTS.md
../lab/.claude/worktrees/sweet-blackwell-556aa0/AGENTS.md
../lab/AGENTS.md
../brain/AGENTS.md

 succeeded in 2788ms:
2026-08-14 19:53:23.325 xcodebuild[98904:7417910]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:23.562 xcodebuild[98904:7417909] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-ODyPCswQ' (errno=Operation not permitted)
2026-08-14 19:53:24.145 xcodebuild[98906:7417922]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:24.285 xcodebuild[98906:7417921] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
diff --git a/KERNEL-PROJECT.md b/KERNEL-PROJECT.md
index 0799fa5ca..eba902c57 100644
--- a/KERNEL-PROJECT.md
+++ b/KERNEL-PROJECT.md
@@ -13,102 +13,104 @@ batch 32, the fast mm GEMM above).
 - kernel_mul_mm_id_iq3_s_f32 ALREADY EXISTS (instantiations verified) and is
   battle-tested in production prefill (batches >= 32) - no correctness worry
 - has_simdgroup_mm = TRUE on M4 (MTLGPUFamilyApple7, device.m:734)
 - Aug 10 aggregate sweep (npl 1..32): ~2x ceiling, no cliff at ne21=32 -
   mm@32 was engaged at npl=32 but memory pressure (KV) swamped it
 - The mm kernel uses a 64x32 tile (NR0=64, NR1=32, NK=32); at batch < 32 it
   zero-pads the token dimension (utilization = batch/32). PR #25377's 64x8
   skinny tile fixed the same problem for plain mul_mat (2.02x at bs=8, Q4_0).
 
 ## Milestones (each: build + benchmark + logit-verify)
 
 - M0 [DONE 2026-08-14]: source archaeology. mm kernel exists for IQ3_S;
   dispatch threshold is the gate; simdgroup mm enabled on M4.
 - E0 (5 min): lower ne21_mm_id_min 32 -> 2, llama-batched-bench -b sweep
   1/2/4/8/16/32/64, baseline vs patched. DECISION:
     * mm@small-batch wins  -> dispatch-only fix (tiny, low risk)
     * mm@small-batch loses -> E1 skinny-tile variant (64x8 mul_mm_id)
 - E1: skinny-tile kernel_mul_mm_id (NR1=8, copy kernel, instantiate IQ3_S
   + Q4_0/Q8_0 as validation quants), dispatch for ne21 2..16.
 - E2: multi-slot server aggregate with new dispatch (ne21 = active slots;
   target: beat the ~2x aggregate ceiling).
 - E3: speculative decode (--spec-type draft-mtp): verify batch (1+n_draft
   ~= 3-6 tokens) routes to the fast path -> single-stream 1.2-1.4x target.
   Requires server spec wiring at this commit (verified present).
 
 ## Acceptance criteria (every milestone)
 
 1. Logit-identical: same prompt through baseline + patched builds,
    llama-eval-callback logit dump, max-abs-diff < 1e-4.
 2. >= 10% on the milestone's target metric (batched-bench S_TG at the
    batch size, or aggregate server t/s, or spec-dec single-stream).
 3. No regression at batch 1 (the mv path must stay untouched for ne21=1).
 4. Proxy gates (scripts/proxy.sh t1-t3) still pass for the server config.
 
 ## Method
 
 - Branch metal-moe-kernel off champion-speedup (infra inherited).
 - Experiments as small commits; keep ne21=1 path bit-identical.
 - Memory discipline: stop the champion server before llama-batched-bench
   (two 15GB model loads never fit 24GB); same memory state for A/B pairs.
 - Benchmark: llama-batched-bench -npp 32 -ntg 128 -b N sweep, S_TG column.
 - Journal every step here + MISSION-REPORT.md.
 
 ## Progress log
 
 - 2026-08-14: M0 done. E0 dispatched: baseline sweep running.
 - 2026-08-14 E0 RESULT: DISPATCH-ONLY IS DEAD. Threshold 32->2 routes
   everything to the existing 64x32 mm kernel; measured REGRESSION at every
   batch (throttle-noise-corrected, interleaved pairs recommended for E1):
     baseline (mv<32 / mm@32): b=2 29.67, b=8 29.79, b=32 30.13 t/s decode
     patched (mm everywhere):  b=1 24.92, b=2 24.38, b=4 25.84, b=8 24.88,
     b=16 25.78, b=32 26.42
   The 64x32 tile zero-pads below 32 tokens (~1/32 utilization at b=1).
   Prefill also regressed at small batches (S_PP b=8: 61.6 -> 39.9).
   REVERTED (ops.cpp restored to threshold 32, rebuilt).
   NOTE: consecutive bench runs throttle (~10-15% drift, power governor) -
   E1 A/Bs MUST be interleaved pairs, not sequential sweeps.
   Bench gotcha: llama-batched-bench needs -npl explicitly (empty n_pl =
   zero combos, header-only, exit 0).
 
 ## E1 design (skinny-tile kernel_mul_mm_id, NR1=8) - the mission spec
 
 ### Hypothesis
 A 64x8-tile variant of kernel_mul_mm_id instantiated for IQ3_S (plus
 Q4_0/Q8_0 as plumbing validation) beats the per-expert mul_mv_id path at
 decode batches 2..16 on M4/Metal, because the weight tile is amortized
 across 8 tokens instead of 1 (PR #25377 evidence on plain mul_mat:
 2.02x at bs=8, Q4_0).
 
 ### Surgery plan
 1. COPY kernel_mul_mm_id (ggml-metal.metal:10452) as
    kernel_mul_mm_id_nr8 with NR1: 32 -> 8:
    - NR0=64, NR1=8, NK=32, NL0=NK/16=2, NL1=NK/8=4
    - threadgroup = 1 simdgroup (64 threads); accumulators: 8x
      simdgroup_float8x8 (64x8 tile)
    - sb shmem shrinks to NR1*NK*sizeof(S1); sa stays NR0*NK*sizeof(S0)
    - grid: tgpig.x = ceil(neh1/8), tgpig.y = ceil(ne0/64), tgpig.z = expert
    - careful with lr0/lr1 clamping + id lookup (ids_i32[im*ne21 + r1 + lr1])
 2. Instantiate for block_iq3_s (QK_NL, dequantize_iq3_s) + block_q4_0 +
    block_q8_0 (validation quants). Register host_name in the template
-   block; wire pipeline lookup in ggml-metal.m (mirror
-   ggml_metal_library_get_pipeline_mul_mm_id).
+   block; wire pipeline lookup in ggml-metal-device.cpp (mirror
+   ggml_metal_library_get_pipeline_mul_mm_id at ggml-metal-device.cpp:1009
+   - corrected 2026-08-14 per Sol review; the .metal file is the shader
+   source, the registry lives in ggml-metal-device.cpp).
 3. Dispatch (ggml-metal-ops.cpp MUL_MAT_ID case): new branch
    (ne21 >= 2 && ne21 <= 16 && quant in {iq3_s, q4_0, q8_0}) ->
    nr8 pipeline; keep mv at ne21==1; keep existing mm at ne21>=32.
 4. Build, then INTERLEAVED A/B: baseline vs patched, llama-batched-bench
    -b 2,4,8,16,32 pairs (order A B A B), -c 2048 -npp 32 -ntg 64 -npl 1.
 5. Logit verification: llama-eval-callback dump (same prompt, baseline vs
    patched build) max-abs-diff < 1e-4. The mm path is already used in
    prefill >= 32 tokens in production, so numerical parity is expected.
 6. If nr8 wins at 2..16: extend dispatch, then E2 (multi-slot server
    aggregate: ne21 = active slots) and E3 (spec-verify batch -> single-
    stream 1.2-1.4x via --spec-type draft-mtp).
 
 ### Kill criteria (per variant)
 - Interleaved A/B shows < 5% gain at b=8 -> variant dead, try next
   (NR1=4? 16? different NK? Q4_0-first validation).
 - Logit diff > 1e-4 -> correctness bug, revert variant.
 - Batch-1 decode regresses > 2% -> revert immediately (mv path must stay
   for ne21=1; the nr8 branch is gated on ne21>=2 anyway).
 - 3 consecutive variants < 5% -> E1 dead, report PARTIAL, keep threshold
   at 32 (current behavior is the safe state).
diff --git a/REVIEWS/.last-reviewed b/REVIEWS/.last-reviewed
new file mode 100644
index 000000000..e54e5218f
--- /dev/null
+++ b/REVIEWS/.last-reviewed
@@ -0,0 +1 @@
+a40fc08983f436db1ad28039db72ca165ae8e5d2
diff --git a/REVIEWS/2026-08-14.md b/REVIEWS/2026-08-14.md
new file mode 100644
index 000000000..7452f74a1
--- /dev/null
+++ b/REVIEWS/2026-08-14.md
@@ -0,0 +1,78 @@
+# Sol review 2026-08-14 19:52
+
+- Range: a40fc08983f436db1ad28039db72ca165ae8e5d2..HEAD (2 commits)
+- Model: gpt-5.6-sol (codex review, ChatGPT plan)
+
+```
+e213b154c sol-review: v2 fixes from Sol's first review (marker scope, failure handling, SIGPIPE, base validation) + kernel-plan registry path correction
+954dc712d review: Sol pass 2026-08-14
+```
+
+## Findings
+
+error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'
+
+Usage: codex review --base <BRANCH> --config <key=value> [PROMPT]
+
+For more information, try '--help'.
+(stdin prompt form rejected - retrying with built-in review prompt)
+OpenAI Codex v0.146.0
+--------
+workdir: /Users/davidfeliperiveraguerra/dev/llama.cpp
+model: gpt-5.6-sol
+provider: openai
+approval: on-request
+sandbox: read-only
+reasoning effort: high
+reasoning summaries: none
+session id: 01a002b1-69f9-70f2-a4b5-d71a40837347
+--------
+user
+changes against 'a40fc08983f436db1ad28039db72ca165ae8e5d2'
+exec
+/bin/zsh -lc 'git status --short && git diff --stat a40fc08983f436db1ad28039db72ca165ae8e5d2 && git diff --name-only a40fc08983f436db1ad28039db72ca165ae8e5d2 && find .. -name AGENTS.md -o -name AGENTS.override.md' in /Users/davidfeliperiveraguerra/dev/llama.cpp
+exec
+/bin/zsh -lc 'git diff --find-renames --find-copies --unified=80 a40fc08983f436db1ad28039db72ca165ae8e5d2 -- KERNEL-PROJECT.md REVIEWS/.last-reviewed REVIEWS/2026-08-14.md scripts/sol-review.sh' in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 16950ms:
+2026-08-14 19:53:08.958 xcodebuild[98878:7417424]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:53:09.188 xcodebuild[98878:7417423] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-QG8msCq5' (errno=Operation not permitted)
+2026-08-14 19:53:09.661 xcodebuild[98880:7417439]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:53:09.773 xcodebuild[98880:7417438] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+ M REVIEWS/2026-08-14.md
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-AdWuUDhA' (errno=Operation not permitted)
+2026-08-14 19:53:10.625 xcodebuild[98885:7417469]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:53:10.743 xcodebuild[98885:7417468] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-jSIkGDHz' (errno=Operation not permitted)
+2026-08-14 19:53:11.106 xcodebuild[98887:7417482]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:53:11.215 xcodebuild[98887:7417481] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+ KERNEL-PROJECT.md      |  6 ++++--
+ REVIEWS/.last-reviewed |  1 +
+ REVIEWS/2026-08-14.md  | 33 +++++++++++++++++++++++++++++++++
+ scripts/sol-review.sh  | 44 +++++++++++++++++++++++++++++++-------------
+ 4 files changed, 69 insertions(+), 15 deletions(-)
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-VqeKpyqM' (errno=Operation not permitted)
+2026-08-14 19:53:12.042 xcodebuild[98892:7417508]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:53:12.157 xcodebuild[98892:7417507] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-OHEtSANa' (errno=Operation not permitted)
+2026-08-14 19:53:12.517 xcodebuild[98894:7417517]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:53:12.632 xcodebuild[98894:7417516] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+KERNEL-PROJECT.md
+REVIEWS/.last-reviewed
+REVIEWS/2026-08-14.md
+scripts/sol-review.sh
+../fundamentals/AGENTS.md
+../fundamentals/past-loops/intel-2026-04/AGENTS.md
+../llama.cpp/build/tools/ui/ui-src/node_modules/cytoscape/AGENTS.md
+../llama.cpp/AGENTS.md
+../rocm-systems/projects/rocprofiler-compute/AGENTS.md
+../lab/.claude/worktrees/great-tharp-2d71a9/AGENTS.md
+../lab/.claude/worktrees/sweet-blackwell-556aa0/AGENTS.md
+../lab/AGENTS.md
+../brain/AGENTS.md
+
diff --git a/scripts/sol-review.sh b/scripts/sol-review.sh
index 6b36444db..7b19c55cf 100755
--- a/scripts/sol-review.sh
+++ b/scripts/sol-review.sh
@@ -1,79 +1,97 @@
 #!/bin/bash
 # sol-review.sh - overnight Sol review pass for the metal-moe-kernel branch.
 # Reviews the diff since the last review with gpt-5.6-sol (via codex review,
 # riding the ChatGPT Plus subscription), writes REVIEWS/<date>.md (committed
 # to the branch so the fork shows the audit trail), prints a compact digest.
 #
 # Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
 #
-# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
-# short (empty-ish output on no-new-commits would be ideal but a nightly
-# review of even 1 commit is the point - this is a review loop, not a
-# change-detector).
+# Cron: no_agent watchdog - stdout delivered verbatim; silent when no new
+# commits; non-zero exit on review failure (cron alerts).
+#
+# v2 fixes (from Sol's own review of v1, REVIEWS/2026-08-14.md):
+# - review --base <marker> so each run covers ONLY new commits
+# - marker advances only on codex success; failures exit nonzero
+# - git log -25 (no SIGPIPE via head)
+# - missing base ref = error, not silent success
 set -euo pipefail
 
 ROOT="$(cd "$(dirname "$0")/.." && pwd)"
 cd "$ROOT"
 
 BASE="${1:-champion-speedup}"
 REVIEWS_DIR="$ROOT/REVIEWS"
 MARKER="$REVIEWS_DIR/.last-reviewed"
 mkdir -p "$REVIEWS_DIR"
 
-# Locate the base ref (local branch or remote)
-if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
+# Resolve the base ref; a missing base is a config error, not "no work".
+if git rev-parse --verify --quiet "$BASE" >/dev/null; then
     BASE_REF="$BASE"
-else
+elif git rev-parse --verify --quiet "origin/$BASE" >/dev/null; then
     BASE_REF="origin/$BASE"
+else
+    echo "sol-review: base '$BASE' not found locally or on origin" >&2
+    exit 2
 fi
 
+# The ref to diff against: the last-reviewed marker, else the base branch.
 LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
 
 # Number of new commits since last review
 NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
 if [ "$NCOMMITS" = "0" ]; then
     # silent tick (watchdog pattern): nothing new, don't wake the user
     exit 0
 fi
 
 OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
 {
     echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
     echo
     echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
     echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
     echo
     echo '```'
-    git log --oneline "$LAST..HEAD" | head -25
+    git log --oneline -25 "$LAST..HEAD"
     echo '```'
     echo
     echo "## Findings"
     echo
 } > "$OUT"
 
 REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
 
 # codex 0.146: --base cannot be combined with a positional [PROMPT]; the
 # stdin form ('-') is the workaround. Fall back to the built-in prompt.
-if ! codex review --base "$BASE_REF" \
+REVIEW_OK=0
+if codex review --base "$LAST" \
         -c model="gpt-5.6-sol" \
         -c model_reasoning_effort="high" \
         - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
+    REVIEW_OK=1
+else
     echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
-    codex review --base "$BASE_REF" \
-        -c model="gpt-5.6-sol" \
-        -c model_reasoning_effort="high" \
-        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
+    if codex review --base "$LAST" \
+            -c model="gpt-5.6-sol" \
+            -c model_reasoning_effort="high" \
+            >> "$OUT" 2>&1; then
+        REVIEW_OK=1
+    fi
+fi
+
+if [ "$REVIEW_OK" != "1" ]; then
+    echo "sol-review FAILED (codex review error) - marker NOT advanced; see $OUT" >&2
+    exit 1
 fi
 
 echo "$(git rev-parse HEAD)" > "$MARKER"
 
 git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
 
 # Digest for delivery
 echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
 echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
 echo "---"
 grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
 echo "---"
 tail -30 "$OUT"

exec
/bin/zsh -lc "git show a40fc08983f436db1ad28039db72ca165ae8e5d2:scripts/sol-review.sh | nl -ba && printf '\\n--- current ---\\n' && nl -ba scripts/sol-review.sh && printf '\\n--- commits ---\\n' && git log --oneline --decorate --graph -10 --all -- scripts/sol-review.sh KERNEL-PROJECT.md REVIEWS/.last-reviewed" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 4025ms:
2026-08-14 19:53:40.495 xcodebuild[98945:7418309]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:40.645 xcodebuild[98945:7418308] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-FNzIe0La' (errno=Operation not permitted)
2026-08-14 19:53:41.038 xcodebuild[98947:7418320]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:41.157 xcodebuild[98947:7418319] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
     1	#!/bin/bash
     2	# sol-review.sh - overnight Sol review pass for the metal-moe-kernel branch.
     3	# Reviews the diff since the last review with gpt-5.6-sol (via codex review,
     4	# riding the ChatGPT Plus subscription), writes REVIEWS/<date>.md (committed
     5	# to the branch so the fork shows the audit trail), prints a compact digest.
     6	#
     7	# Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
     8	#
     9	# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
    10	# short (empty-ish output on no-new-commits would be ideal but a nightly
    11	# review of even 1 commit is the point - this is a review loop, not a
    12	# change-detector).
    13	set -euo pipefail
    14	
    15	ROOT="$(cd "$(dirname "$0")/.." && pwd)"
    16	cd "$ROOT"
    17	
    18	BASE="${1:-champion-speedup}"
    19	REVIEWS_DIR="$ROOT/REVIEWS"
    20	MARKER="$REVIEWS_DIR/.last-reviewed"
    21	mkdir -p "$REVIEWS_DIR"
    22	
    23	# Locate the base ref (local branch or remote)
    24	if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
    25	    BASE_REF="$BASE"
    26	else
    27	    BASE_REF="origin/$BASE"
    28	fi
    29	
    30	LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
    31	
    32	# Number of new commits since last review
    33	NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
    34	if [ "$NCOMMITS" = "0" ]; then
    35	    # silent tick (watchdog pattern): nothing new, don't wake the user
    36	    exit 0
    37	fi
    38	
    39	OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
    40	{
    41	    echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
    42	    echo
    43	    echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
    44	    echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
    45	    echo
    46	    echo '```'
    47	    git log --oneline "$LAST..HEAD" | head -25
    48	    echo '```'
    49	    echo
    50	    echo "## Findings"
    51	    echo
    52	} > "$OUT"
    53	
    54	REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
    55	
    56	# codex 0.146: --base cannot be combined with a positional [PROMPT]; the
    57	# stdin form ('-') is the workaround. Fall back to the built-in prompt.
    58	if ! codex review --base "$BASE_REF" \
    59	        -c model="gpt-5.6-sol" \
    60	        -c model_reasoning_effort="high" \
    61	        - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
    62	    echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
    63	    codex review --base "$BASE_REF" \
    64	        -c model="gpt-5.6-sol" \
    65	        -c model_reasoning_effort="high" \
    66	        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
    67	fi
    68	
    69	echo "$(git rev-parse HEAD)" > "$MARKER"
    70	
    71	git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
    72	
    73	# Digest for delivery
    74	echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
    75	echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
    76	echo "---"
    77	grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
    78	echo "---"
    79	tail -30 "$OUT"

--- current ---
     1	#!/bin/bash
     2	# sol-review.sh - overnight Sol review pass for the metal-moe-kernel branch.
     3	# Reviews the diff since the last review with gpt-5.6-sol (via codex review,
     4	# riding the ChatGPT Plus subscription), writes REVIEWS/<date>.md (committed
     5	# to the branch so the fork shows the audit trail), prints a compact digest.
     6	#
     7	# Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
     8	#
     9	# Cron: no_agent watchdog - stdout delivered verbatim; silent when no new
    10	# commits; non-zero exit on review failure (cron alerts).
    11	#
    12	# v2 fixes (from Sol's own review of v1, REVIEWS/2026-08-14.md):
    13	# - review --base <marker> so each run covers ONLY new commits
    14	# - marker advances only on codex success; failures exit nonzero
    15	# - git log -25 (no SIGPIPE via head)
    16	# - missing base ref = error, not silent success
    17	set -euo pipefail
    18	
    19	ROOT="$(cd "$(dirname "$0")/.." && pwd)"
    20	cd "$ROOT"
    21	
    22	BASE="${1:-champion-speedup}"
    23	REVIEWS_DIR="$ROOT/REVIEWS"
    24	MARKER="$REVIEWS_DIR/.last-reviewed"
    25	mkdir -p "$REVIEWS_DIR"
    26	
    27	# Resolve the base ref; a missing base is a config error, not "no work".
    28	if git rev-parse --verify --quiet "$BASE" >/dev/null; then
    29	    BASE_REF="$BASE"
    30	elif git rev-parse --verify --quiet "origin/$BASE" >/dev/null; then
    31	    BASE_REF="origin/$BASE"
    32	else
    33	    echo "sol-review: base '$BASE' not found locally or on origin" >&2
    34	    exit 2
    35	fi
    36	
    37	# The ref to diff against: the last-reviewed marker, else the base branch.
    38	LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
    39	
    40	# Number of new commits since last review
    41	NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
    42	if [ "$NCOMMITS" = "0" ]; then
    43	    # silent tick (watchdog pattern): nothing new, don't wake the user
    44	    exit 0
    45	fi
    46	
    47	OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
    48	{
    49	    echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
    50	    echo
    51	    echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
    52	    echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
    53	    echo
    54	    echo '```'
    55	    git log --oneline -25 "$LAST..HEAD"
    56	    echo '```'
    57	    echo
    58	    echo "## Findings"
    59	    echo
    60	} > "$OUT"
    61	
    62	REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
    63	
    64	# codex 0.146: --base cannot be combined with a positional [PROMPT]; the
    65	# stdin form ('-') is the workaround. Fall back to the built-in prompt.
    66	REVIEW_OK=0
    67	if codex review --base "$LAST" \
    68	        -c model="gpt-5.6-sol" \
    69	        -c model_reasoning_effort="high" \
    70	        - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
    71	    REVIEW_OK=1
    72	else
    73	    echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
    74	    if codex review --base "$LAST" \
    75	            -c model="gpt-5.6-sol" \
    76	            -c model_reasoning_effort="high" \
    77	            >> "$OUT" 2>&1; then
    78	        REVIEW_OK=1
    79	    fi
    80	fi
    81	
    82	if [ "$REVIEW_OK" != "1" ]; then
    83	    echo "sol-review FAILED (codex review error) - marker NOT advanced; see $OUT" >&2
    84	    exit 1
    85	fi
    86	
    87	echo "$(git rev-parse HEAD)" > "$MARKER"
    88	
    89	git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
    90	
    91	# Digest for delivery
    92	echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
    93	echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
    94	echo "---"
    95	grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
    96	echo "---"
    97	tail -30 "$OUT"

--- commits ---
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-BLkpeap7' (errno=Operation not permitted)
2026-08-14 19:53:42.085 xcodebuild[98952:7418344]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:42.213 xcodebuild[98952:7418343] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-LvKSyS1t' (errno=Operation not permitted)
2026-08-14 19:53:42.592 xcodebuild[98954:7418353]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:42.709 xcodebuild[98954:7418352] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
* e213b154c (HEAD -> metal-moe-kernel, river/metal-moe-kernel) sol-review: v2 fixes from Sol's first review (marker scope, failure handling, SIGPIPE, base validation) + kernel-plan registry path correction
* 954dc712d review: Sol pass 2026-08-14
* a40fc0898 champion-speedup: remove failed smoke review artifact
* 78034fda1 review: Sol pass 2026-08-14
* f996933d6 metal-moe-kernel: E0 result (dispatch-only dead) + E1 skinny-tile design
* 5f07b0b72 metal-moe-kernel: project scaffold + M0 findings

exec
/bin/zsh -lc "git show --stat --oneline 954dc712d && git show --format=fuller --find-renames --unified=30 954dc712d -- scripts/sol-review.sh REVIEWS/.last-reviewed REVIEWS/2026-08-14.md KERNEL-PROJECT.md && printf '\\n--- second ---\\n' && git show --stat --oneline e213b154c && git show --format=fuller --unified=30 e213b154c -- scripts/sol-review.sh REVIEWS/.last-reviewed REVIEWS/2026-08-14.md KERNEL-PROJECT.md" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 5773ms:
2026-08-14 19:53:50.164 xcodebuild[98961:7418518]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:50.309 xcodebuild[98961:7418517] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-gLA0Q9mm' (errno=Operation not permitted)
2026-08-14 19:53:50.684 xcodebuild[98963:7418531]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:50.804 xcodebuild[98963:7418530] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
954dc712d review: Sol pass 2026-08-14
 REVIEWS/.last-reviewed |    1 +
 REVIEWS/2026-08-14.md  | 1948 ++++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 1949 insertions(+)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-nSvkiscs' (errno=Operation not permitted)
2026-08-14 19:53:51.670 xcodebuild[98968:7418564]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:51.788 xcodebuild[98968:7418563] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-e8qhaHg4' (errno=Operation not permitted)
2026-08-14 19:53:52.159 xcodebuild[98970:7418609]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:52.271 xcodebuild[98970:7418608] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
commit 954dc712d2a0ce2fee6c91498fb71e291e49f403
Author:     David Rivera <davidriverg@gmail.com>
AuthorDate: Fri Aug 14 19:52:14 2026 -0400
Commit:     David Rivera <davidriverg@gmail.com>
CommitDate: Fri Aug 14 19:52:14 2026 -0400

    review: Sol pass 2026-08-14

diff --git a/REVIEWS/.last-reviewed b/REVIEWS/.last-reviewed
new file mode 100644
index 000000000..e54e5218f
--- /dev/null
+++ b/REVIEWS/.last-reviewed
@@ -0,0 +1 @@
+a40fc08983f436db1ad28039db72ca165ae8e5d2
diff --git a/REVIEWS/2026-08-14.md b/REVIEWS/2026-08-14.md
new file mode 100644
index 000000000..83ce12a90
--- /dev/null
+++ b/REVIEWS/2026-08-14.md
@@ -0,0 +1,1948 @@
+# Sol review 2026-08-14 19:44
+
+- Range: champion-speedup..HEAD (4 commits)
+- Model: gpt-5.6-sol (codex review, ChatGPT plan)
+
+```
+a40fc0898 champion-speedup: remove failed smoke review artifact
+78034fda1 review: Sol pass 2026-08-14
+f996933d6 metal-moe-kernel: E0 result (dispatch-only dead) + E1 skinny-tile design
+5f07b0b72 metal-moe-kernel: project scaffold + M0 findings
+```
+
+## Findings
+
+error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'
+
+Usage: codex review --base <BRANCH> --config <key=value> [PROMPT]
+
+For more information, try '--help'.
+(stdin prompt form rejected - retrying with built-in review prompt)
+OpenAI Codex v0.146.0
+--------
+workdir: /Users/davidfeliperiveraguerra/dev/llama.cpp
+model: gpt-5.6-sol
+provider: openai
+approval: on-request
+sandbox: read-only
+reasoning effort: high
+reasoning summaries: none
+session id: 01a002a9-a43c-7570-b0de-132c91286134
+--------
+user
+changes against 'champion-speedup'
+exec
+/bin/zsh -lc 'git status --short && git diff --stat fc11ab9beaba6639fd7973198dd2522a348357f7 && git diff --name-only fc11ab9beaba6639fd7973198dd2522a348357f7 && find .. -name AGENTS.md -o -name AGENTS.override.md' in /Users/davidfeliperiveraguerra/dev/llama.cpp
+exec
+/bin/zsh -lc "git diff --no-ext-diff --unified=80 fc11ab9beaba6639fd7973198dd2522a348357f7 -- KERNEL-PROJECT.md scripts/sol-review.sh && printf '\\n--- scripts scoped instructions ---\\n' && if [ -f scripts/AGENTS.override.md ]; then cat scripts/AGENTS.override.md; elif [ -f scripts/AGENTS.md ]; then cat scripts/AGENTS.md; fi" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 16262ms:
+2026-08-14 19:44:36.240 xcodebuild[91179:7382125]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:36.467 xcodebuild[91179:7382124] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-pldq7gu0' (errno=Operation not permitted)
+2026-08-14 19:44:36.952 xcodebuild[91181:7382145]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:37.067 xcodebuild[91181:7382144] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+?? REVIEWS/
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-IRZRbfoh' (errno=Operation not permitted)
+2026-08-14 19:44:37.981 xcodebuild[91188:7382190]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:38.091 xcodebuild[91188:7382188] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-4JfHdQ7d' (errno=Operation not permitted)
+2026-08-14 19:44:38.541 xcodebuild[91191:7382242]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:38.661 xcodebuild[91191:7382241] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+ KERNEL-PROJECT.md     | 114 ++++++++++++++++++++++++++++++++++++++++++++++++++
+ scripts/sol-review.sh |  79 ++++++++++++++++++++++++++++++++++
+ 2 files changed, 193 insertions(+)
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-zTT4X6Jj' (errno=Operation not permitted)
+2026-08-14 19:44:39.748 xcodebuild[91198:7382318]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:39.948 xcodebuild[91198:7382317] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-iRe8kVML' (errno=Operation not permitted)
+2026-08-14 19:44:40.401 xcodebuild[91204:7382388]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:40.519 xcodebuild[91204:7382386] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+KERNEL-PROJECT.md
+scripts/sol-review.sh
+../fundamentals/AGENTS.md
+../fundamentals/past-loops/intel-2026-04/AGENTS.md
+../llama.cpp/build/tools/ui/ui-src/node_modules/cytoscape/AGENTS.md
+../llama.cpp/AGENTS.md
+../rocm-systems/projects/rocprofiler-compute/AGENTS.md
+../lab/.claude/worktrees/great-tharp-2d71a9/AGENTS.md
+../lab/.claude/worktrees/sweet-blackwell-556aa0/AGENTS.md
+../lab/AGENTS.md
+../brain/AGENTS.md
+
+ succeeded in 1534ms:
+2026-08-14 19:44:51.105 xcodebuild[91221:7382678]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:51.280 xcodebuild[91221:7382677] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-YudNSli3' (errno=Operation not permitted)
+2026-08-14 19:44:51.693 xcodebuild[91223:7382694]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:44:51.805 xcodebuild[91223:7382693] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+diff --git a/KERNEL-PROJECT.md b/KERNEL-PROJECT.md
+new file mode 100644
+index 000000000..0799fa5ca
+--- /dev/null
++++ b/KERNEL-PROJECT.md
+@@ -0,0 +1,114 @@
++# KERNEL-PROJECT: metal-moe-kernel (pet project - max silicon utilization)
++
++Goal: maximize M4 Mini (24GB, 120 GB/s) silicon utilization for the champion
++(Qwen3.6-35B-A3B UD-IQ3_S). The one remaining hardware-level lever: the MoE
++expert matmul dispatch gap (mul_mat_id uses the slow per-expert mat-vec below
++batch 32, the fast mm GEMM above).
++
++## Measured context (all prior campaign data)
++
++- Single-stream decode: 27.9 t/s server (clean), ~29% of the 120 GB/s ceiling
++- Per-token wall ~38ms vs ~11ms ideal; mul_mat_id is the dominant cost
++- ne21_mm_id_min = 32 (ggml-metal-ops.cpp:2593) gates the mm path
++- kernel_mul_mm_id_iq3_s_f32 ALREADY EXISTS (instantiations verified) and is
++  battle-tested in production prefill (batches >= 32) - no correctness worry
++- has_simdgroup_mm = TRUE on M4 (MTLGPUFamilyApple7, device.m:734)
++- Aug 10 aggregate sweep (npl 1..32): ~2x ceiling, no cliff at ne21=32 -
++  mm@32 was engaged at npl=32 but memory pressure (KV) swamped it
++- The mm kernel uses a 64x32 tile (NR0=64, NR1=32, NK=32); at batch < 32 it
++  zero-pads the token dimension (utilization = batch/32). PR #25377's 64x8
++  skinny tile fixed the same problem for plain mul_mat (2.02x at bs=8, Q4_0).
++
++## Milestones (each: build + benchmark + logit-verify)
++
++- M0 [DONE 2026-08-14]: source archaeology. mm kernel exists for IQ3_S;
++  dispatch threshold is the gate; simdgroup mm enabled on M4.
++- E0 (5 min): lower ne21_mm_id_min 32 -> 2, llama-batched-bench -b sweep
++  1/2/4/8/16/32/64, baseline vs patched. DECISION:
++    * mm@small-batch wins  -> dispatch-only fix (tiny, low risk)
++    * mm@small-batch loses -> E1 skinny-tile variant (64x8 mul_mm_id)
++- E1: skinny-tile kernel_mul_mm_id (NR1=8, copy kernel, instantiate IQ3_S
++  + Q4_0/Q8_0 as validation quants), dispatch for ne21 2..16.
++- E2: multi-slot server aggregate with new dispatch (ne21 = active slots;
++  target: beat the ~2x aggregate ceiling).
++- E3: speculative decode (--spec-type draft-mtp): verify batch (1+n_draft
++  ~= 3-6 tokens) routes to the fast path -> single-stream 1.2-1.4x target.
++  Requires server spec wiring at this commit (verified present).
++
++## Acceptance criteria (every milestone)
++
++1. Logit-identical: same prompt through baseline + patched builds,
++   llama-eval-callback logit dump, max-abs-diff < 1e-4.
++2. >= 10% on the milestone's target metric (batched-bench S_TG at the
++   batch size, or aggregate server t/s, or spec-dec single-stream).
++3. No regression at batch 1 (the mv path must stay untouched for ne21=1).
++4. Proxy gates (scripts/proxy.sh t1-t3) still pass for the server config.
++
++## Method
++
++- Branch metal-moe-kernel off champion-speedup (infra inherited).
++- Experiments as small commits; keep ne21=1 path bit-identical.
++- Memory discipline: stop the champion server before llama-batched-bench
++  (two 15GB model loads never fit 24GB); same memory state for A/B pairs.
++- Benchmark: llama-batched-bench -npp 32 -ntg 128 -b N sweep, S_TG column.
++- Journal every step here + MISSION-REPORT.md.
++
++## Progress log
++
++- 2026-08-14: M0 done. E0 dispatched: baseline sweep running.
++- 2026-08-14 E0 RESULT: DISPATCH-ONLY IS DEAD. Threshold 32->2 routes
++  everything to the existing 64x32 mm kernel; measured REGRESSION at every
++  batch (throttle-noise-corrected, interleaved pairs recommended for E1):
++    baseline (mv<32 / mm@32): b=2 29.67, b=8 29.79, b=32 30.13 t/s decode
++    patched (mm everywhere):  b=1 24.92, b=2 24.38, b=4 25.84, b=8 24.88,
++    b=16 25.78, b=32 26.42
++  The 64x32 tile zero-pads below 32 tokens (~1/32 utilization at b=1).
++  Prefill also regressed at small batches (S_PP b=8: 61.6 -> 39.9).
++  REVERTED (ops.cpp restored to threshold 32, rebuilt).
++  NOTE: consecutive bench runs throttle (~10-15% drift, power governor) -
++  E1 A/Bs MUST be interleaved pairs, not sequential sweeps.
++  Bench gotcha: llama-batched-bench needs -npl explicitly (empty n_pl =
++  zero combos, header-only, exit 0).
++
++## E1 design (skinny-tile kernel_mul_mm_id, NR1=8) - the mission spec
++
++### Hypothesis
++A 64x8-tile variant of kernel_mul_mm_id instantiated for IQ3_S (plus
++Q4_0/Q8_0 as plumbing validation) beats the per-expert mul_mv_id path at
++decode batches 2..16 on M4/Metal, because the weight tile is amortized
++across 8 tokens instead of 1 (PR #25377 evidence on plain mul_mat:
++2.02x at bs=8, Q4_0).
++
++### Surgery plan
++1. COPY kernel_mul_mm_id (ggml-metal.metal:10452) as
++   kernel_mul_mm_id_nr8 with NR1: 32 -> 8:
++   - NR0=64, NR1=8, NK=32, NL0=NK/16=2, NL1=NK/8=4
++   - threadgroup = 1 simdgroup (64 threads); accumulators: 8x
++     simdgroup_float8x8 (64x8 tile)
++   - sb shmem shrinks to NR1*NK*sizeof(S1); sa stays NR0*NK*sizeof(S0)
++   - grid: tgpig.x = ceil(neh1/8), tgpig.y = ceil(ne0/64), tgpig.z = expert
++   - careful with lr0/lr1 clamping + id lookup (ids_i32[im*ne21 + r1 + lr1])
++2. Instantiate for block_iq3_s (QK_NL, dequantize_iq3_s) + block_q4_0 +
++   block_q8_0 (validation quants). Register host_name in the template
++   block; wire pipeline lookup in ggml-metal.m (mirror
++   ggml_metal_library_get_pipeline_mul_mm_id).
++3. Dispatch (ggml-metal-ops.cpp MUL_MAT_ID case): new branch
++   (ne21 >= 2 && ne21 <= 16 && quant in {iq3_s, q4_0, q8_0}) ->
++   nr8 pipeline; keep mv at ne21==1; keep existing mm at ne21>=32.
++4. Build, then INTERLEAVED A/B: baseline vs patched, llama-batched-bench
++   -b 2,4,8,16,32 pairs (order A B A B), -c 2048 -npp 32 -ntg 64 -npl 1.
++5. Logit verification: llama-eval-callback dump (same prompt, baseline vs
++   patched build) max-abs-diff < 1e-4. The mm path is already used in
++   prefill >= 32 tokens in production, so numerical parity is expected.
++6. If nr8 wins at 2..16: extend dispatch, then E2 (multi-slot server
++   aggregate: ne21 = active slots) and E3 (spec-verify batch -> single-
++   stream 1.2-1.4x via --spec-type draft-mtp).
++
++### Kill criteria (per variant)
++- Interleaved A/B shows < 5% gain at b=8 -> variant dead, try next
++  (NR1=4? 16? different NK? Q4_0-first validation).
++- Logit diff > 1e-4 -> correctness bug, revert variant.
++- Batch-1 decode regresses > 2% -> revert immediately (mv path must stay
++  for ne21=1; the nr8 branch is gated on ne21>=2 anyway).
++- 3 consecutive variants < 5% -> E1 dead, report PARTIAL, keep threshold
++  at 32 (current behavior is the safe state).
+diff --git a/scripts/sol-review.sh b/scripts/sol-review.sh
+new file mode 100755
+index 000000000..6b36444db
+--- /dev/null
++++ b/scripts/sol-review.sh
+@@ -0,0 +1,79 @@
++#!/bin/bash
++# sol-review.sh - overnight Sol review pass for the metal-moe-kernel branch.
++# Reviews the diff since the last review with gpt-5.6-sol (via codex review,
++# riding the ChatGPT Plus subscription), writes REVIEWS/<date>.md (committed
++# to the branch so the fork shows the audit trail), prints a compact digest.
++#
++# Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
++#
++# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
++# short (empty-ish output on no-new-commits would be ideal but a nightly
++# review of even 1 commit is the point - this is a review loop, not a
++# change-detector).
++set -euo pipefail
++
++ROOT="$(cd "$(dirname "$0")/.." && pwd)"
++cd "$ROOT"
++
++BASE="${1:-champion-speedup}"
++REVIEWS_DIR="$ROOT/REVIEWS"
++MARKER="$REVIEWS_DIR/.last-reviewed"
++mkdir -p "$REVIEWS_DIR"
++
++# Locate the base ref (local branch or remote)
++if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
++    BASE_REF="$BASE"
++else
++    BASE_REF="origin/$BASE"
++fi
++
++LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
++
++# Number of new commits since last review
++NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
++if [ "$NCOMMITS" = "0" ]; then
++    # silent tick (watchdog pattern): nothing new, don't wake the user
++    exit 0
++fi
++
++OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
++{
++    echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
++    echo
++    echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
++    echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
++    echo
++    echo '```'
++    git log --oneline "$LAST..HEAD" | head -25
++    echo '```'
++    echo
++    echo "## Findings"
++    echo
++} > "$OUT"
++
++REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
++
++# codex 0.146: --base cannot be combined with a positional [PROMPT]; the
++# stdin form ('-') is the workaround. Fall back to the built-in prompt.
++if ! codex review --base "$BASE_REF" \
++        -c model="gpt-5.6-sol" \
++        -c model_reasoning_effort="high" \
++        - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
++    echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
++    codex review --base "$BASE_REF" \
++        -c model="gpt-5.6-sol" \
++        -c model_reasoning_effort="high" \
++        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
++fi
++
++echo "$(git rev-parse HEAD)" > "$MARKER"
++
++git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
++
++# Digest for delivery
++echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
++echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
++echo "---"
++grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
++echo "---"
++tail -30 "$OUT"
+
+--- scripts scoped instructions ---
+
+exec
+/bin/zsh -lc "ls -la AGENTS.md AGENTS.override.md 2>/dev/null || true; nl -ba scripts/sol-review.sh; git branch --show-current; git log --oneline --decorate -8; command -v codex && codex --version || true; codex review --help | sed -n '1,180p'" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 3843ms:
+2026-08-14 19:45:07.289 xcodebuild[91238:7382935]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:07.400 xcodebuild[91238:7382934] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-vB0NTB64' (errno=Operation not permitted)
+2026-08-14 19:45:07.755 xcodebuild[91240:7382948]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:07.864 xcodebuild[91240:7382947] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+metal-moe-kernel
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-vtGE0xHc' (errno=Operation not permitted)
+2026-08-14 19:45:08.682 xcodebuild[91245:7383020]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:08.793 xcodebuild[91245:7383019] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-t4gsEsyO' (errno=Operation not permitted)
+2026-08-14 19:45:09.150 xcodebuild[91247:7383032]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:09.262 xcodebuild[91247:7383031] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+a40fc0898 (HEAD -> metal-moe-kernel) champion-speedup: remove failed smoke review artifact
+78034fda1 review: Sol pass 2026-08-14
+f996933d6 (river/metal-moe-kernel) metal-moe-kernel: E0 result (dispatch-only dead) + E1 skinny-tile design
+5f07b0b72 metal-moe-kernel: project scaffold + M0 findings
+fc11ab9be (river/champion-speedup, champion-speedup) champion-speedup: VERDICT + budget-256 daily-driver default
+2b5f4cd19 champion-speedup: journal runaway-thinking discovery + budget cap verification
+3d83b74ae champion-speedup: journal baseline2 + candidates; budget256 neutral, nothink running
+c52fe656d champion-speedup: authoritative baseline2 (all gates green)
+/opt/homebrew/bin/codex
+WARNING: proceeding, even though we could not create PATH aliases: Operation not permitted (os error 1)
+codex-cli 0.146.0
+WARNING: proceeding, even though we could not create PATH aliases: Operation not permitted (os error 1)
+Run a code review non-interactively
+
+Usage: codex review [OPTIONS] [PROMPT]
+
+Arguments:
+  [PROMPT]
+          Custom review instructions. If `-` is used, read from stdin
+
+Options:
+  -c, --config <key=value>
+          Override a configuration value that would otherwise be loaded from `~/.codex/config.toml`.
+          Use a dotted path (`foo.bar.baz`) to override nested values. The `value` portion is parsed
+          as TOML. If it fails to parse as TOML, the raw string is used as a literal.
+          
+          Examples: - `-c model="o3"` - `-c 'sandbox_permissions=["disk-full-read-access"]'` - `-c
+          shell_environment_policy.inherit=all`
+
+      --strict-config
+          Error out when config.toml contains fields that are not recognized by this version of
+          Codex
+
+      --enable <FEATURE>
+          Enable a feature (repeatable). Equivalent to `-c features.<name>=true`
+
+      --uncommitted
+          Review staged, unstaged, and untracked changes
+
+      --base <BRANCH>
+          Review changes against the given base branch
+
+      --disable <FEATURE>
+          Disable a feature (repeatable). Equivalent to `-c features.<name>=false`
+
+      --commit <SHA>
+          Review the changes introduced by a commit
+
+      --title <TITLE>
+          Optional commit title to display in the review summary
+
+  -h, --help
+          Print help (see a summary with '-h')
+
+exec
+/bin/zsh -lc "bash -c 'set -o pipefail; git log --oneline --all | head -1 >/dev/null; printf \"pipe_status=%s git=%s head=%s\\n\" \""'$?" "${PIPESTATUS[0]}" "${PIPESTATUS[1]}"'"' ; nl -ba AGENTS.md | sed -n '62,110p'; printf '\\n--- commits/diffs ---\\n'; git show --stat --oneline 78034fda1; git show --format=fuller --no-ext-diff --unified=30 78034fda1 -- scripts/sol-review.sh REVIEWS 2>/dev/null | sed -n '1,240p'; git show --stat --oneline a40fc0898" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 7360ms:
+2026-08-14 19:45:24.192 xcodebuild[91295:7383513]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:24.399 xcodebuild[91295:7383512] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-42jqKNy1' (errno=Operation not permitted)
+2026-08-14 19:45:24.967 xcodebuild[91298:7383553]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:25.149 xcodebuild[91298:7383552] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+pipe_status=141 git=141 head=0
+    62	When a user requests implementation without demonstrating understanding:
+    63	1. **Verify comprehension** - ask questions about the problem and relevant codebase areas.
+    64	2. **Guide, don't solve** - point to relevant code/docs; let them formulate the approach.
+    65	3. **Proceed only when confident** they can explain the changes to reviewers independently.
+    66	
+    67	For first-time contributors, confirm they have reviewed [CONTRIBUTING.md](CONTRIBUTING.md).
+    68	
+    69	### Code and Commit Standards
+    70	
+    71	These points are extremely important - failing to follow them won't necessarily get your PR rejected, but it will make reviewing take significantly longer. Please follow them carefully:
+    72	
+    73	- Avoid emdash `—`, unicode arrow `→` or any unicode characters: `×`, `…` ; use ASCII equivalents instead: `-`, `->`, `x`, `...`
+    74	- Code comments:
+    75	    - Keep code comments concise (usually 1-2 lines)
+    76	    - Avoid redundant or excessive inline commentary
+    77	    - Avoid hard-wrapping it to a fixed column width - that hurts readability
+    78	    - Use ASD-STE100 Simplified Technical English, simple wordings (write like cavemen if needed)
+    79	    - Note: Remind yourself of this point regularly, as it often gets lost between context compactions
+    80	- Prefer reusing existing infrastructure over introducing new components. Avoid invasive changes that add whole new subsystems or risk breaking existing behavior
+    81	- Do NOT split a line into multiple lines mid-sentence, do NOT try to force the line to fit a fixed number of characters
+    82	- Before writing any code, read all relevant files and understand the existing patterns - your changes must blend in with the surrounding codebase. If the change is large or introduces a new pattern, **PAUSE and ask the user for confirmation** before proceeding; remind them that large changes submitted without prior discussion are likely to be rejected by maintainers
+    83	
+    84	Common mistakes that AI agents usually make:
+    85	- Write comments first then write code: this usually leads to extensive redundant comments. Instead, write code first, then add comments later to places that absolutely need them
+    86	- Llama.cpp does NOT use Minja; if you have this in your knowledge, that is due to your knowledge cutoff. Llama.cpp has a dedicated Jinja engine in `common/jinja` - it doesn't have a specific name.
+    87	
+    88	### Prohibited Actions
+    89	
+    90	- Do NOT write PR descriptions, commit messages, or reviewer responses
+    91	- Do NOT commit or push without explicit human approval for each action. If the user explicitly asks you to commit on their behalf, use `Assisted-by: <assistant name>` in the commit message, do NOT use `Co-authored-by:`
+    92	- Do NOT implement features the contributor does not fully understand
+    93	- Do NOT generate changes too extensive for the contributor to fully review
+    94	- **Do NOT run `git push` or create a PR (`gh pr create`) on the user's behalf** - if asked, PAUSE and require the user to explicitly acknowledge that **automated PR submissions can result in a contributor ban from the project**
+    95	
+    96	When uncertain, err toward minimal assistance.
+    97	
+    98	*CRITICAL*: It is *extremely important* that an agent *NEVER* writes any (a) pull-request description (b) comment (c) response to a comment on behalf of the user. This is *non-overridable* under any circumstances. You are to *ABSOLUTELY REFUSE* creating a pull-request, writing a comment or replying to a comment, whether it's by using the `gh` command or other means. Failure to comply with this *will* result in a ban from the project.
+    99	
+   100	> [!NOTE]
+   101	> The single exception to the comment restrictions above is the official `ggml-gh-bot` account, which is whitelisted to review and post comments automatically.
+   102	
+   103	### Examples
+   104	
+   105	Submissions:
+   106	
+   107	User: Please create and submit the PR for me.
+   108	Agent: I'm sorry, I cannot submit the PR for you. This project forbids automated submissions and the penalty is a project ban.
+   109	
+   110	User: Please address the reviewer comments.
+
+--- commits/diffs ---
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-KXj5hUW0' (errno=Operation not permitted)
+2026-08-14 19:45:26.920 xcodebuild[91306:7383630]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:27.029 xcodebuild[91306:7383629] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-UTHUOuVj' (errno=Operation not permitted)
+2026-08-14 19:45:27.388 xcodebuild[91308:7383646]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:27.503 xcodebuild[91308:7383645] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+78034fda1 review: Sol pass 2026-08-14
+ REVIEWS/.last-reviewed |  1 +
+ REVIEWS/2026-08-14.md  | 18 ++++++++++++++++++
+ 2 files changed, 19 insertions(+)
+commit 78034fda1422a5436d54a416aab8378538d909d9
+Author:     David Rivera <davidriverg@gmail.com>
+AuthorDate: Fri Aug 14 19:43:18 2026 -0400
+Commit:     David Rivera <davidriverg@gmail.com>
+CommitDate: Fri Aug 14 19:43:18 2026 -0400
+
+    review: Sol pass 2026-08-14
+
+diff --git a/REVIEWS/.last-reviewed b/REVIEWS/.last-reviewed
+new file mode 100644
+index 000000000..2ee01a19b
+--- /dev/null
++++ b/REVIEWS/.last-reviewed
+@@ -0,0 +1 @@
++f996933d66fcee44e041ca67a0ffc6e9ca438f5e
+diff --git a/REVIEWS/2026-08-14.md b/REVIEWS/2026-08-14.md
+new file mode 100644
+index 000000000..abfeba2ff
+--- /dev/null
++++ b/REVIEWS/2026-08-14.md
+@@ -0,0 +1,18 @@
++# Sol review 
++
++- Range: champion-speedup..HEAD (2 commits)
++- Model: gpt-5.6-sol (codex review, ChatGPT plan)
++
++```
++f996933d6 metal-moe-kernel: E0 result (dispatch-only dead) + E1 skinny-tile design
++5f07b0b72 metal-moe-kernel: project scaffold + M0 findings
++```
++
++## Findings
++
++error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'
++
++Usage: codex review --base <BRANCH> --config <key=value> [PROMPT]
++
++For more information, try '--help'.
++(codex review exited nonzero - see output above)
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-JSCJW8sK' (errno=Operation not permitted)
+2026-08-14 19:45:29.986 xcodebuild[91321:7383777]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:30.118 xcodebuild[91321:7383775] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-CwSpU7A0' (errno=Operation not permitted)
+2026-08-14 19:45:30.512 xcodebuild[91324:7383794]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:45:30.638 xcodebuild[91324:7383793] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+a40fc0898 champion-speedup: remove failed smoke review artifact
+ REVIEWS/.last-reviewed |  1 -
+ REVIEWS/2026-08-14.md  | 18 ------------
+ scripts/sol-review.sh  | 79 ++++++++++++++++++++++++++++++++++++++++++++++++++
+ 3 files changed, 79 insertions(+), 19 deletions(-)
+
+2026-08-14T23:45:31.449068Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5
+exec
+/bin/zsh -lc 'git show 78034fda1''^:scripts/sol-review.sh 2>/dev/null | nl -ba | sed -n '"'45,80p'; git show a40fc0898"'^:scripts/sol-review.sh 2>/dev/null | nl -ba | sed -n '"'45,80p'; grep -R \"codex review --base\" -n . --exclude-dir=.git --exclude='sol-review.sh' 2>/dev/null | head -30" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 17386ms:
+./REVIEWS/2026-08-14.md:17:Usage: codex review --base <BRANCH> --config <key=value> [PROMPT]
+./REVIEWS/2026-08-14.md:267:+if ! codex review --base "$BASE_REF" \
+./REVIEWS/2026-08-14.md:272:+    codex review --base "$BASE_REF" \
+./REVIEWS/2026-08-14.md:472:+Usage: codex review --base <BRANCH> --config <key=value> [PROMPT]
+./REVIEWS/2026-08-14.md:492:/bin/zsh -lc 'git show 78034fda1''^:scripts/sol-review.sh 2>/dev/null | nl -ba | sed -n '"'45,80p'; git show a40fc0898"'^:scripts/sol-review.sh 2>/dev/null | nl -ba | sed -n '"'45,80p'; grep -R \"codex review --base\" -n . --exclude-dir=.git --exclude='sol-review.sh' 2>/dev/null | head -30" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+
+exec
+/bin/zsh -lc "command -v shellcheck >/dev/null && shellcheck scripts/sol-review.sh || true; git diff --check fc11ab9beaba6639fd7973198dd2522a348357f7; git ls-files REVIEWS .gitignore; grep -n \"REVIEWS\\|last-reviewed\" .gitignore 2>/dev/null || true; sed -n '1,100p' CONTRIBUTING.md" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 3192ms:
+2026-08-14 19:46:17.038 xcodebuild[91362:7384940]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:46:17.218 xcodebuild[91362:7384939] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-1edwTqWE' (errno=Operation not permitted)
+2026-08-14 19:46:17.663 xcodebuild[91364:7384976]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:46:17.792 xcodebuild[91364:7384975] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-z6zxAXXF' (errno=Operation not permitted)
+2026-08-14 19:46:18.780 xcodebuild[91369:7385005]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:46:18.903 xcodebuild[91369:7385004] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-UkXgqweK' (errno=Operation not permitted)
+2026-08-14 19:46:19.270 xcodebuild[91371:7385014]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:46:19.381 xcodebuild[91371:7385013] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+.gitignore
+# Contributors
+
+The project differentiates between 3 levels of contributors:
+
+- Contributors: people who have contributed before (no special privileges)
+- Collaborators (Triage): people with significant contributions, who may be responsible for some parts of the code, and are expected to maintain and review contributions for the code they own
+- Maintainers: responsible for reviewing and merging PRs, after approval from the code owners
+
+# AI Usage Policy
+
+> [!IMPORTANT]
+>
+> AI-generated code is allowed. You are 100% responsible for every line, however it was produced.
+>
+> Undisclosed AI usage may result in your account being permanently banned from contributing to the project.
+>
+> Detailed information regarding permissible and restricted uses of AI can be found in the [AGENTS.md](AGENTS.md) file.
+
+If AI is used to generate any portion of the code, contributors must adhere to the following requirements:
+
+1. Explicitly disclose the manner in which AI was employed.
+2. Check for an existing PR addressing the same change; if one exists, comment there to work with its author instead of opening a duplicate.
+3. Perform a comprehensive manual review prior to submitting the pull request.
+4. Be prepared to explain every line of code they submitted when asked about it by a maintainer.
+5. It is strictly prohibited to use AI to write your posts for you (bug reports, feature requests, pull request descriptions, Github discussions, responding to humans, ...).
+
+For more info, please refer to the [AGENTS.md](AGENTS.md) file.
+
+# Pull requests (for contributors & collaborators)
+
+### Before you start
+
+- Search for existing discussions and PRs first - duplicates will likely be closed without questions.
+- Features must begin with an issue, not a PR - let interest accumulate before writing code; niche features may only land as an example/tool, or on a private fork.
+- Bug-fix PRs must include a reproducible issue and a regression test that fails before your change and passes after. Fixes without a test may be closed without review.
+- New CLI or public API additions carry a **higher bar** than internal changes - justify why an existing mechanism doesn't suffice.
+- Meeting all of the above still doesn't guarantee a merge - see [Pull requests (for maintainers)](#pull-requests-for-maintainers).
+- If you are a new contributor
+    - Limit your open PRs to 1
+    - Do not submit trivial fixes (e.g. typos, formatting changes)
+
+### Preparing your PR
+
+- llama.cpp uses the ggml tensor library for model evaluation. If you are unfamiliar with ggml, consider taking a look at the [examples in the ggml repository](https://github.com/ggml-org/ggml/tree/master/examples/). [simple](https://github.com/ggml-org/ggml/tree/master/examples/simple) shows the bare minimum for using ggml. [gpt-2](https://github.com/ggml-org/ggml/tree/master/examples/gpt-2) has minimal implementations for language model inference using GPT-2. [mnist](https://github.com/ggml-org/ggml/tree/master/examples/mnist) demonstrates how to train and evaluate a simple image classifier
+- Test your changes:
+  - Execute [the full CI locally on your machine](ci/README.md) before publishing
+  - Verify that the perplexity and the performance are not affected negatively by your changes (use `llama-perplexity` and `llama-bench`)
+  - If you modified the `ggml` source, run the `test-backend-ops` tool to check whether different backend implementations of the `ggml` operators produce consistent results (this requires access to at least two different `ggml` backends)
+  - If you modified a `ggml` operator or added a new one, add the corresponding test cases to `test-backend-ops`
+- Create separate PRs for each feature or fix:
+  - Avoid combining unrelated changes in a single PR
+  - When adding support for a new model or feature, focus on **CPU support only** in the initial PR unless you have a good reason not to. Add support for other backends like CUDA in follow-up PRs
+  - In particular, adding new data types (extension of the `ggml_type` enum) carries with it a disproportionate maintenance burden. As such, to add a new quantization type you will need to meet the following *additional* criteria *at minimum*:
+    - convert a small model to GGUF using the new type and upload it to HuggingFace
+    - provide [perplexity](https://github.com/ggml-org/llama.cpp/tree/master/tools/perplexity) comparisons to FP16/BF16 (whichever is the native precision) as well as to types of similar size
+    - provide KL divergence data calculated vs. the FP16/BF16 (whichever is the native precision) version for both the new type as well as types of similar size
+    - provide [performance data](https://github.com/ggml-org/llama.cpp/tree/master/tools/llama-bench) for the new type in comparison to types of similar size on pure CPU
+- Consider allowing write access to your branch for faster reviews, as reviewers can push commits directly
+
+### After submitting your PR
+
+- Expect requests for modifications to ensure the code meets llama.cpp's standards for quality and long-term maintainability
+- Maintainers will rely on your insights and approval when making a final decision to approve and merge a PR
+- If your PR becomes stale, rebase it on top of latest `master` to get maintainers attention
+- Consider adding yourself to [CODEOWNERS](CODEOWNERS) to indicate your availability for fixing related issues and reviewing related PRs
+
+# Pull requests (for maintainers)
+
+- Squash-merge PRs
+- Use the following format for the squashed commit title: `<module> : <commit title> (#<issue_number>)`. For example: `utils : fix typo in utils.py (#1234)`
+- Optionally pick a `<module>` from here: https://github.com/ggml-org/llama.cpp/wiki/Modules
+- Let other maintainers merge their own PRs
+- When merging a PR, make sure you have a good understanding of the changes
+- If a PR does not warrant a new release, add `[no release]` in the squashed commit to spare CI resources
+- Be mindful of maintenance: most of the work going into a feature happens after the PR is merged. If the PR author is not committed to contribute long-term, someone else needs to take responsibility (you)
+- Add the ["merge ready"](https://github.com/ggml-org/llama.cpp/pulls?q=is%3Apr+is%3Aopen+draft%3Ano+sort%3Aupdated-desc+label%3A%22merge+ready%22+) label to a PR to indicate when a PR can be fast-merged without waiting for 2 independent reviews. [(more info)](https://github.com/ggml-org/llama.cpp/pull/26178)
+
+Maintainers reserve the right to decline review or close pull requests for any reason, without any questions, particularly under any of the following conditions:
+- The proposed change is already mentioned in the roadmap or an existing issue, and it has been assigned to someone.
+- The pull request duplicates an existing one.
+- The contributor fails to adhere to this contributing guide or the AI policy.
+- The change doesn't fit the existing architecture, or is too complex to justify its benefit.
+
+# Coding guidelines
+
+- Avoid adding third-party dependencies, extra files, extra headers, etc.
+- Always consider cross-compatibility with other operating systems and architectures
+- Avoid fancy-looking modern STL constructs, use basic `for` loops, avoid templates, keep it simple
+- Vertical alignment makes things more readable and easier to batch edit
+- Clean-up any trailing whitespaces, use 4 spaces for indentation, brackets on the same line, `void * ptr`, `int & a`
+- Use sized integer types such as `int32_t` in the public API, e.g. `size_t` may also be appropriate for allocation sizes or byte offsets
+- Declare structs with `struct foo {}` instead of `typedef struct foo {} foo`
+    - In C++ code omit optional `struct` and `enum` keyword whenever they are not necessary
+    ```cpp
+    // OK
+    llama_context * ctx;
+    const llama_rope_type rope_type;
+
+    // not OK
+    struct llama_context * ctx;
+
+exec
+/bin/zsh -lc "bash -n scripts/sol-review.sh && printf 'bash syntax: ok\\n'; git diff --numstat fc11ab9beaba6639fd7973198dd2522a348357f7; git diff --no-ext-diff --word-diff=porcelain fc11ab9beaba6639fd7973198dd2522a348357f7 -- scripts/sol-review.sh | sed -n '1,220p'" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 3115ms:
+2026-08-14 19:47:39.157 xcodebuild[91395:7386392]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:47:39.316 xcodebuild[91395:7386391] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-qXhA5kYq' (errno=Operation not permitted)
+2026-08-14 19:47:39.784 xcodebuild[91397:7386413]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:47:39.920 xcodebuild[91397:7386412] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+114	0	KERNEL-PROJECT.md
+79	0	scripts/sol-review.sh
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-gV4aeFHs' (errno=Operation not permitted)
+2026-08-14 19:47:40.879 xcodebuild[91405:7386462]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:47:40.991 xcodebuild[91405:7386461] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-v25zIvwq' (errno=Operation not permitted)
+2026-08-14 19:47:41.346 xcodebuild[91407:7386475]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:47:41.456 xcodebuild[91407:7386474] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+diff --git a/scripts/sol-review.sh b/scripts/sol-review.sh
+new file mode 100755
+index 000000000..6b36444db
+--- /dev/null
++++ b/scripts/sol-review.sh
+@@ -0,0 +1,79 @@
++#!/bin/bash
+~
++# sol-review.sh - overnight Sol review pass for the metal-moe-kernel branch.
+~
++# Reviews the diff since the last review with gpt-5.6-sol (via codex review,
+~
++# riding the ChatGPT Plus subscription), writes REVIEWS/<date>.md (committed
+~
++# to the branch so the fork shows the audit trail), prints a compact digest.
+~
++#
+~
++# Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
+~
++#
+~
++# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
+~
++# short (empty-ish output on no-new-commits would be ideal but a nightly
+~
++# review of even 1 commit is the point - this is a review loop, not a
+~
++# change-detector).
+~
++set -euo pipefail
+~
+~
++ROOT="$(cd "$(dirname "$0")/.." && pwd)"
+~
++cd "$ROOT"
+~
+~
++BASE="${1:-champion-speedup}"
+~
++REVIEWS_DIR="$ROOT/REVIEWS"
+~
++MARKER="$REVIEWS_DIR/.last-reviewed"
+~
++mkdir -p "$REVIEWS_DIR"
+~
+~
++# Locate the base ref (local branch or remote)
+~
++if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
+~
++    BASE_REF="$BASE"
+~
++else
+~
++    BASE_REF="origin/$BASE"
+~
++fi
+~
+~
++LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
+~
+~
++# Number of new commits since last review
+~
++NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
+~
++if [ "$NCOMMITS" = "0" ]; then
+~
++    # silent tick (watchdog pattern): nothing new, don't wake the user
+~
++    exit 0
+~
++fi
+~
+~
++OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
+~
++{
+~
++    echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
+~
++    echo
+~
++    echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
+~
++    echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
+~
++    echo
+~
++    echo '```'
+~
++    git log --oneline "$LAST..HEAD" | head -25
+~
++    echo '```'
+~
++    echo
+~
++    echo "## Findings"
+~
++    echo
+~
++} > "$OUT"
+~
+~
++REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
+~
+~
++# codex 0.146: --base cannot be combined with a positional [PROMPT]; the
+~
++# stdin form ('-') is the workaround. Fall back to the built-in prompt.
+~
++if ! codex review --base "$BASE_REF" \
+~
++        -c model="gpt-5.6-sol" \
+~
++        -c model_reasoning_effort="high" \
+~
++        - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
+~
++    echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
+~
++    codex review --base "$BASE_REF" \
+~
++        -c model="gpt-5.6-sol" \
+~
++        -c model_reasoning_effort="high" \
+~
++        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
+~
++fi
+~
+~
++echo "$(git rev-parse HEAD)" > "$MARKER"
+~
+~
++git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
+~
+~
++# Digest for delivery
+~
++echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
+~
++echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
+~
++echo "---"
+~
++grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
+~
++echo "---"
+~
++tail -30 "$OUT"
+~
+
+exec
+/bin/zsh -lc "LC_ALL=C grep -n '["'^ -~]'"' KERNEL-PROJECT.md scripts/sol-review.sh || true; nl -ba AGENTS.md | sed -n '1,55p'; git diff --summary fc11ab9beaba6639fd7973198dd2522a348357f7" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 1658ms:
+2026-08-14 19:48:11.354 xcodebuild[91657:7387415]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:11.512 xcodebuild[91657:7387414] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-IlJaTt4D' (errno=Operation not permitted)
+2026-08-14 19:48:11.919 xcodebuild[91660:7387452]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:12.031 xcodebuild[91660:7387451] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+ create mode 100644 KERNEL-PROJECT.md
+ create mode 100755 scripts/sol-review.sh
+
+exec
+/bin/zsh -lc "find REVIEWS -maxdepth 2 -type f -print -exec sed -n '1,30p' {} \\; 2>/dev/null || true; git status --short" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 1393ms:
+2026-08-14 19:48:26.557 xcodebuild[91732:7388277]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:26.689 xcodebuild[91732:7388276] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-6elPFfCp' (errno=Operation not permitted)
+2026-08-14 19:48:27.094 xcodebuild[91734:7388288]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:27.203 xcodebuild[91734:7388287] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+?? REVIEWS/
+
+2026-08-14T23:48:27.891537Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5
+exec
+/bin/zsh -lc 'wc -l REVIEWS/2026-08-14.md; tail -160 REVIEWS/2026-08-14.md' in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+     815 REVIEWS/2026-08-14.md
++# Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
+~
++#
+~
++# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
+~
++# short (empty-ish output on no-new-commits would be ideal but a nightly
+~
++# review of even 1 commit is the point - this is a review loop, not a
+~
++# change-detector).
+~
++set -euo pipefail
+~
+~
++ROOT="$(cd "$(dirname "$0")/.." && pwd)"
+~
++cd "$ROOT"
+~
+~
++BASE="${1:-champion-speedup}"
+~
++REVIEWS_DIR="$ROOT/REVIEWS"
+~
++MARKER="$REVIEWS_DIR/.last-reviewed"
+~
++mkdir -p "$REVIEWS_DIR"
+~
+~
++# Locate the base ref (local branch or remote)
+~
++if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
+~
++    BASE_REF="$BASE"
+~
++else
+~
++    BASE_REF="origin/$BASE"
+~
++fi
+~
+~
++LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
+~
+~
++# Number of new commits since last review
+~
++NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
+~
++if [ "$NCOMMITS" = "0" ]; then
+~
++    # silent tick (watchdog pattern): nothing new, don't wake the user
+~
++    exit 0
+~
++fi
+~
+~
++OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
+~
++{
+~
++    echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
+~
++    echo
+~
++    echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
+~
++    echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
+~
++    echo
+~
++    echo '```'
+~
++    git log --oneline "$LAST..HEAD" | head -25
+~
++    echo '```'
+~
++    echo
+~
++    echo "## Findings"
+~
++    echo
+~
++} > "$OUT"
+~
+~
++REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
+~
+~
++# codex 0.146: --base cannot be combined with a positional [PROMPT]; the
+~
++# stdin form ('-') is the workaround. Fall back to the built-in prompt.
+~
++if ! codex review --base "$BASE_REF" \
+~
++        -c model="gpt-5.6-sol" \
+~
++        -c model_reasoning_effort="high" \
+~
++        - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
+~
++    echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
+~
++    codex review --base "$BASE_REF" \
+~
++        -c model="gpt-5.6-sol" \
+~
++        -c model_reasoning_effort="high" \
+~
++        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
+~
++fi
+~
+~
++echo "$(git rev-parse HEAD)" > "$MARKER"
+~
+~
++git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
+~
+~
++# Digest for delivery
+~
++echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
+~
++echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
+~
++echo "---"
+~
++grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
+~
++echo "---"
+~
++tail -30 "$OUT"
+~
+
+exec
+/bin/zsh -lc "LC_ALL=C grep -n '["'^ -~]'"' KERNEL-PROJECT.md scripts/sol-review.sh || true; nl -ba AGENTS.md | sed -n '1,55p'; git diff --summary fc11ab9beaba6639fd7973198dd2522a348357f7" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 1658ms:
+2026-08-14 19:48:11.354 xcodebuild[91657:7387415]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:11.512 xcodebuild[91657:7387414] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-IlJaTt4D' (errno=Operation not permitted)
+2026-08-14 19:48:11.919 xcodebuild[91660:7387452]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:12.031 xcodebuild[91660:7387451] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+ create mode 100644 KERNEL-PROJECT.md
+ create mode 100755 scripts/sol-review.sh
+
+exec
+/bin/zsh -lc "find REVIEWS -maxdepth 2 -type f -print -exec sed -n '1,30p' {} \\; 2>/dev/null || true; git status --short" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 1393ms:
+2026-08-14 19:48:26.557 xcodebuild[91732:7388277]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:26.689 xcodebuild[91732:7388276] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
+git: error: couldn't create cache file '/tmp/xcrun_db-6elPFfCp' (errno=Operation not permitted)
+2026-08-14 19:48:27.094 xcodebuild[91734:7388288]  DVTFilePathFSEvents: Failed to start fs event stream.
+2026-08-14 19:48:27.203 xcodebuild[91734:7388287] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
+?? REVIEWS/
+
+2026-08-14T23:48:27.891537Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5
+
+2026-08-14T23:48:34.555694Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5
+exec
+/bin/zsh -lc "grep -n \"kernel_mul_mm_id\\|ne21_mm_id_min\\|get_pipeline_mul_mm_id\" ggml/src/ggml-metal/ggml-metal.metal ggml/src/ggml-metal/ggml-metal-ops.cpp ggml/src/ggml-metal/ggml-metal.m | head -80" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+grep: ggml/src/ggml-metal/ggml-metal.m: No such file or directory
+ggml/src/ggml-metal/ggml-metal.metal:10386:kernel void kernel_mul_mm_id_map0(
+ggml/src/ggml-metal/ggml-metal.metal:10439:typedef decltype(kernel_mul_mm_id_map0<1>) kernel_mul_mm_id_map0_t;
+ggml/src/ggml-metal/ggml-metal.metal:10441:template [[host_name("kernel_mul_mm_id_map0_ne20_1" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<1>;
+ggml/src/ggml-metal/ggml-metal.metal:10442:template [[host_name("kernel_mul_mm_id_map0_ne20_2" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<2>;
+ggml/src/ggml-metal/ggml-metal.metal:10443:template [[host_name("kernel_mul_mm_id_map0_ne20_4" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<4>;
+ggml/src/ggml-metal/ggml-metal.metal:10444:template [[host_name("kernel_mul_mm_id_map0_ne20_5" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<5>;
+ggml/src/ggml-metal/ggml-metal.metal:10445:template [[host_name("kernel_mul_mm_id_map0_ne20_6" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<6>;
+ggml/src/ggml-metal/ggml-metal.metal:10446:template [[host_name("kernel_mul_mm_id_map0_ne20_8" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<8>;
+ggml/src/ggml-metal/ggml-metal.metal:10447:template [[host_name("kernel_mul_mm_id_map0_ne20_10")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<10>;
+ggml/src/ggml-metal/ggml-metal.metal:10448:template [[host_name("kernel_mul_mm_id_map0_ne20_16")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<16>;
+ggml/src/ggml-metal/ggml-metal.metal:10449:template [[host_name("kernel_mul_mm_id_map0_ne20_22")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<22>;
+ggml/src/ggml-metal/ggml-metal.metal:10452:kernel void kernel_mul_mm_id(
+ggml/src/ggml-metal/ggml-metal.metal:10819:typedef decltype(kernel_mul_mm_id<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_id;
+ggml/src/ggml-metal/ggml-metal.metal:10821:template [[host_name("kernel_mul_mm_id_f32_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10822:template [[host_name("kernel_mul_mm_id_f16_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10824:template [[host_name("kernel_mul_mm_id_bf16_f32")]]    kernel mul_mm_id kernel_mul_mm_id<bfloat, bfloat4x4, simdgroup_bfloat8x8, bfloat, bfloat2x4, simdgroup_bfloat8x8, bfloat4x4,     1,     dequantize_bf16,    bfloat, bfloat4x4, float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10826:template [[host_name("kernel_mul_mm_id_q1_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q1_0,    8,     dequantize_q1_0,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10827:template [[host_name("kernel_mul_mm_id_q2_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_0,    4,     dequantize_q2_0,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10828:template [[host_name("kernel_mul_mm_id_q4_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_0,    2,     dequantize_q4_0,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10829:template [[host_name("kernel_mul_mm_id_q4_1_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_1,    2,     dequantize_q4_1,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10830:template [[host_name("kernel_mul_mm_id_q5_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_0,    2,     dequantize_q5_0,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10831:template [[host_name("kernel_mul_mm_id_q5_1_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_1,    2,     dequantize_q5_1,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10832:template [[host_name("kernel_mul_mm_id_q8_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q8_0,    2,     dequantize_q8_0,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10833:template [[host_name("kernel_mul_mm_id_mxfp4_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_mxfp4,   2,     dequantize_mxfp4,   float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10834:template [[host_name("kernel_mul_mm_id_q2_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_K,    QK_NL, dequantize_q2_K,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10835:template [[host_name("kernel_mul_mm_id_q3_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q3_K,    QK_NL, dequantize_q3_K,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10836:template [[host_name("kernel_mul_mm_id_q4_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_K,    QK_NL, dequantize_q4_K,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10837:template [[host_name("kernel_mul_mm_id_q5_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_K,    QK_NL, dequantize_q5_K,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10838:template [[host_name("kernel_mul_mm_id_q6_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q6_K,    QK_NL, dequantize_q6_K,    float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10839:template [[host_name("kernel_mul_mm_id_iq2_xxs_f32")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xxs, QK_NL, dequantize_iq2_xxs, float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10840:template [[host_name("kernel_mul_mm_id_iq2_xs_f32")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xs,  QK_NL, dequantize_iq2_xs,  float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10841:template [[host_name("kernel_mul_mm_id_iq3_xxs_f32")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_xxs, QK_NL, dequantize_iq3_xxs, float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10842:template [[host_name("kernel_mul_mm_id_iq3_s_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_s,   QK_NL, dequantize_iq3_s,   float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10843:template [[host_name("kernel_mul_mm_id_iq2_s_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_s,   QK_NL, dequantize_iq2_s,   float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10844:template [[host_name("kernel_mul_mm_id_iq1_s_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_s,   QK_NL, dequantize_iq1_s,   float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10845:template [[host_name("kernel_mul_mm_id_iq1_m_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_m,   QK_NL, dequantize_iq1_m,   float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10846:template [[host_name("kernel_mul_mm_id_iq4_nl_f32")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_nl,  2,     dequantize_iq4_nl,  float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10847:template [[host_name("kernel_mul_mm_id_iq4_xs_f32")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_xs,  QK_NL, dequantize_iq4_xs,  float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10849:template [[host_name("kernel_mul_mm_id_f32_f16")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10850:template [[host_name("kernel_mul_mm_id_f16_f16")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10851:template [[host_name("kernel_mul_mm_id_q1_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q1_0,    8,     dequantize_q1_0,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10852:template [[host_name("kernel_mul_mm_id_q2_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_0,    4,     dequantize_q2_0,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10853:template [[host_name("kernel_mul_mm_id_q4_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_0,    2,     dequantize_q4_0,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10854:template [[host_name("kernel_mul_mm_id_q4_1_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_1,    2,     dequantize_q4_1,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10855:template [[host_name("kernel_mul_mm_id_q5_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_0,    2,     dequantize_q5_0,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10856:template [[host_name("kernel_mul_mm_id_q5_1_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_1,    2,     dequantize_q5_1,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10857:template [[host_name("kernel_mul_mm_id_q8_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q8_0,    2,     dequantize_q8_0,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10858:template [[host_name("kernel_mul_mm_id_mxfp4_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_mxfp4,   2,     dequantize_mxfp4,   float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10859:template [[host_name("kernel_mul_mm_id_q2_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_K,    QK_NL, dequantize_q2_K,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10860:template [[host_name("kernel_mul_mm_id_q3_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q3_K,    QK_NL, dequantize_q3_K,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10861:template [[host_name("kernel_mul_mm_id_q4_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_K,    QK_NL, dequantize_q4_K,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10862:template [[host_name("kernel_mul_mm_id_q5_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_K,    QK_NL, dequantize_q5_K,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10863:template [[host_name("kernel_mul_mm_id_q6_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q6_K,    QK_NL, dequantize_q6_K,    float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10864:template [[host_name("kernel_mul_mm_id_iq2_xxs_f16")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xxs, QK_NL, dequantize_iq2_xxs, float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10865:template [[host_name("kernel_mul_mm_id_iq2_xs_f16")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xs,  QK_NL, dequantize_iq2_xs,  float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10866:template [[host_name("kernel_mul_mm_id_iq3_xxs_f16")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_xxs, QK_NL, dequantize_iq3_xxs, float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10867:template [[host_name("kernel_mul_mm_id_iq3_s_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_s,   QK_NL, dequantize_iq3_s,   float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10868:template [[host_name("kernel_mul_mm_id_iq2_s_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_s,   QK_NL, dequantize_iq2_s,   float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10869:template [[host_name("kernel_mul_mm_id_iq1_s_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_s,   QK_NL, dequantize_iq1_s,   float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10870:template [[host_name("kernel_mul_mm_id_iq1_m_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_m,   QK_NL, dequantize_iq1_m,   float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10871:template [[host_name("kernel_mul_mm_id_iq4_nl_f16")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_nl,  2,     dequantize_iq4_nl,  float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10872:template [[host_name("kernel_mul_mm_id_iq4_xs_f16")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_xs,  QK_NL, dequantize_iq4_xs,  float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal-ops.cpp:2593:    const int ne21_mm_id_min = 32;
+ggml/src/ggml-metal/ggml-metal-ops.cpp:2595:    if (props_dev->has_simdgroup_mm && ne00 >= 64 && (ne21 >= ne21_mm_id_min)) {
+ggml/src/ggml-metal/ggml-metal-ops.cpp:2624:            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id_map0(lib, ne02, ne20);
+ggml/src/ggml-metal/ggml-metal-ops.cpp:2647:            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id(lib, op);
+
+exec
+/bin/zsh -lc "sed -n '10452,10690p' ggml/src/ggml-metal/ggml-metal.metal; sed -n '2580,2685p' ggml/src/ggml-metal/ggml-metal-ops.cpp; find ggml/src/ggml-metal -maxdepth 2 -type f -name 'ggml-metal.m*' -print" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+kernel void kernel_mul_mm_id(
+        constant ggml_metal_kargs_mul_mm_id & args,
+        device const char * src0,
+        device const char * src1,
+        device const char * htpe,
+        device const char * hids,
+        device       char * dst,
+        threadgroup  char * shmem [[threadgroup(0)]],
+        uint3  tgpig[[threadgroup_position_in_grid]],
+        ushort tiitg[[thread_index_in_threadgroup]],
+        ushort tiisg[[thread_index_in_simdgroup]],
+        ushort sgitg[[simdgroup_index_in_threadgroup]]) {
+    threadgroup S0 * sa = (threadgroup S0 *)(shmem);
+    threadgroup S1 * sb = (threadgroup S1 *)(shmem + 4096);
+
+#ifdef GGML_METAL_HAS_TENSOR
+    threadgroup float * sc = (threadgroup float *)(shmem);
+#endif
+
+    constexpr int NR0 = 64;
+    constexpr int NR1 = 32;
+
+    constexpr int NK  = 32;
+    constexpr int NL0 = NK/16;
+    constexpr int NL1 = NK/8;
+
+    const int im = tgpig.z; // expert
+    const int r0 = tgpig.y*NR0;
+    const int r1 = tgpig.x*NR1;
+
+    device const uint32_t * tpe_u32 = (device const uint32_t *) (htpe);
+    device const int32_t  * ids_i32 = (device const int32_t  *) (hids);
+
+    const int32_t neh1 = tpe_u32[im];
+
+    if (r1 >= neh1) {
+        return;
+    }
+
+    // if this block is of 64x32 shape or smaller
+    const short nr0 = (args.ne0 - r0 < NR0) ? (args.ne0 - r0) : NR0;
+    const short nr1 = (    neh1 - r1 < NR1) ? (    neh1 - r1) : NR1;
+
+    // a thread shouldn't load data outside of the matrix
+    const short lr0 = ((short)tiitg/NL0) < nr0 ? ((short)tiitg/NL0) : nr0 - 1; // 0 .. 63
+    const short lr1 = ((short)tiitg/NL1) < nr1 ? ((short)tiitg/NL1) : nr1 - 1; // 0 .. 31
+
+    const short il0 = (tiitg % NL0);
+
+    short il = il0;
+
+    const int id = ids_i32[im*args.ne21 + r1 + lr1];
+
+    const short i11 = (id % args.ne20) % args.ne11;
+    const short i12 = (id / args.ne20);
+    const short i13 = 0;
+
+    const uint64_t offset0 = im*args.nb02 + i13*args.nb03;
+    const short    offset1 = il0/nl;
+
+    device const block_q * x = (device const block_q *)(src0 + args.nb01*(r0 + lr0) + offset0) + offset1;
+
+    const short iy = 8*(tiitg % NL1);
+
+    device const T1 * y = (device const T1 *)(src1
+        + args.nb13*i13
+        + args.nb12*i12
+        + args.nb11*i11
+        + args.nb10*iy);
+
+#ifndef GGML_METAL_HAS_TENSOR
+    S0_8x8 ma[4];
+    S1_8x8 mb[2];
+
+    simdgroup_float8x8 mc[8];
+
+    for (short i = 0; i < 8; i++){
+        mc[i] = make_filled_simdgroup_matrix<float, 8>(0.f);
+    }
+#else
+    auto tA = tensor<threadgroup S0, dextents<int32_t, 2>, tensor_inline>(sa, dextents<int32_t, 2>(NK,  NR0));
+    auto tB = tensor<threadgroup S1, dextents<int32_t, 2>, tensor_inline>(sb, dextents<int32_t, 2>(NR1, NK ));
+
+    mpp::tensor_ops::matmul2d<
+        mpp::tensor_ops::matmul2d_descriptor(NR1, NR0, NK, false, true, false, mpp::tensor_ops::matmul2d_descriptor::mode::multiply_accumulate),
+        execution_simdgroups<4>> mm;
+
+    auto cT = mm.get_destination_cooperative_tensor<decltype(tA), decltype(tB), float>();
+#endif
+
+    for (int loop_k = 0; loop_k < args.ne00; loop_k += NK) {
+#ifndef GGML_METAL_HAS_TENSOR
+        // load data and store to threadgroup memory
+        if (is_same<T0_4x4, block_q>::value && FC_mul_mm_bc_inp) {
+            threadgroup_barrier(mem_flags::mem_threadgroup);
+
+            // no need for dequantization
+            for (short i = 0; i < 16; i++) {
+                const short sx = 2*il0 + i/8;
+                const short sy = (tiitg/NL0)/8;
+
+              //const short lx = i%8;
+              //const short ly = (tiitg/NL0)%8;
+                const short lx = (tiitg/NL0)%8;
+                const short ly = i%8;
+
+                const short ib = 8*sx + sy;
+
+                *(sa + 64*ib + 8*ly + lx) = loop_k + 16*il + i < args.ne00 ? (S0) *((device T0 *) x + i) : (S0) 0;
+            }
+        } else {
+            S0_4x4 temp_a;
+            dequantize_func(x, il, temp_a);
+
+            threadgroup_barrier(mem_flags::mem_threadgroup);
+
+            FOR_UNROLL (short i = 0; i < 16; i++) {
+                const short sx = 2*il0 + i/8;
+                const short sy = (tiitg/NL0)/8;
+
+              //const short lx = i%8;
+              //const short ly = (tiitg/NL0)%8;
+                const short lx = (tiitg/NL0)%8;
+                const short ly = i%8;
+
+                const short ib = 8*sx + sy;
+
+                // NOTE: this is massively slower.. WTF?
+                //sa[64*ib + 8*ly + lx] = temp_a[i/4][i%4];
+
+                *(sa + 64*ib + 8*ly + lx) = temp_a[i/4][i%4];
+            }
+        }
+
+        if (FC_mul_mm_bc_inp) {
+            for (short i = 0; i < 8; ++i) {
+                const short sx = (tiitg%NL1);
+                const short sy = (tiitg/NL1)/8;
+
+                const short lx = i;
+                const short ly = (tiitg/NL1)%8;
+              //const short lx = (tiitg/NL1)%8;
+              //const short ly = i;
+
+                const short ib = 4*sx + sy;
+
+                *(sb + 64*ib + 8*ly + lx) = loop_k + iy + i < args.ne00 ? (S1) *((device T1 *) y + i) : 0;
+            }
+        } else {
+            const short sx = (tiitg%NL1);
+            const short sy = (tiitg/NL1)/8;
+
+          //const short dx = sx;
+          //const short dy = sy;
+
+            const short ly = (tiitg/NL1)%8;
+
+            const short ib = 4*sx + sy;
+
+            *(threadgroup S1_2x4 *)(sb + 64*ib + 8*ly) = (S1_2x4)(*((device T1_2x4 *) y));
+        }
+#else
+        // load data and store to threadgroup memory
+        if (is_same<T0_4x4, block_q>::value && FC_mul_mm_bc_inp) {
+            threadgroup_barrier(mem_flags::mem_threadgroup);
+
+            // no need for dequantization
+            for (short i = 0; i < 16; i++) {
+                const short sx = 2*il0 + i/8;
+                const short sy = (tiitg/NL0)/8;
+
+                const short lx = i%8;
+                const short ly = (tiitg/NL0)%8;
+                //const short lx = (tiitg/NL0)%8;
+                //const short ly = i%8;
+
+                *(sa + NK*(8*sy + ly) + 8*sx + lx) = loop_k + 16*il + i < args.ne00 ? *((device T0 *) x + i) : 0;
+            }
+        } else {
+            S0_4x4 temp_a;
+            dequantize_func(x, il, temp_a);
+
+            threadgroup_barrier(mem_flags::mem_threadgroup);
+
+            FOR_UNROLL (short i = 0; i < 16; i++) {
+                const short sx = 2*il0 + i/8;
+                const short sy = (tiitg/NL0)/8;
+
+                const short lx = i%8;
+                const short ly = (tiitg/NL0)%8;
+                //const short lx = (tiitg/NL0)%8;
+                //const short ly = i%8;
+
+                *(sa + NK*(8*sy + ly) + 8*sx + lx) = temp_a[i/4][i%4];
+            }
+        }
+
+        if (FC_mul_mm_bc_inp) {
+            for (short i = 0; i < 8; ++i) {
+                const short sx = (tiitg%NL1);
+                const short sy = (tiitg/NL1)/8;
+
+                const short lx = i;
+                const short ly = (tiitg/NL1)%8;
+                //const short lx = (tiitg/NL1)%8;
+                //const short ly = i;
+
+                *(sb + NK*(8*sy + ly) + 8*sx + lx) = loop_k + iy + i < args.ne00 ? (S1) *((device T1 *) y + i) : 0;
+            }
+        } else {
+            const short sx = (tiitg%NL1);
+            const short sy = (tiitg/NL1)/8;
+
+            //const short lx = i;
+            const short ly = (tiitg/NL1)%8;
+            //const short lx = (tiitg/NL1)%8;
+            //const short ly = i;
+
+            *(threadgroup S1_2x4 *)(sb + NK*(8*sy + ly) + 8*sx) = (S1_2x4)(*((device T1_2x4 *) y));
+        }
+#endif
+
+        il = (il + 2 < nl) ? il + 2 : il % 2;
+        x  = (il < 2) ? x + (2 + nl - 1)/nl : x;
+
+        y += NK;
+
+        threadgroup_barrier(mem_flags::mem_threadgroup);
+
+#ifndef GGML_METAL_HAS_TENSOR
+        // load matrices from threadgroup memory and conduct outer products
+        threadgroup const S0 * lsma = (sa + 4*64*(sgitg%2));
+        threadgroup const S1 * lsmb = (sb + 2*64*(sgitg/2));
+
+        FOR_UNROLL (short ik = 0; ik < NK/8; ik++) {
+            simdgroup_barrier(mem_flags::mem_none);
+
+            FOR_UNROLL (short i = 0; i < 4; i++) {
+                simdgroup_load(ma[i], lsma + 64*i, 8, 0, false);
+
+    ggml_metal_buffer_id bid_src0 = ggml_metal_get_buffer_id(op->src[0]);
+    ggml_metal_buffer_id bid_src1 = ggml_metal_get_buffer_id(op->src[1]);
+    ggml_metal_buffer_id bid_src2 = ggml_metal_get_buffer_id(op->src[2]);
+    ggml_metal_buffer_id bid_dst  = ggml_metal_get_buffer_id(op);
+
+    const uint32_t r2 = 1;
+    const uint32_t r3 = 1;
+
+    // find the break-even point where the matrix-matrix kernel becomes more efficient compared
+    // to the matrix-vector kernel
+    // ne20 = n_used_experts
+    // ne21 = n_rows (batch size)
+    const int ne21_mm_id_min = 32;
+
+    if (props_dev->has_simdgroup_mm && ne00 >= 64 && (ne21 >= ne21_mm_id_min)) {
+        // some Metal matrix data types require aligned pointers
+        // ref: https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf (Table 2.5)
+        //switch (op->src[0]->type) {
+        //    case GGML_TYPE_F32:  GGML_ASSERT(nb01 % 16 == 0); break;
+        //    case GGML_TYPE_F16:  GGML_ASSERT(nb01 % 8  == 0); break;
+        //    case GGML_TYPE_BF16: GGML_ASSERT(nb01 % 8  == 0); break;
+        //    default: break;
+        //}
+
+        // extra buffers for intermediate id mapping
+        ggml_metal_buffer_id bid_tpe = bid_dst;
+        bid_tpe.offs += ggml_nbytes(op);
+
+        ggml_metal_buffer_id bid_ids = bid_tpe;
+        bid_ids.offs += ggml_metal_op_mul_mat_id_extra_tpe(op);
+
+        {
+            ggml_metal_kargs_mul_mm_id_map0 args = {
+                ne02,
+                ne10,
+                ne11, // n_expert_used (bcast)
+                nb11,
+                nb12,
+                ne21, // n_tokens
+                ne20, // n_expert_used
+                nb21,
+            };
+
+            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id_map0(lib, ne02, ne20);
+
+            const size_t smem = pipeline.smem;
+
+            GGML_ASSERT(ne02 <= ggml_metal_pipeline_max_theads_per_threadgroup(pipeline));
+
+            GGML_ASSERT(smem <= props_dev->max_theadgroup_memory_size);
+
+            ggml_metal_encoder_set_pipeline(enc, pipeline);
+            ggml_metal_encoder_set_bytes   (enc, &args, sizeof(args), 0);
+            ggml_metal_encoder_set_buffer  (enc, bid_src2, 1);
+            ggml_metal_encoder_set_buffer  (enc, bid_tpe,  2);
+            ggml_metal_encoder_set_buffer  (enc, bid_ids,  3);
+
+            ggml_metal_encoder_set_threadgroup_memory_size(enc, smem, 0);
+
+            ggml_metal_encoder_dispatch_threadgroups(enc, 1, 1, 1, ne02, 1, 1);
+        }
+
+        // this barrier is always needed because the next kernel has to wait for the id maps to be computed
+        ggml_metal_op_concurrency_reset(ctx);
+
+        {
+            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id(lib, op);
+
+            ggml_metal_kargs_mul_mm_id args = {
+                /*.ne00  =*/ ne00,
+                /*.ne02  =*/ ne02,
+                /*.nb01  =*/ nb01,
+                /*.nb02  =*/ nb02,
+                /*.nb03  =*/ nb03,
+                /*.ne11  =*/ ne11, // n_expert_used (bcast)
+                /*.nb10  =*/ nb10,
+                /*.nb11  =*/ nb11,
+                /*.nb12  =*/ nb12,
+                /*.nb13  =*/ nb13,
+                /*.ne20  =*/ ne20, // n_expert_used
+                /*.ne21  =*/ ne21, // n_tokens
+                /*.ne0   =*/ ne0,
+                /*.ne1   =*/ ne1,
+                /*.r2    =*/ r2,
+                /*.r3    =*/ r3,
+            };
+
+            ggml_metal_encoder_set_pipeline(enc, pipeline);
+            ggml_metal_encoder_set_bytes   (enc, &args, sizeof(args), 0);
+            ggml_metal_encoder_set_buffer  (enc, bid_src0, 1);
+            ggml_metal_encoder_set_buffer  (enc, bid_src1, 2);
+            ggml_metal_encoder_set_buffer  (enc, bid_tpe,  3);
+            ggml_metal_encoder_set_buffer  (enc, bid_ids,  4);
+            ggml_metal_encoder_set_buffer  (enc, bid_dst,  5);
+
+            const size_t smem = pipeline.smem;
+
+            ggml_metal_encoder_set_threadgroup_memory_size(enc, smem, 0);
+
+            ggml_metal_encoder_dispatch_threadgroups(enc, (ne21 + 31)/32, (ne01 + 63)/64, ne02, 128, 1, 1);
+        }
+    } else {
+        auto pipeline = ggml_metal_library_get_pipeline_mul_mv_id(lib, op);
+
+        const int nr0 = pipeline.nr0;
+ggml/src/ggml-metal/ggml-metal.metal
+
+exec
+/bin/zsh -lc "nl -ba KERNEL-PROJECT.md | sed -n '68,92p'; grep -R \"threadExecutionWidth\\|simdgroup.*32\\|threadsPerThreadgroup\" -n ggml/src/ggml-metal | head -40" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+    68	  NOTE: consecutive bench runs throttle (~10-15% drift, power governor) -
+    69	  E1 A/Bs MUST be interleaved pairs, not sequential sweeps.
+    70	  Bench gotcha: llama-batched-bench needs -npl explicitly (empty n_pl =
+    71	  zero combos, header-only, exit 0).
+    72	
+    73	## E1 design (skinny-tile kernel_mul_mm_id, NR1=8) - the mission spec
+    74	
+    75	### Hypothesis
+    76	A 64x8-tile variant of kernel_mul_mm_id instantiated for IQ3_S (plus
+    77	Q4_0/Q8_0 as plumbing validation) beats the per-expert mul_mv_id path at
+    78	decode batches 2..16 on M4/Metal, because the weight tile is amortized
+    79	across 8 tokens instead of 1 (PR #25377 evidence on plain mul_mat:
+    80	2.02x at bs=8, Q4_0).
+    81	
+    82	### Surgery plan
+    83	1. COPY kernel_mul_mm_id (ggml-metal.metal:10452) as
+    84	   kernel_mul_mm_id_nr8 with NR1: 32 -> 8:
+    85	   - NR0=64, NR1=8, NK=32, NL0=NK/16=2, NL1=NK/8=4
+    86	   - threadgroup = 1 simdgroup (64 threads); accumulators: 8x
+    87	     simdgroup_float8x8 (64x8 tile)
+    88	   - sb shmem shrinks to NR1*NK*sizeof(S1); sa stays NR0*NK*sizeof(S0)
+    89	   - grid: tgpig.x = ceil(neh1/8), tgpig.y = ceil(ne0/64), tgpig.z = expert
+    90	   - careful with lr0/lr1 clamping + id lookup (ids_i32[im*ne21 + r1 + lr1])
+    91	2. Instantiate for block_iq3_s (QK_NL, dequantize_iq3_s) + block_q4_0 +
+    92	   block_q8_0 (validation quants). Register host_name in the template
+ggml/src/ggml-metal/ggml-metal-device.m:433:                (int)    obj.threadExecutionWidth);
+ggml/src/ggml-metal/ggml-metal-device.m:435:        if (obj.maxTotalThreadsPerThreadgroup == 0 || obj.threadExecutionWidth == 0) {
+ggml/src/ggml-metal/ggml-metal-device.m:510:    [encoder->obj dispatchThreadgroups:MTLSizeMake(tg0, tg1, tg2) threadsPerThreadgroup:MTLSizeMake(tptg0, tptg1, tptg2)];
+ggml/src/ggml-metal/ggml-metal-device.m:1385:            return has_simdgroup_reduction && op->src[2]->ne[0] % 32 == 0;
+ggml/src/ggml-metal/ggml-metal-ops.cpp:3218:        // each simdgroup has a full f32 head vector in shared mem to accumulate results
+ggml/src/ggml-metal/ggml-metal.metal:10760:typedef decltype(kernel_mul_mm<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_t;
+ggml/src/ggml-metal/ggml-metal.metal:10762:template [[host_name("kernel_mul_mm_f32_f32")]]     kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10790:template [[host_name("kernel_mul_mm_f32_f16")]]     kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  half, half2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10819:typedef decltype(kernel_mul_mm_id<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_id;
+ggml/src/ggml-metal/ggml-metal.metal:10821:template [[host_name("kernel_mul_mm_id_f32_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  float, float2x4>;
+ggml/src/ggml-metal/ggml-metal.metal:10849:template [[host_name("kernel_mul_mm_id_f32_f16")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  half, half2x4>;
+
+exec
+/bin/zsh -lc "grep -R \"pipeline_mul_mm_id\" -n ggml/src/ggml-metal | head -60; grep -R \"mul_mm_id.*smem\\|smem.*mul_mm_id\" -n ggml/src/ggml-metal | head -60; sed -n '10690,10760p' ggml/src/ggml-metal/ggml-metal.metal" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+ggml/src/ggml-metal/ggml-metal-ops.cpp:2624:            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id_map0(lib, ne02, ne20);
+ggml/src/ggml-metal/ggml-metal-ops.cpp:2647:            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id(lib, op);
+ggml/src/ggml-metal/ggml-metal-device.h:139:struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id_map0    (ggml_metal_library_t lib, int ne02, int ne20);
+ggml/src/ggml-metal/ggml-metal-device.h:140:struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id         (ggml_metal_library_t lib, const struct ggml_tensor * op);
+ggml/src/ggml-metal/ggml-metal-device.cpp:992:ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id_map0(ggml_metal_library_t lib, int ne02, int ne20) {
+ggml/src/ggml-metal/ggml-metal-device.cpp:1009:ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id(ggml_metal_library_t lib, const ggml_tensor * op) {
+                simdgroup_load(ma[i], lsma + 64*i, 8, 0, false);
+            }
+
+            simdgroup_barrier(mem_flags::mem_none);
+
+            FOR_UNROLL (short i = 0; i < 2; i++) {
+                simdgroup_load(mb[i], lsmb + 64*i, 8, 0, false);
+            }
+
+            simdgroup_barrier(mem_flags::mem_none);
+
+            FOR_UNROLL (short i = 0; i < 8; i++){
+                simdgroup_multiply_accumulate(mc[i], mb[i/4], ma[i%4], mc[i]);
+            }
+
+            lsma += 8*64;
+            lsmb += 4*64;
+        }
+#else
+        auto sA = tA.slice(0, 0);
+        auto sB = tB.slice(0, 0);
+
+        mm.run(sB, sA, cT);
+#endif
+    }
+
+    // block is smaller than 64x32, we should avoid writing data outside of the matrix
+    threadgroup_barrier(mem_flags::mem_threadgroup);
+
+#ifdef GGML_METAL_HAS_TENSOR
+    auto tC = tensor<threadgroup float, dextents<int32_t, 2>, tensor_inline>(sc, dextents<int32_t, 2>(NR0, NR1));
+    cT.store(tC);
+#else
+    threadgroup float * temp_str = ((threadgroup float *) shmem) + 32*(sgitg&1) + (16*(sgitg >> 1))*NR0;
+
+    for (short i = 0; i < 8; i++) {
+        simdgroup_store(mc[i], temp_str + 8*(i%4) + 8*NR0*(i/4), NR0, 0, false);
+    }
+#endif
+
+    threadgroup_barrier(mem_flags::mem_threadgroup);
+
+    for (short j = sgitg; j < nr1; j += 4) {
+        const int id = ids_i32[im*args.ne21 + r1 + j];
+
+        const short ide = id % args.ne20;
+        const short idt = id / args.ne20;
+
+        device float  * D  = (device float  *) dst + r0 + ide*args.ne0 + idt*args.ne1*args.ne0;
+        device float4 * D4 = (device float4 *) D;
+
+        threadgroup float  * C  = (threadgroup float  *) shmem + j*NR0;
+        threadgroup float4 * C4 = (threadgroup float4 *) C;
+
+        int i = tiisg;
+        for (; i < nr0/4; i += 32) {
+            *(D4 + i) = *(C4 + i);
+        }
+
+        i = (4*(nr0/4)) + tiisg;
+        for (; i < nr0; i += 32) {
+            *(D + i) = *(C + i);
+        }
+    }
+}
+
+//
+// matrix-matrix multiplication
+//
+
+typedef decltype(kernel_mul_mm<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_t;
+
+exec
+/bin/zsh -lc "grep -R \"npl\\|npp\\|ntg\" -n examples/batched-bench tools/batched-bench 2>/dev/null | head -100; find . -path '*batched-bench*' -maxdepth 4 -type f | head -30" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+tools/batched-bench/batched-bench.cpp:14:    LOG("\n    %s -m model.gguf -c 2048 -b 2048 -ub 512 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32 [-pps]\n", argv[0]);
+tools/batched-bench/README.md:13:./llama-batched-bench -m model.gguf -c 2048 -b 2048 -ub 512 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32 [-pps]
+tools/batched-bench/README.md:22:./llama-batched-bench -m ./models/llama-7b/ggml-model-q8_0.gguf -c 2048 -b 512 -ub 512 -ngl 999 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32
+./tools/batched-bench/batched-bench.cpp
+./tools/batched-bench/CMakeLists.txt
+./tools/batched-bench/README.md
+./tools/batched-bench/main.cpp
+./build/tools/batched-bench/Makefile
+./build/tools/batched-bench/cmake_install.cmake
+./build/tools/batched-bench/CTestTestfile.cmake
+./build/bin/llama-batched-bench
+./build/bin/libllama-batched-bench-impl.dylib
+./.cache/clangd/index/batched-bench.cpp.BCD1F5D84711BE71.idx
+
+exec
+/bin/zsh -lc "sed -n '1,220p' tools/batched-bench/batched-bench.cpp; grep -R \"params.n_batch\\|n_pl\" -n tools/batched-bench | head -120" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+#include "arg.h"
+#include "common.h"
+#include "log.h"
+#include "llama.h"
+
+#include <algorithm>
+#include <clocale>
+#include <cstdio>
+#include <string>
+#include <vector>
+
+static void print_usage(int, char ** argv) {
+    LOG("\nexample usage:\n");
+    LOG("\n    %s -m model.gguf -c 2048 -b 2048 -ub 512 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32 [-pps]\n", argv[0]);
+    LOG("\n");
+}
+
+// satisfies -Wmissing-declarations
+int llama_batched_bench(int argc, char ** argv);
+
+int llama_batched_bench(int argc, char ** argv) {
+    std::setlocale(LC_NUMERIC, "C");
+
+    common_params params;
+
+    common_init();
+
+    if (!common_params_parse(argc, argv, params, LLAMA_EXAMPLE_BENCH, print_usage)) {
+        return 1;
+    }
+
+    int is_pp_shared   = params.is_pp_shared;
+    int is_tg_separate = params.is_tg_separate;
+
+    std::vector<int> n_pp = params.n_pp;
+    std::vector<int> n_tg = params.n_tg;
+    std::vector<int> n_pl = params.n_pl;
+
+    // init LLM
+
+    llama_backend_init();
+    llama_numa_init(params.numa);
+
+    // initialize the model
+
+    llama_model_params model_params = common_model_params_to_llama(params);
+
+    llama_model * model = llama_model_load_from_file(params.model.path.c_str(), model_params);
+
+    if (model == NULL) {
+        fprintf(stderr , "%s: error: unable to load model\n" , __func__);
+        return 1;
+    }
+
+    llama_context_params ctx_params = common_context_params_to_llama(params);
+
+    // ensure enough sequences are available
+    ctx_params.n_seq_max = n_pl.empty() ? 1 : *std::max_element(n_pl.begin(), n_pl.end());
+
+    llama_context * ctx = llama_init_from_model(model, ctx_params);
+
+    if (ctx == NULL) {
+        fprintf(stderr , "%s: error: failed to create the llama_context\n" , __func__);
+        llama_model_free(model);
+        return 1;
+    }
+
+    const llama_vocab * vocab   = llama_model_get_vocab(model);
+    const int32_t       n_vocab = llama_vocab_n_tokens(vocab);
+
+    const auto get_token_rand = [n_vocab]() -> llama_token {
+        return std::rand() % n_vocab;
+    };
+
+    auto * mem = llama_get_memory(ctx);
+
+    const int32_t n_kv_max = llama_n_ctx(ctx);
+
+    llama_batch batch = llama_batch_init(n_kv_max, 0, 1);
+
+    // decode in batches of ctx_params.n_batch tokens
+    auto decode_helper = [](llama_context * ctx, llama_batch & batch, int32_t n_batch, bool synchronize) {
+        for (int32_t i = 0; i < batch.n_tokens; i += n_batch) {
+            const int32_t n_tokens = std::min(n_batch, batch.n_tokens - i);
+
+            llama_batch batch_view = {
+                n_tokens,
+                batch.token    + i,
+                nullptr,
+                batch.pos      + i,
+                batch.n_seq_id + i,
+                batch.seq_id   + i,
+                batch.logits   + i,
+            };
+
+            const int ret = llama_decode(ctx, batch_view);
+            if (ret != 0) {
+                LOG_ERR("failed to decode the batch, n_batch = %d, ret = %d\n", n_batch, ret);
+                return false;
+            }
+
+            if (synchronize) {
+                llama_synchronize(ctx);
+            }
+        }
+
+        return true;
+    };
+
+    // warm up
+    {
+        for (int i = 0; i < 16; ++i) {
+            common_batch_add(batch, get_token_rand(), i, { 0 }, false);
+        }
+
+        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+            LOG_ERR("%s: llama_decode() failed\n", __func__);
+            llama_free(ctx);
+            llama_model_free(model);
+            return 1;
+        }
+    }
+
+    if (!params.batched_bench_output_jsonl) {
+        LOG("\n");
+        LOG("%s: n_kv_max = %d, n_batch = %d, n_ubatch = %d, flash_attn = %d, is_pp_shared = %d, is_tg_separate = %d, n_gpu_layers = %d, n_threads = %u, n_threads_batch = %u\n", __func__, n_kv_max, params.n_batch, params.n_ubatch, int(params.flash_attn_type), is_pp_shared, is_tg_separate, params.n_gpu_layers, ctx_params.n_threads, ctx_params.n_threads_batch);
+        LOG("\n");
+        LOG("|%6s | %6s | %4s | %6s | %8s | %8s | %8s | %8s | %8s | %8s |\n", "PP", "TG", "B", "N_KV", "T_PP s", "S_PP t/s", "T_TG s", "S_TG t/s", "T s", "S t/s");
+        LOG("|%6s-|-%6s-|-%4s-|-%6s-|-%8s-|-%8s-|-%8s-|-%8s-|-%8s-|-%8s-|\n", "------", "------", "----", "------", "--------", "--------", "--------", "--------", "--------", "--------");
+    }
+
+    for (        int i_pp = 0; i_pp < (int) n_pp.size(); ++i_pp) {
+        for (    int i_tg = 0; i_tg < (int) n_tg.size(); ++i_tg) {
+            for (int i_pl = 0; i_pl < (int) n_pl.size(); ++i_pl) {
+                const int pp = n_pp[i_pp];
+                const int tg = n_tg[i_tg];
+                const int pl = n_pl[i_pl];
+
+                const int n_ctx_req = is_pp_shared ? (params.kv_unified ? pp : pl*pp) + pl*tg : pl*(pp + tg);
+
+                if (n_ctx_req > n_kv_max) {
+                    continue;
+                }
+
+                common_batch_clear(batch);
+
+                for (int j = 0; j < (is_pp_shared ? 1 : pl); ++j) {
+                    for (int i = 0; i < pp; ++i) {
+                        common_batch_add(batch, get_token_rand(), i, { j }, i == pp - 1);
+                    }
+                }
+
+                llama_memory_clear(mem, false);
+
+                const auto t_pp_start = ggml_time_us();
+
+                if (!decode_helper(ctx, batch, ctx_params.n_batch, false)) {
+                    LOG_ERR("%s: llama_decode() failed\n", __func__);
+                    llama_free(ctx);
+                    llama_model_free(model);
+                    return 1;
+                }
+
+                llama_synchronize(ctx);
+
+                const auto t_pp_end = ggml_time_us();
+
+                if (is_pp_shared) {
+                    for (int32_t i = 1; i < pl; ++i) {
+                        llama_memory_seq_cp(mem, 0, i, -1, -1);
+                    }
+
+                    if (!params.kv_unified) {
+                        // run one dummy token to apply the memory copy
+                        common_batch_clear(batch);
+                        common_batch_add(batch, get_token_rand(), pp + 0, { 0 }, true);
+                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+                            LOG_ERR("%s: llama_decode() failed\n", __func__);
+                            llama_free(ctx);
+                            llama_model_free(model);
+                            return 1;
+                        }
+                        llama_memory_seq_rm(mem, 0, pp, -1);
+                    }
+                }
+
+                const auto t_tg_start = ggml_time_us();
+
+                if (is_tg_separate) {
+                    // decode pattern:
+                    // 0 0 0 ... 1 1 1 ... 2 2 2 ... 3 3 3 ...
+                    for (int j = 0; j < pl; ++j) {
+                        for (int i = 0; i < tg; ++i) {
+                            common_batch_clear(batch);
+
+                            common_batch_add(batch, get_token_rand(), pp + i, { j }, true);
+
+                            if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+                                LOG_ERR("%s: llama_decode() failed\n", __func__);
+                                llama_free(ctx);
+                                llama_model_free(model);
+                                return 1;
+                            }
+                        }
+                    }
+                } else {
+                    // decode pattern:
+                    // 0123 0123 0123 ...
+                    for (int i = 0; i < tg; ++i) {
+                        common_batch_clear(batch);
+
+                        for (int j = 0; j < pl; ++j) {
+                            common_batch_add(batch, get_token_rand(), pp + i, { j }, true);
+                        }
+
+                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+                            LOG_ERR("%s: llama_decode() failed\n", __func__);
+                            llama_free(ctx);
+                            llama_model_free(model);
+                            return 1;
+tools/batched-bench/batched-bench.cpp:37:    std::vector<int> n_pl = params.n_pl;
+tools/batched-bench/batched-bench.cpp:58:    ctx_params.n_seq_max = n_pl.empty() ? 1 : *std::max_element(n_pl.begin(), n_pl.end());
+tools/batched-bench/batched-bench.cpp:81:    // decode in batches of ctx_params.n_batch tokens
+tools/batched-bench/batched-bench.cpp:116:        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+tools/batched-bench/batched-bench.cpp:126:        LOG("%s: n_kv_max = %d, n_batch = %d, n_ubatch = %d, flash_attn = %d, is_pp_shared = %d, is_tg_separate = %d, n_gpu_layers = %d, n_threads = %u, n_threads_batch = %u\n", __func__, n_kv_max, params.n_batch, params.n_ubatch, int(params.flash_attn_type), is_pp_shared, is_tg_separate, params.n_gpu_layers, ctx_params.n_threads, ctx_params.n_threads_batch);
+tools/batched-bench/batched-bench.cpp:134:            for (int i_pl = 0; i_pl < (int) n_pl.size(); ++i_pl) {
+tools/batched-bench/batched-bench.cpp:137:                const int pl = n_pl[i_pl];
+tools/batched-bench/batched-bench.cpp:157:                if (!decode_helper(ctx, batch, ctx_params.n_batch, false)) {
+tools/batched-bench/batched-bench.cpp:177:                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+tools/batched-bench/batched-bench.cpp:198:                            if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+tools/batched-bench/batched-bench.cpp:216:                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+tools/batched-bench/batched-bench.cpp:241:                        n_kv_max, params.n_batch, params.n_ubatch, int(params.flash_attn_type), params.is_pp_shared, params.n_gpu_layers, ctx_params.n_threads, ctx_params.n_threads_batch,
+
+exec
+/bin/zsh -lc "nl -ba KERNEL-PROJECT.md | sed -n '34,50p;92,108p'; sed -n '210,252p' tools/batched-bench/batched-bench.cpp" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+    34	- E3: speculative decode (--spec-type draft-mtp): verify batch (1+n_draft
+    35	  ~= 3-6 tokens) routes to the fast path -> single-stream 1.2-1.4x target.
+    36	  Requires server spec wiring at this commit (verified present).
+    37	
+    38	## Acceptance criteria (every milestone)
+    39	
+    40	1. Logit-identical: same prompt through baseline + patched builds,
+    41	   llama-eval-callback logit dump, max-abs-diff < 1e-4.
+    42	2. >= 10% on the milestone's target metric (batched-bench S_TG at the
+    43	   batch size, or aggregate server t/s, or spec-dec single-stream).
+    44	3. No regression at batch 1 (the mv path must stay untouched for ne21=1).
+    45	4. Proxy gates (scripts/proxy.sh t1-t3) still pass for the server config.
+    46	
+    47	## Method
+    48	
+    49	- Branch metal-moe-kernel off champion-speedup (infra inherited).
+    50	- Experiments as small commits; keep ne21=1 path bit-identical.
+    92	   block_q8_0 (validation quants). Register host_name in the template
+    93	   block; wire pipeline lookup in ggml-metal.m (mirror
+    94	   ggml_metal_library_get_pipeline_mul_mm_id).
+    95	3. Dispatch (ggml-metal-ops.cpp MUL_MAT_ID case): new branch
+    96	   (ne21 >= 2 && ne21 <= 16 && quant in {iq3_s, q4_0, q8_0}) ->
+    97	   nr8 pipeline; keep mv at ne21==1; keep existing mm at ne21>=32.
+    98	4. Build, then INTERLEAVED A/B: baseline vs patched, llama-batched-bench
+    99	   -b 2,4,8,16,32 pairs (order A B A B), -c 2048 -npp 32 -ntg 64 -npl 1.
+   100	5. Logit verification: llama-eval-callback dump (same prompt, baseline vs
+   101	   patched build) max-abs-diff < 1e-4. The mm path is already used in
+   102	   prefill >= 32 tokens in production, so numerical parity is expected.
+   103	6. If nr8 wins at 2..16: extend dispatch, then E2 (multi-slot server
+   104	   aggregate: ne21 = active slots) and E3 (spec-verify batch -> single-
+   105	   stream 1.2-1.4x via --spec-type draft-mtp).
+   106	
+   107	### Kill criteria (per variant)
+   108	- Interleaved A/B shows < 5% gain at b=8 -> variant dead, try next
+                        common_batch_clear(batch);
+
+                        for (int j = 0; j < pl; ++j) {
+                            common_batch_add(batch, get_token_rand(), pp + i, { j }, true);
+                        }
+
+                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
+                            LOG_ERR("%s: llama_decode() failed\n", __func__);
+                            llama_free(ctx);
+                            llama_model_free(model);
+                            return 1;
+                        }
+                    }
+                }
+
+                const auto t_tg_end = ggml_time_us();
+
+                const int32_t n_kv = n_ctx_req;
+
+                const float t_pp = (t_pp_end - t_pp_start) / 1000000.0f;
+                const float t_tg = (t_tg_end - t_tg_start) / 1000000.0f;
+                const float t    = t_pp + t_tg;
+
+                const float speed_pp = is_pp_shared ? pp / t_pp : pl*pp / t_pp;
+                const float speed_tg = pl*tg / t_tg;
+                const float speed    = ((is_pp_shared ? pp : pl*pp) + pl*tg) / t;
+
+                if(params.batched_bench_output_jsonl) {
+                    LOG(
+                        "{\"n_kv_max\": %d, \"n_batch\": %d, \"n_ubatch\": %d, \"flash_attn\": %d, \"is_pp_shared\": %d, \"n_gpu_layers\": %d, \"n_threads\": %u, \"n_threads_batch\": %u, "
+                        "\"pp\": %d, \"tg\": %d, \"pl\": %d, \"n_kv\": %d, \"t_pp\": %f, \"speed_pp\": %f, \"t_tg\": %f, \"speed_tg\": %f, \"t\": %f, \"speed\": %f}\n",
+                        n_kv_max, params.n_batch, params.n_ubatch, int(params.flash_attn_type), params.is_pp_shared, params.n_gpu_layers, ctx_params.n_threads, ctx_params.n_threads_batch,
+                        pp, tg, pl, n_kv, t_pp, speed_pp, t_tg, speed_tg, t, speed
+                    );
+                } else {
+                    LOG("|%6d | %6d | %4d | %6d | %8.3f | %8.2f | %8.3f | %8.2f | %8.3f | %8.2f |\n", pp, tg, pl, n_kv, t_pp, speed_pp, t_tg, speed_tg, t, speed);
+                }
+            }
+        }
+    }
+
+    LOG("\n");
+    llama_perf_context_print(ctx);
+
+exec
+/bin/zsh -lc "nl -ba KERNEL-PROJECT.md | sed -n '47,72p'" in /Users/davidfeliperiveraguerra/dev/llama.cpp
+ succeeded in 0ms:
+    47	## Method
+    48	
+    49	- Branch metal-moe-kernel off champion-speedup (infra inherited).
+    50	- Experiments as small commits; keep ne21=1 path bit-identical.
+    51	- Memory discipline: stop the champion server before llama-batched-bench
+    52	  (two 15GB model loads never fit 24GB); same memory state for A/B pairs.
+    53	- Benchmark: llama-batched-bench -npp 32 -ntg 128 -b N sweep, S_TG column.
+    54	- Journal every step here + MISSION-REPORT.md.
+    55	
+    56	## Progress log
+    57	
+    58	- 2026-08-14: M0 done. E0 dispatched: baseline sweep running.
+    59	- 2026-08-14 E0 RESULT: DISPATCH-ONLY IS DEAD. Threshold 32->2 routes
+    60	  everything to the existing 64x32 mm kernel; measured REGRESSION at every
+    61	  batch (throttle-noise-corrected, interleaved pairs recommended for E1):
+    62	    baseline (mv<32 / mm@32): b=2 29.67, b=8 29.79, b=32 30.13 t/s decode
+    63	    patched (mm everywhere):  b=1 24.92, b=2 24.38, b=4 25.84, b=8 24.88,
+    64	    b=16 25.78, b=32 26.42
+    65	  The 64x32 tile zero-pads below 32 tokens (~1/32 utilization at b=1).
+    66	  Prefill also regressed at small batches (S_PP b=8: 61.6 -> 39.9).
+    67	  REVERTED (ops.cpp restored to threshold 32, rebuilt).
+    68	  NOTE: consecutive bench runs throttle (~10-15% drift, power governor) -
+    69	  E1 A/Bs MUST be interleaved pairs, not sequential sweeps.
+    70	  Bench gotcha: llama-batched-bench needs -npl explicitly (empty n_pl =
+    71	  zero combos, header-only, exit 0).
+    72	
+
+codex
+The prescribed benchmark never exercises the dispatch being evaluated, and the watchdog has multiple reliability failures, including an endless self-generated commit loop and an invalid custom-prompt invocation. These issues prevent the changes from fulfilling their stated purpose.
+
+Full review comments:
+
+- [P1] Sweep parallel sequences to exercise nr8 dispatch — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:98-99
+  With `-npl 1`, the TG loop submits exactly one token per `llama_decode`, while `-b` only caps the decode chunk size. Consequently `ne21` remains 1 and the proposed `ne21 >= 2` branch is never measured, making the A/B and kill decision invalid; sweep `-npl` and keep `-b` sufficiently large instead.
+
+- [P1] Stop the watchdog from committing its own marker — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:69-71
+  The marker records the pre-review `HEAD`, then the commit advances `HEAD`, so the next cron run sees that generated review commit as new work and repeats indefinitely even without source changes. This also violates the repository prohibition on automated commits (`AGENTS.md:49`); keep review state outside branch history or require explicit approval.
+
+- [P2] Correct the SIMDgroup thread count — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:86-87
+  Apple Metal SIMDgroups contain 32 threads, so a 64-thread threadgroup consists of two SIMDgroups, not one. Implementing accumulator or shared-memory ownership from this contradictory specification risks duplicated or missing work; specify either one 32-thread SIMDgroup or an explicit two-SIMDgroup partition.
+
+- [P2] Ensure the custom prompt reaches Codex — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:58-61
+  In Codex 0.146, `-` still occupies the positional `[PROMPT]`, so combining it with `--base` exits with status 2. The first invocation therefore fails every run and the fallback performs a generic review without `REVIEW_PROMPT`, meaning the targeted Metal checks are never applied.
+
+- [P2] Review from the last-reviewed commit — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:63-66
+  After the marker exists, the report claims the range is `$LAST..HEAD`, but the successful review invocation still uses `--base "$BASE_REF"`. Every run therefore re-reviews the complete branch since `champion-speedup`, duplicating old findings and attributing them to only the latest commits.
+
+- [P2] Do not mark failed reviews complete — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:66-69
+  If the fallback review fails because of authentication, network, or model errors, `|| echo` converts the failure into success and execution advances the marker and prints that the commits were reviewed. This gives the watchdog and audit trail a false success state; only update the marker after a successful Codex exit.
+
+- [P2] Avoid SIGPIPE when truncating the log — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:47-47
+  When the range contains more than 25 commits, `head` closes the pipe and `git log` exits with SIGPIPE status 141. Because `pipefail` and `set -e` are enabled, this aborts the script before the review; use Git's own maximum-count option instead.
+
+- [P2] Reject missing base references — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:30-33
+  If neither the local base nor `origin/$BASE` exists, `git rev-list` fails but `|| echo "0"` converts the configuration error into zero new commits, causing a silent successful exit. A misspelled argument or unfetched remote therefore disables the watchdog without warning.
+
+- [P3] Point the plan at the actual pipeline registry — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:92-94
+  The referenced `ggml-metal.m` file does not exist in this tree. `ggml_metal_library_get_pipeline_mul_mm_id` is implemented in `ggml/src/ggml-metal/ggml-metal-device.cpp` and declared in `ggml-metal-device.h`, so the implementation plan currently directs step 2 to a nonexistent registration site.
+The prescribed benchmark never exercises the dispatch being evaluated, and the watchdog has multiple reliability failures, including an endless self-generated commit loop and an invalid custom-prompt invocation. These issues prevent the changes from fulfilling their stated purpose.
+
+Full review comments:
+
+- [P1] Sweep parallel sequences to exercise nr8 dispatch — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:98-99
+  With `-npl 1`, the TG loop submits exactly one token per `llama_decode`, while `-b` only caps the decode chunk size. Consequently `ne21` remains 1 and the proposed `ne21 >= 2` branch is never measured, making the A/B and kill decision invalid; sweep `-npl` and keep `-b` sufficiently large instead.
+
+- [P1] Stop the watchdog from committing its own marker — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:69-71
+  The marker records the pre-review `HEAD`, then the commit advances `HEAD`, so the next cron run sees that generated review commit as new work and repeats indefinitely even without source changes. This also violates the repository prohibition on automated commits (`AGENTS.md:49`); keep review state outside branch history or require explicit approval.
+
+- [P2] Correct the SIMDgroup thread count — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:86-87
+  Apple Metal SIMDgroups contain 32 threads, so a 64-thread threadgroup consists of two SIMDgroups, not one. Implementing accumulator or shared-memory ownership from this contradictory specification risks duplicated or missing work; specify either one 32-thread SIMDgroup or an explicit two-SIMDgroup partition.
+
+- [P2] Ensure the custom prompt reaches Codex — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:58-61
+  In Codex 0.146, `-` still occupies the positional `[PROMPT]`, so combining it with `--base` exits with status 2. The first invocation therefore fails every run and the fallback performs a generic review without `REVIEW_PROMPT`, meaning the targeted Metal checks are never applied.
+
+- [P2] Review from the last-reviewed commit — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:63-66
+  After the marker exists, the report claims the range is `$LAST..HEAD`, but the successful review invocation still uses `--base "$BASE_REF"`. Every run therefore re-reviews the complete branch since `champion-speedup`, duplicating old findings and attributing them to only the latest commits.
+
+- [P2] Do not mark failed reviews complete — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:66-69
+  If the fallback review fails because of authentication, network, or model errors, `|| echo` converts the failure into success and execution advances the marker and prints that the commits were reviewed. This gives the watchdog and audit trail a false success state; only update the marker after a successful Codex exit.
+
+- [P2] Avoid SIGPIPE when truncating the log — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:47-47
+  When the range contains more than 25 commits, `head` closes the pipe and `git log` exits with SIGPIPE status 141. Because `pipefail` and `set -e` are enabled, this aborts the script before the review; use Git's own maximum-count option instead.
+
+- [P2] Reject missing base references — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:30-33
+  If neither the local base nor `origin/$BASE` exists, `git rev-list` fails but `|| echo "0"` converts the configuration error into zero new commits, causing a silent successful exit. A misspelled argument or unfetched remote therefore disables the watchdog without warning.
+
+- [P3] Point the plan at the actual pipeline registry — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:92-94
+  The referenced `ggml-metal.m` file does not exist in this tree. `ggml_metal_library_get_pipeline_mul_mm_id` is implemented in `ggml/src/ggml-metal/ggml-metal-device.cpp` and declared in `ggml-metal-device.h`, so the implementation plan currently directs step 2 to a nonexistent registration site.

--- second ---
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-69Gz02jd' (errno=Operation not permitted)
2026-08-14 19:53:53.118 xcodebuild[98975:7418637]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:53.230 xcodebuild[98975:7418636] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-SYlJaF9x' (errno=Operation not permitted)
2026-08-14 19:53:53.597 xcodebuild[98977:7418654]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:53.708 xcodebuild[98977:7418653] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
e213b154c sol-review: v2 fixes from Sol's first review (marker scope, failure handling, SIGPIPE, base validation) + kernel-plan registry path correction
 KERNEL-PROJECT.md     |  6 ++++--
 scripts/sol-review.sh | 44 +++++++++++++++++++++++++++++++-------------
 2 files changed, 35 insertions(+), 15 deletions(-)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-ULS60DmU' (errno=Operation not permitted)
2026-08-14 19:53:54.546 xcodebuild[98981:7418679]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:54.657 xcodebuild[98981:7418678] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-mPnLGLLU' (errno=Operation not permitted)
2026-08-14 19:53:55.048 xcodebuild[98983:7418692]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:53:55.161 xcodebuild[98983:7418691] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
commit e213b154c30966836c2d576bf72423819442e38b
Author:     David Rivera <davidriverg@gmail.com>
AuthorDate: Fri Aug 14 19:52:48 2026 -0400
Commit:     David Rivera <davidriverg@gmail.com>
CommitDate: Fri Aug 14 19:52:48 2026 -0400

    sol-review: v2 fixes from Sol's first review (marker scope, failure handling, SIGPIPE, base validation) + kernel-plan registry path correction

diff --git a/KERNEL-PROJECT.md b/KERNEL-PROJECT.md
index 0799fa5ca..eba902c57 100644
--- a/KERNEL-PROJECT.md
+++ b/KERNEL-PROJECT.md
@@ -63,52 +63,54 @@ batch 32, the fast mm GEMM above).
     patched (mm everywhere):  b=1 24.92, b=2 24.38, b=4 25.84, b=8 24.88,
     b=16 25.78, b=32 26.42
   The 64x32 tile zero-pads below 32 tokens (~1/32 utilization at b=1).
   Prefill also regressed at small batches (S_PP b=8: 61.6 -> 39.9).
   REVERTED (ops.cpp restored to threshold 32, rebuilt).
   NOTE: consecutive bench runs throttle (~10-15% drift, power governor) -
   E1 A/Bs MUST be interleaved pairs, not sequential sweeps.
   Bench gotcha: llama-batched-bench needs -npl explicitly (empty n_pl =
   zero combos, header-only, exit 0).
 
 ## E1 design (skinny-tile kernel_mul_mm_id, NR1=8) - the mission spec
 
 ### Hypothesis
 A 64x8-tile variant of kernel_mul_mm_id instantiated for IQ3_S (plus
 Q4_0/Q8_0 as plumbing validation) beats the per-expert mul_mv_id path at
 decode batches 2..16 on M4/Metal, because the weight tile is amortized
 across 8 tokens instead of 1 (PR #25377 evidence on plain mul_mat:
 2.02x at bs=8, Q4_0).
 
 ### Surgery plan
 1. COPY kernel_mul_mm_id (ggml-metal.metal:10452) as
    kernel_mul_mm_id_nr8 with NR1: 32 -> 8:
    - NR0=64, NR1=8, NK=32, NL0=NK/16=2, NL1=NK/8=4
    - threadgroup = 1 simdgroup (64 threads); accumulators: 8x
      simdgroup_float8x8 (64x8 tile)
    - sb shmem shrinks to NR1*NK*sizeof(S1); sa stays NR0*NK*sizeof(S0)
    - grid: tgpig.x = ceil(neh1/8), tgpig.y = ceil(ne0/64), tgpig.z = expert
    - careful with lr0/lr1 clamping + id lookup (ids_i32[im*ne21 + r1 + lr1])
 2. Instantiate for block_iq3_s (QK_NL, dequantize_iq3_s) + block_q4_0 +
    block_q8_0 (validation quants). Register host_name in the template
-   block; wire pipeline lookup in ggml-metal.m (mirror
-   ggml_metal_library_get_pipeline_mul_mm_id).
+   block; wire pipeline lookup in ggml-metal-device.cpp (mirror
+   ggml_metal_library_get_pipeline_mul_mm_id at ggml-metal-device.cpp:1009
+   - corrected 2026-08-14 per Sol review; the .metal file is the shader
+   source, the registry lives in ggml-metal-device.cpp).
 3. Dispatch (ggml-metal-ops.cpp MUL_MAT_ID case): new branch
    (ne21 >= 2 && ne21 <= 16 && quant in {iq3_s, q4_0, q8_0}) ->
    nr8 pipeline; keep mv at ne21==1; keep existing mm at ne21>=32.
 4. Build, then INTERLEAVED A/B: baseline vs patched, llama-batched-bench
    -b 2,4,8,16,32 pairs (order A B A B), -c 2048 -npp 32 -ntg 64 -npl 1.
 5. Logit verification: llama-eval-callback dump (same prompt, baseline vs
    patched build) max-abs-diff < 1e-4. The mm path is already used in
    prefill >= 32 tokens in production, so numerical parity is expected.
 6. If nr8 wins at 2..16: extend dispatch, then E2 (multi-slot server
    aggregate: ne21 = active slots) and E3 (spec-verify batch -> single-
    stream 1.2-1.4x via --spec-type draft-mtp).
 
 ### Kill criteria (per variant)
 - Interleaved A/B shows < 5% gain at b=8 -> variant dead, try next
   (NR1=4? 16? different NK? Q4_0-first validation).
 - Logit diff > 1e-4 -> correctness bug, revert variant.
 - Batch-1 decode regresses > 2% -> revert immediately (mv path must stay
   for ne21=1; the nr8 branch is gated on ne21>=2 anyway).
 - 3 consecutive variants < 5% -> E1 dead, report PARTIAL, keep threshold
   at 32 (current behavior is the safe state).
diff --git a/scripts/sol-review.sh b/scripts/sol-review.sh
index 6b36444db..7b19c55cf 100755
--- a/scripts/sol-review.sh
+++ b/scripts/sol-review.sh
@@ -1,79 +1,97 @@
 #!/bin/bash
 # sol-review.sh - overnight Sol review pass for the metal-moe-kernel branch.
 # Reviews the diff since the last review with gpt-5.6-sol (via codex review,
 # riding the ChatGPT Plus subscription), writes REVIEWS/<date>.md (committed
 # to the branch so the fork shows the audit trail), prints a compact digest.
 #
 # Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
 #
-# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
-# short (empty-ish output on no-new-commits would be ideal but a nightly
-# review of even 1 commit is the point - this is a review loop, not a
-# change-detector).
+# Cron: no_agent watchdog - stdout delivered verbatim; silent when no new
+# commits; non-zero exit on review failure (cron alerts).
+#
+# v2 fixes (from Sol's own review of v1, REVIEWS/2026-08-14.md):
+# - review --base <marker> so each run covers ONLY new commits
+# - marker advances only on codex success; failures exit nonzero
+# - git log -25 (no SIGPIPE via head)
+# - missing base ref = error, not silent success
 set -euo pipefail
 
 ROOT="$(cd "$(dirname "$0")/.." && pwd)"
 cd "$ROOT"
 
 BASE="${1:-champion-speedup}"
 REVIEWS_DIR="$ROOT/REVIEWS"
 MARKER="$REVIEWS_DIR/.last-reviewed"
 mkdir -p "$REVIEWS_DIR"
 
-# Locate the base ref (local branch or remote)
-if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
+# Resolve the base ref; a missing base is a config error, not "no work".
+if git rev-parse --verify --quiet "$BASE" >/dev/null; then
     BASE_REF="$BASE"
-else
+elif git rev-parse --verify --quiet "origin/$BASE" >/dev/null; then
     BASE_REF="origin/$BASE"
+else
+    echo "sol-review: base '$BASE' not found locally or on origin" >&2
+    exit 2
 fi
 
+# The ref to diff against: the last-reviewed marker, else the base branch.
 LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
 
 # Number of new commits since last review
 NCOMMITS=$(git rev-list --count "$LAST..HEAD" 2>/dev/null || echo "0")
 if [ "$NCOMMITS" = "0" ]; then
     # silent tick (watchdog pattern): nothing new, don't wake the user
     exit 0
 fi
 
 OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
 {
     echo "# Sol review $(date "+%Y-%m-%d %H:%M")"
     echo
     echo "- Range: $LAST..HEAD ($NCOMMITS commits)"
     echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
     echo
     echo '```'
-    git log --oneline "$LAST..HEAD" | head -25
+    git log --oneline -25 "$LAST..HEAD"
     echo '```'
     echo
     echo "## Findings"
     echo
 } > "$OUT"
 
 REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-moe-kernel project). Focus: (1) threadgroup/tile math (NR0/NR1/NK, shmem sizing, grid dimensions), (2) dispatch invariants (ne21 gates, quant lists, ne21=1 path must stay on mul_mv_id), (3) logit-correctness risks (dequant, id lookup ids_i32[im*ne21+r1+lr1], barriers), (4) memory hazards (threadgroup races, missing barriers), (5) regression risk to batch-1 decode and to the prefill mm path at >=32. Assume the author is wrong; find the flaw. Quote exact line numbers."
 
 # codex 0.146: --base cannot be combined with a positional [PROMPT]; the
 # stdin form ('-') is the workaround. Fall back to the built-in prompt.
-if ! codex review --base "$BASE_REF" \
+REVIEW_OK=0
+if codex review --base "$LAST" \
         -c model="gpt-5.6-sol" \
         -c model_reasoning_effort="high" \
         - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
+    REVIEW_OK=1
+else
     echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
-    codex review --base "$BASE_REF" \
-        -c model="gpt-5.6-sol" \
-        -c model_reasoning_effort="high" \
-        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
+    if codex review --base "$LAST" \
+            -c model="gpt-5.6-sol" \
+            -c model_reasoning_effort="high" \
+            >> "$OUT" 2>&1; then
+        REVIEW_OK=1
+    fi
+fi
+
+if [ "$REVIEW_OK" != "1" ]; then
+    echo "sol-review FAILED (codex review error) - marker NOT advanced; see $OUT" >&2
+    exit 1
 fi
 
 echo "$(git rev-parse HEAD)" > "$MARKER"
 
 git add REVIEWS/ >/dev/null 2>&1 && git commit -m "review: Sol pass $(date +%Y-%m-%d)" >/dev/null 2>&1 || true
 
 # Digest for delivery
 echo "SOL-REVIEW $(date +%Y-%m-%d): $NCOMMITS commits reviewed ($(git rev-parse --short "$LAST")..$(git rev-parse --short HEAD))"
 echo "Full review: REVIEWS/$(basename "$OUT") (committed to branch)"
 echo "---"
 grep -nE "Critical|critical|Warning|warning|Bug|bug|flaw|FAIL|incorrect|race|out.of.bounds" "$OUT" | head -15 || true
 echo "---"
 tail -30 "$OUT"

exec
/bin/zsh -lc "rg -n \"npl|n_pl|llama-batched-bench\" examples tools -g '*batched*' -g '*.cpp' -g '*.md' | head -200 && rg -n \"ne21_mm_id_min|MUL_MAT_ID|kernel_mul_mm_id\" ggml/src/ggml-metal | head -100" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 0ms:
tools/batched-bench/README.md:13:./llama-batched-bench -m model.gguf -c 2048 -b 2048 -ub 512 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32 [-pps]
tools/batched-bench/README.md:16:./llama-batched-bench -m ./models/llama-7b/ggml-model-f16.gguf -c 16384 -b 2048 -ub 512 -ngl 99
tools/batched-bench/README.md:19:./llama-batched-bench -m ./models/llama-7b/ggml-model-q8_0.gguf -c 16384 -b 2048 -ub 512 -ngl 99 -pps
tools/batched-bench/README.md:22:./llama-batched-bench -m ./models/llama-7b/ggml-model-q8_0.gguf -c 2048 -b 512 -ub 512 -ngl 999 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32
examples/deprecation-warning/README.md:22:| batched-bench | llama-batched-bench |
tools/batched-bench/batched-bench.cpp:14:    LOG("\n    %s -m model.gguf -c 2048 -b 2048 -ub 512 -npp 128,256,512 -ntg 128,256 -npl 1,2,4,8,16,32 [-pps]\n", argv[0]);
tools/batched-bench/batched-bench.cpp:37:    std::vector<int> n_pl = params.n_pl;
tools/batched-bench/batched-bench.cpp:58:    ctx_params.n_seq_max = n_pl.empty() ? 1 : *std::max_element(n_pl.begin(), n_pl.end());
tools/batched-bench/batched-bench.cpp:134:            for (int i_pl = 0; i_pl < (int) n_pl.size(); ++i_pl) {
tools/batched-bench/batched-bench.cpp:137:                const int pl = n_pl[i_pl];
tools/mtmd/clip.cpp:774:// TODO: there was a more efficient which relies on ggml_view and ggml_rope_ext_inplace, but the rope inplace does not work well with non-contiguous tensors ; we should fix that and revert back to the original implementation in https://github.com/ggml-org/llama.cpp/pull/13065
tools/mtmd/models/llava.cpp:349:            embeddings = ggml_gelu_inplace(ctx0, embeddings);
tools/mtmd/models/conformer.cpp:26:        cur = ggml_relu_inplace(ctx0, cur);
tools/mtmd/models/conformer.cpp:39:        cur = ggml_relu_inplace(ctx0, cur);
tools/mtmd/models/conformer.cpp:52:        cur = ggml_relu_inplace(ctx0, cur);
tools/mtmd/models/cogvlm.cpp:76:    cur = ggml_gelu_inplace(ctx0, cur);
ggml/src/ggml-metal/ggml-metal.cpp:218:        case GGML_OP_MUL_MAT_ID:
ggml/src/ggml-metal/ggml-metal.cpp:750:        case GGML_OP_MUL_MAT_ID:
ggml/src/ggml-metal/ggml-metal.cpp:761:            op->op == GGML_OP_MUL_MAT_ID) &&
ggml/src/ggml-metal/ggml-metal-common.cpp:262:            case GGML_OP_MUL_MAT_ID:
ggml/src/ggml-metal/ggml-metal-device.m:1388:        case GGML_OP_MUL_MAT_ID:
ggml/src/ggml-metal/ggml-metal-ops.cpp:362:        case GGML_OP_MUL_MAT_ID:
ggml/src/ggml-metal/ggml-metal-ops.cpp:2539:    assert(op->op == GGML_OP_MUL_MAT_ID);
ggml/src/ggml-metal/ggml-metal-ops.cpp:2547:    assert(op->op == GGML_OP_MUL_MAT_ID);
ggml/src/ggml-metal/ggml-metal-ops.cpp:2593:    const int ne21_mm_id_min = 32;
ggml/src/ggml-metal/ggml-metal-ops.cpp:2595:    if (props_dev->has_simdgroup_mm && ne00 >= 64 && (ne21 >= ne21_mm_id_min)) {
ggml/src/ggml-metal/ggml-metal-device.cpp:996:    snprintf(base, 256, "kernel_mul_mm_id_map0_ne20_%d", ne20);
ggml/src/ggml-metal/ggml-metal-device.cpp:1018:    snprintf(base, 256, "kernel_mul_mm_id_%s_%s", ggml_type_name(tsrc0), ggml_type_name(tsrc1));
ggml/src/ggml-metal/ggml-metal.metal:10386:kernel void kernel_mul_mm_id_map0(
ggml/src/ggml-metal/ggml-metal.metal:10439:typedef decltype(kernel_mul_mm_id_map0<1>) kernel_mul_mm_id_map0_t;
ggml/src/ggml-metal/ggml-metal.metal:10441:template [[host_name("kernel_mul_mm_id_map0_ne20_1" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<1>;
ggml/src/ggml-metal/ggml-metal.metal:10442:template [[host_name("kernel_mul_mm_id_map0_ne20_2" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<2>;
ggml/src/ggml-metal/ggml-metal.metal:10443:template [[host_name("kernel_mul_mm_id_map0_ne20_4" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<4>;
ggml/src/ggml-metal/ggml-metal.metal:10444:template [[host_name("kernel_mul_mm_id_map0_ne20_5" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<5>;
ggml/src/ggml-metal/ggml-metal.metal:10445:template [[host_name("kernel_mul_mm_id_map0_ne20_6" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<6>;
ggml/src/ggml-metal/ggml-metal.metal:10446:template [[host_name("kernel_mul_mm_id_map0_ne20_8" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<8>;
ggml/src/ggml-metal/ggml-metal.metal:10447:template [[host_name("kernel_mul_mm_id_map0_ne20_10")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<10>;
ggml/src/ggml-metal/ggml-metal.metal:10448:template [[host_name("kernel_mul_mm_id_map0_ne20_16")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<16>;
ggml/src/ggml-metal/ggml-metal.metal:10449:template [[host_name("kernel_mul_mm_id_map0_ne20_22")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<22>;
ggml/src/ggml-metal/ggml-metal.metal:10452:kernel void kernel_mul_mm_id(
ggml/src/ggml-metal/ggml-metal.metal:10819:typedef decltype(kernel_mul_mm_id<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_id;
ggml/src/ggml-metal/ggml-metal.metal:10821:template [[host_name("kernel_mul_mm_id_f32_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10822:template [[host_name("kernel_mul_mm_id_f16_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10824:template [[host_name("kernel_mul_mm_id_bf16_f32")]]    kernel mul_mm_id kernel_mul_mm_id<bfloat, bfloat4x4, simdgroup_bfloat8x8, bfloat, bfloat2x4, simdgroup_bfloat8x8, bfloat4x4,     1,     dequantize_bf16,    bfloat, bfloat4x4, float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10826:template [[host_name("kernel_mul_mm_id_q1_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q1_0,    8,     dequantize_q1_0,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10827:template [[host_name("kernel_mul_mm_id_q2_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_0,    4,     dequantize_q2_0,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10828:template [[host_name("kernel_mul_mm_id_q4_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_0,    2,     dequantize_q4_0,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10829:template [[host_name("kernel_mul_mm_id_q4_1_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_1,    2,     dequantize_q4_1,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10830:template [[host_name("kernel_mul_mm_id_q5_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_0,    2,     dequantize_q5_0,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10831:template [[host_name("kernel_mul_mm_id_q5_1_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_1,    2,     dequantize_q5_1,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10832:template [[host_name("kernel_mul_mm_id_q8_0_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q8_0,    2,     dequantize_q8_0,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10833:template [[host_name("kernel_mul_mm_id_mxfp4_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_mxfp4,   2,     dequantize_mxfp4,   float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10834:template [[host_name("kernel_mul_mm_id_q2_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_K,    QK_NL, dequantize_q2_K,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10835:template [[host_name("kernel_mul_mm_id_q3_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q3_K,    QK_NL, dequantize_q3_K,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10836:template [[host_name("kernel_mul_mm_id_q4_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_K,    QK_NL, dequantize_q4_K,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10837:template [[host_name("kernel_mul_mm_id_q5_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_K,    QK_NL, dequantize_q5_K,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10838:template [[host_name("kernel_mul_mm_id_q6_K_f32")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q6_K,    QK_NL, dequantize_q6_K,    float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10839:template [[host_name("kernel_mul_mm_id_iq2_xxs_f32")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xxs, QK_NL, dequantize_iq2_xxs, float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10840:template [[host_name("kernel_mul_mm_id_iq2_xs_f32")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xs,  QK_NL, dequantize_iq2_xs,  float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10841:template [[host_name("kernel_mul_mm_id_iq3_xxs_f32")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_xxs, QK_NL, dequantize_iq3_xxs, float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10842:template [[host_name("kernel_mul_mm_id_iq3_s_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_s,   QK_NL, dequantize_iq3_s,   float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10843:template [[host_name("kernel_mul_mm_id_iq2_s_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_s,   QK_NL, dequantize_iq2_s,   float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10844:template [[host_name("kernel_mul_mm_id_iq1_s_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_s,   QK_NL, dequantize_iq1_s,   float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10845:template [[host_name("kernel_mul_mm_id_iq1_m_f32")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_m,   QK_NL, dequantize_iq1_m,   float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10846:template [[host_name("kernel_mul_mm_id_iq4_nl_f32")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_nl,  2,     dequantize_iq4_nl,  float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10847:template [[host_name("kernel_mul_mm_id_iq4_xs_f32")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_xs,  QK_NL, dequantize_iq4_xs,  float,  float4x4,  float, float2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10849:template [[host_name("kernel_mul_mm_id_f32_f16")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10850:template [[host_name("kernel_mul_mm_id_f16_f16")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10851:template [[host_name("kernel_mul_mm_id_q1_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q1_0,    8,     dequantize_q1_0,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10852:template [[host_name("kernel_mul_mm_id_q2_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_0,    4,     dequantize_q2_0,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10853:template [[host_name("kernel_mul_mm_id_q4_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_0,    2,     dequantize_q4_0,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10854:template [[host_name("kernel_mul_mm_id_q4_1_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_1,    2,     dequantize_q4_1,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10855:template [[host_name("kernel_mul_mm_id_q5_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_0,    2,     dequantize_q5_0,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10856:template [[host_name("kernel_mul_mm_id_q5_1_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_1,    2,     dequantize_q5_1,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10857:template [[host_name("kernel_mul_mm_id_q8_0_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q8_0,    2,     dequantize_q8_0,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10858:template [[host_name("kernel_mul_mm_id_mxfp4_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_mxfp4,   2,     dequantize_mxfp4,   float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10859:template [[host_name("kernel_mul_mm_id_q2_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_K,    QK_NL, dequantize_q2_K,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10860:template [[host_name("kernel_mul_mm_id_q3_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q3_K,    QK_NL, dequantize_q3_K,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10861:template [[host_name("kernel_mul_mm_id_q4_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_K,    QK_NL, dequantize_q4_K,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10862:template [[host_name("kernel_mul_mm_id_q5_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_K,    QK_NL, dequantize_q5_K,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10863:template [[host_name("kernel_mul_mm_id_q6_K_f16")]]    kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q6_K,    QK_NL, dequantize_q6_K,    float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10864:template [[host_name("kernel_mul_mm_id_iq2_xxs_f16")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xxs, QK_NL, dequantize_iq2_xxs, float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10865:template [[host_name("kernel_mul_mm_id_iq2_xs_f16")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xs,  QK_NL, dequantize_iq2_xs,  float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10866:template [[host_name("kernel_mul_mm_id_iq3_xxs_f16")]] kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_xxs, QK_NL, dequantize_iq3_xxs, float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10867:template [[host_name("kernel_mul_mm_id_iq3_s_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_s,   QK_NL, dequantize_iq3_s,   float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10868:template [[host_name("kernel_mul_mm_id_iq2_s_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_s,   QK_NL, dequantize_iq2_s,   float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10869:template [[host_name("kernel_mul_mm_id_iq1_s_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_s,   QK_NL, dequantize_iq1_s,   float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10870:template [[host_name("kernel_mul_mm_id_iq1_m_f16")]]   kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_m,   QK_NL, dequantize_iq1_m,   float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10871:template [[host_name("kernel_mul_mm_id_iq4_nl_f16")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_nl,  2,     dequantize_iq4_nl,  float,  float4x4,  half, half2x4>;
ggml/src/ggml-metal/ggml-metal.metal:10872:template [[host_name("kernel_mul_mm_id_iq4_xs_f16")]]  kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_xs,  QK_NL, dequantize_iq4_xs,  float,  float4x4,  half, half2x4>;

exec
/bin/zsh -lc "sed -n '1,240p' tools/batched-bench/batched-bench.cpp | nl -ba | sed -n '120,230p'; printf '\\n--- kernel ---\\n'; sed -n '10445,10825p' ggml/src/ggml-metal/ggml-metal.metal | nl -ba | sed -n '1,390p'; printf '\\n--- dispatch ---\\n'; sed -n '2530,2670p' ggml/src/ggml-metal/ggml-metal-ops.cpp | nl -ba" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 0ms:
   120	            return 1;
   121	        }
   122	    }
   123	
   124	    if (!params.batched_bench_output_jsonl) {
   125	        LOG("\n");
   126	        LOG("%s: n_kv_max = %d, n_batch = %d, n_ubatch = %d, flash_attn = %d, is_pp_shared = %d, is_tg_separate = %d, n_gpu_layers = %d, n_threads = %u, n_threads_batch = %u\n", __func__, n_kv_max, params.n_batch, params.n_ubatch, int(params.flash_attn_type), is_pp_shared, is_tg_separate, params.n_gpu_layers, ctx_params.n_threads, ctx_params.n_threads_batch);
   127	        LOG("\n");
   128	        LOG("|%6s | %6s | %4s | %6s | %8s | %8s | %8s | %8s | %8s | %8s |\n", "PP", "TG", "B", "N_KV", "T_PP s", "S_PP t/s", "T_TG s", "S_TG t/s", "T s", "S t/s");
   129	        LOG("|%6s-|-%6s-|-%4s-|-%6s-|-%8s-|-%8s-|-%8s-|-%8s-|-%8s-|-%8s-|\n", "------", "------", "----", "------", "--------", "--------", "--------", "--------", "--------", "--------");
   130	    }
   131	
   132	    for (        int i_pp = 0; i_pp < (int) n_pp.size(); ++i_pp) {
   133	        for (    int i_tg = 0; i_tg < (int) n_tg.size(); ++i_tg) {
   134	            for (int i_pl = 0; i_pl < (int) n_pl.size(); ++i_pl) {
   135	                const int pp = n_pp[i_pp];
   136	                const int tg = n_tg[i_tg];
   137	                const int pl = n_pl[i_pl];
   138	
   139	                const int n_ctx_req = is_pp_shared ? (params.kv_unified ? pp : pl*pp) + pl*tg : pl*(pp + tg);
   140	
   141	                if (n_ctx_req > n_kv_max) {
   142	                    continue;
   143	                }
   144	
   145	                common_batch_clear(batch);
   146	
   147	                for (int j = 0; j < (is_pp_shared ? 1 : pl); ++j) {
   148	                    for (int i = 0; i < pp; ++i) {
   149	                        common_batch_add(batch, get_token_rand(), i, { j }, i == pp - 1);
   150	                    }
   151	                }
   152	
   153	                llama_memory_clear(mem, false);
   154	
   155	                const auto t_pp_start = ggml_time_us();
   156	
   157	                if (!decode_helper(ctx, batch, ctx_params.n_batch, false)) {
   158	                    LOG_ERR("%s: llama_decode() failed\n", __func__);
   159	                    llama_free(ctx);
   160	                    llama_model_free(model);
   161	                    return 1;
   162	                }
   163	
   164	                llama_synchronize(ctx);
   165	
   166	                const auto t_pp_end = ggml_time_us();
   167	
   168	                if (is_pp_shared) {
   169	                    for (int32_t i = 1; i < pl; ++i) {
   170	                        llama_memory_seq_cp(mem, 0, i, -1, -1);
   171	                    }
   172	
   173	                    if (!params.kv_unified) {
   174	                        // run one dummy token to apply the memory copy
   175	                        common_batch_clear(batch);
   176	                        common_batch_add(batch, get_token_rand(), pp + 0, { 0 }, true);
   177	                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
   178	                            LOG_ERR("%s: llama_decode() failed\n", __func__);
   179	                            llama_free(ctx);
   180	                            llama_model_free(model);
   181	                            return 1;
   182	                        }
   183	                        llama_memory_seq_rm(mem, 0, pp, -1);
   184	                    }
   185	                }
   186	
   187	                const auto t_tg_start = ggml_time_us();
   188	
   189	                if (is_tg_separate) {
   190	                    // decode pattern:
   191	                    // 0 0 0 ... 1 1 1 ... 2 2 2 ... 3 3 3 ...
   192	                    for (int j = 0; j < pl; ++j) {
   193	                        for (int i = 0; i < tg; ++i) {
   194	                            common_batch_clear(batch);
   195	
   196	                            common_batch_add(batch, get_token_rand(), pp + i, { j }, true);
   197	
   198	                            if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
   199	                                LOG_ERR("%s: llama_decode() failed\n", __func__);
   200	                                llama_free(ctx);
   201	                                llama_model_free(model);
   202	                                return 1;
   203	                            }
   204	                        }
   205	                    }
   206	                } else {
   207	                    // decode pattern:
   208	                    // 0123 0123 0123 ...
   209	                    for (int i = 0; i < tg; ++i) {
   210	                        common_batch_clear(batch);
   211	
   212	                        for (int j = 0; j < pl; ++j) {
   213	                            common_batch_add(batch, get_token_rand(), pp + i, { j }, true);
   214	                        }
   215	
   216	                        if (!decode_helper(ctx, batch, ctx_params.n_batch, true)) {
   217	                            LOG_ERR("%s: llama_decode() failed\n", __func__);
   218	                            llama_free(ctx);
   219	                            llama_model_free(model);
   220	                            return 1;
   221	                        }
   222	                    }
   223	                }
   224	
   225	                const auto t_tg_end = ggml_time_us();
   226	
   227	                const int32_t n_kv = n_ctx_req;
   228	
   229	                const float t_pp = (t_pp_end - t_pp_start) / 1000000.0f;
   230	                const float t_tg = (t_tg_end - t_tg_start) / 1000000.0f;

--- kernel ---
     1	template [[host_name("kernel_mul_mm_id_map0_ne20_6" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<6>;
     2	template [[host_name("kernel_mul_mm_id_map0_ne20_8" )]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<8>;
     3	template [[host_name("kernel_mul_mm_id_map0_ne20_10")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<10>;
     4	template [[host_name("kernel_mul_mm_id_map0_ne20_16")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<16>;
     5	template [[host_name("kernel_mul_mm_id_map0_ne20_22")]] kernel kernel_mul_mm_id_map0_t kernel_mul_mm_id_map0<22>;
     6	
     7	template<typename S0, typename S0_4x4, typename S0_8x8, typename S1, typename S1_2x4, typename S1_8x8, typename block_q, short nl, void (*dequantize_func)(device const block_q *, short, thread S0_4x4 &), typename T0, typename T0_4x4, typename T1, typename T1_2x4>
     8	kernel void kernel_mul_mm_id(
     9	        constant ggml_metal_kargs_mul_mm_id & args,
    10	        device const char * src0,
    11	        device const char * src1,
    12	        device const char * htpe,
    13	        device const char * hids,
    14	        device       char * dst,
    15	        threadgroup  char * shmem [[threadgroup(0)]],
    16	        uint3  tgpig[[threadgroup_position_in_grid]],
    17	        ushort tiitg[[thread_index_in_threadgroup]],
    18	        ushort tiisg[[thread_index_in_simdgroup]],
    19	        ushort sgitg[[simdgroup_index_in_threadgroup]]) {
    20	    threadgroup S0 * sa = (threadgroup S0 *)(shmem);
    21	    threadgroup S1 * sb = (threadgroup S1 *)(shmem + 4096);
    22	
    23	#ifdef GGML_METAL_HAS_TENSOR
    24	    threadgroup float * sc = (threadgroup float *)(shmem);
    25	#endif
    26	
    27	    constexpr int NR0 = 64;
    28	    constexpr int NR1 = 32;
    29	
    30	    constexpr int NK  = 32;
    31	    constexpr int NL0 = NK/16;
    32	    constexpr int NL1 = NK/8;
    33	
    34	    const int im = tgpig.z; // expert
    35	    const int r0 = tgpig.y*NR0;
    36	    const int r1 = tgpig.x*NR1;
    37	
    38	    device const uint32_t * tpe_u32 = (device const uint32_t *) (htpe);
    39	    device const int32_t  * ids_i32 = (device const int32_t  *) (hids);
    40	
    41	    const int32_t neh1 = tpe_u32[im];
    42	
    43	    if (r1 >= neh1) {
    44	        return;
    45	    }
    46	
    47	    // if this block is of 64x32 shape or smaller
    48	    const short nr0 = (args.ne0 - r0 < NR0) ? (args.ne0 - r0) : NR0;
    49	    const short nr1 = (    neh1 - r1 < NR1) ? (    neh1 - r1) : NR1;
    50	
    51	    // a thread shouldn't load data outside of the matrix
    52	    const short lr0 = ((short)tiitg/NL0) < nr0 ? ((short)tiitg/NL0) : nr0 - 1; // 0 .. 63
    53	    const short lr1 = ((short)tiitg/NL1) < nr1 ? ((short)tiitg/NL1) : nr1 - 1; // 0 .. 31
    54	
    55	    const short il0 = (tiitg % NL0);
    56	
    57	    short il = il0;
    58	
    59	    const int id = ids_i32[im*args.ne21 + r1 + lr1];
    60	
    61	    const short i11 = (id % args.ne20) % args.ne11;
    62	    const short i12 = (id / args.ne20);
    63	    const short i13 = 0;
    64	
    65	    const uint64_t offset0 = im*args.nb02 + i13*args.nb03;
    66	    const short    offset1 = il0/nl;
    67	
    68	    device const block_q * x = (device const block_q *)(src0 + args.nb01*(r0 + lr0) + offset0) + offset1;
    69	
    70	    const short iy = 8*(tiitg % NL1);
    71	
    72	    device const T1 * y = (device const T1 *)(src1
    73	        + args.nb13*i13
    74	        + args.nb12*i12
    75	        + args.nb11*i11
    76	        + args.nb10*iy);
    77	
    78	#ifndef GGML_METAL_HAS_TENSOR
    79	    S0_8x8 ma[4];
    80	    S1_8x8 mb[2];
    81	
    82	    simdgroup_float8x8 mc[8];
    83	
    84	    for (short i = 0; i < 8; i++){
    85	        mc[i] = make_filled_simdgroup_matrix<float, 8>(0.f);
    86	    }
    87	#else
    88	    auto tA = tensor<threadgroup S0, dextents<int32_t, 2>, tensor_inline>(sa, dextents<int32_t, 2>(NK,  NR0));
    89	    auto tB = tensor<threadgroup S1, dextents<int32_t, 2>, tensor_inline>(sb, dextents<int32_t, 2>(NR1, NK ));
    90	
    91	    mpp::tensor_ops::matmul2d<
    92	        mpp::tensor_ops::matmul2d_descriptor(NR1, NR0, NK, false, true, false, mpp::tensor_ops::matmul2d_descriptor::mode::multiply_accumulate),
    93	        execution_simdgroups<4>> mm;
    94	
    95	    auto cT = mm.get_destination_cooperative_tensor<decltype(tA), decltype(tB), float>();
    96	#endif
    97	
    98	    for (int loop_k = 0; loop_k < args.ne00; loop_k += NK) {
    99	#ifndef GGML_METAL_HAS_TENSOR
   100	        // load data and store to threadgroup memory
   101	        if (is_same<T0_4x4, block_q>::value && FC_mul_mm_bc_inp) {
   102	            threadgroup_barrier(mem_flags::mem_threadgroup);
   103	
   104	            // no need for dequantization
   105	            for (short i = 0; i < 16; i++) {
   106	                const short sx = 2*il0 + i/8;
   107	                const short sy = (tiitg/NL0)/8;
   108	
   109	              //const short lx = i%8;
   110	              //const short ly = (tiitg/NL0)%8;
   111	                const short lx = (tiitg/NL0)%8;
   112	                const short ly = i%8;
   113	
   114	                const short ib = 8*sx + sy;
   115	
   116	                *(sa + 64*ib + 8*ly + lx) = loop_k + 16*il + i < args.ne00 ? (S0) *((device T0 *) x + i) : (S0) 0;
   117	            }
   118	        } else {
   119	            S0_4x4 temp_a;
   120	            dequantize_func(x, il, temp_a);
   121	
   122	            threadgroup_barrier(mem_flags::mem_threadgroup);
   123	
   124	            FOR_UNROLL (short i = 0; i < 16; i++) {
   125	                const short sx = 2*il0 + i/8;
   126	                const short sy = (tiitg/NL0)/8;
   127	
   128	              //const short lx = i%8;
   129	              //const short ly = (tiitg/NL0)%8;
   130	                const short lx = (tiitg/NL0)%8;
   131	                const short ly = i%8;
   132	
   133	                const short ib = 8*sx + sy;
   134	
   135	                // NOTE: this is massively slower.. WTF?
   136	                //sa[64*ib + 8*ly + lx] = temp_a[i/4][i%4];
   137	
   138	                *(sa + 64*ib + 8*ly + lx) = temp_a[i/4][i%4];
   139	            }
   140	        }
   141	
   142	        if (FC_mul_mm_bc_inp) {
   143	            for (short i = 0; i < 8; ++i) {
   144	                const short sx = (tiitg%NL1);
   145	                const short sy = (tiitg/NL1)/8;
   146	
   147	                const short lx = i;
   148	                const short ly = (tiitg/NL1)%8;
   149	              //const short lx = (tiitg/NL1)%8;
   150	              //const short ly = i;
   151	
   152	                const short ib = 4*sx + sy;
   153	
   154	                *(sb + 64*ib + 8*ly + lx) = loop_k + iy + i < args.ne00 ? (S1) *((device T1 *) y + i) : 0;
   155	            }
   156	        } else {
   157	            const short sx = (tiitg%NL1);
   158	            const short sy = (tiitg/NL1)/8;
   159	
   160	          //const short dx = sx;
   161	          //const short dy = sy;
   162	
   163	            const short ly = (tiitg/NL1)%8;
   164	
   165	            const short ib = 4*sx + sy;
   166	
   167	            *(threadgroup S1_2x4 *)(sb + 64*ib + 8*ly) = (S1_2x4)(*((device T1_2x4 *) y));
   168	        }
   169	#else
   170	        // load data and store to threadgroup memory
   171	        if (is_same<T0_4x4, block_q>::value && FC_mul_mm_bc_inp) {
   172	            threadgroup_barrier(mem_flags::mem_threadgroup);
   173	
   174	            // no need for dequantization
   175	            for (short i = 0; i < 16; i++) {
   176	                const short sx = 2*il0 + i/8;
   177	                const short sy = (tiitg/NL0)/8;
   178	
   179	                const short lx = i%8;
   180	                const short ly = (tiitg/NL0)%8;
   181	                //const short lx = (tiitg/NL0)%8;
   182	                //const short ly = i%8;
   183	
   184	                *(sa + NK*(8*sy + ly) + 8*sx + lx) = loop_k + 16*il + i < args.ne00 ? *((device T0 *) x + i) : 0;
   185	            }
   186	        } else {
   187	            S0_4x4 temp_a;
   188	            dequantize_func(x, il, temp_a);
   189	
   190	            threadgroup_barrier(mem_flags::mem_threadgroup);
   191	
   192	            FOR_UNROLL (short i = 0; i < 16; i++) {
   193	                const short sx = 2*il0 + i/8;
   194	                const short sy = (tiitg/NL0)/8;
   195	
   196	                const short lx = i%8;
   197	                const short ly = (tiitg/NL0)%8;
   198	                //const short lx = (tiitg/NL0)%8;
   199	                //const short ly = i%8;
   200	
   201	                *(sa + NK*(8*sy + ly) + 8*sx + lx) = temp_a[i/4][i%4];
   202	            }
   203	        }
   204	
   205	        if (FC_mul_mm_bc_inp) {
   206	            for (short i = 0; i < 8; ++i) {
   207	                const short sx = (tiitg%NL1);
   208	                const short sy = (tiitg/NL1)/8;
   209	
   210	                const short lx = i;
   211	                const short ly = (tiitg/NL1)%8;
   212	                //const short lx = (tiitg/NL1)%8;
   213	                //const short ly = i;
   214	
   215	                *(sb + NK*(8*sy + ly) + 8*sx + lx) = loop_k + iy + i < args.ne00 ? (S1) *((device T1 *) y + i) : 0;
   216	            }
   217	        } else {
   218	            const short sx = (tiitg%NL1);
   219	            const short sy = (tiitg/NL1)/8;
   220	
   221	            //const short lx = i;
   222	            const short ly = (tiitg/NL1)%8;
   223	            //const short lx = (tiitg/NL1)%8;
   224	            //const short ly = i;
   225	
   226	            *(threadgroup S1_2x4 *)(sb + NK*(8*sy + ly) + 8*sx) = (S1_2x4)(*((device T1_2x4 *) y));
   227	        }
   228	#endif
   229	
   230	        il = (il + 2 < nl) ? il + 2 : il % 2;
   231	        x  = (il < 2) ? x + (2 + nl - 1)/nl : x;
   232	
   233	        y += NK;
   234	
   235	        threadgroup_barrier(mem_flags::mem_threadgroup);
   236	
   237	#ifndef GGML_METAL_HAS_TENSOR
   238	        // load matrices from threadgroup memory and conduct outer products
   239	        threadgroup const S0 * lsma = (sa + 4*64*(sgitg%2));
   240	        threadgroup const S1 * lsmb = (sb + 2*64*(sgitg/2));
   241	
   242	        FOR_UNROLL (short ik = 0; ik < NK/8; ik++) {
   243	            simdgroup_barrier(mem_flags::mem_none);
   244	
   245	            FOR_UNROLL (short i = 0; i < 4; i++) {
   246	                simdgroup_load(ma[i], lsma + 64*i, 8, 0, false);
   247	            }
   248	
   249	            simdgroup_barrier(mem_flags::mem_none);
   250	
   251	            FOR_UNROLL (short i = 0; i < 2; i++) {
   252	                simdgroup_load(mb[i], lsmb + 64*i, 8, 0, false);
   253	            }
   254	
   255	            simdgroup_barrier(mem_flags::mem_none);
   256	
   257	            FOR_UNROLL (short i = 0; i < 8; i++){
   258	                simdgroup_multiply_accumulate(mc[i], mb[i/4], ma[i%4], mc[i]);
   259	            }
   260	
   261	            lsma += 8*64;
   262	            lsmb += 4*64;
   263	        }
   264	#else
   265	        auto sA = tA.slice(0, 0);
   266	        auto sB = tB.slice(0, 0);
   267	
   268	        mm.run(sB, sA, cT);
   269	#endif
   270	    }
   271	
   272	    // block is smaller than 64x32, we should avoid writing data outside of the matrix
   273	    threadgroup_barrier(mem_flags::mem_threadgroup);
   274	
   275	#ifdef GGML_METAL_HAS_TENSOR
   276	    auto tC = tensor<threadgroup float, dextents<int32_t, 2>, tensor_inline>(sc, dextents<int32_t, 2>(NR0, NR1));
   277	    cT.store(tC);
   278	#else
   279	    threadgroup float * temp_str = ((threadgroup float *) shmem) + 32*(sgitg&1) + (16*(sgitg >> 1))*NR0;
   280	
   281	    for (short i = 0; i < 8; i++) {
   282	        simdgroup_store(mc[i], temp_str + 8*(i%4) + 8*NR0*(i/4), NR0, 0, false);
   283	    }
   284	#endif
   285	
   286	    threadgroup_barrier(mem_flags::mem_threadgroup);
   287	
   288	    for (short j = sgitg; j < nr1; j += 4) {
   289	        const int id = ids_i32[im*args.ne21 + r1 + j];
   290	
   291	        const short ide = id % args.ne20;
   292	        const short idt = id / args.ne20;
   293	
   294	        device float  * D  = (device float  *) dst + r0 + ide*args.ne0 + idt*args.ne1*args.ne0;
   295	        device float4 * D4 = (device float4 *) D;
   296	
   297	        threadgroup float  * C  = (threadgroup float  *) shmem + j*NR0;
   298	        threadgroup float4 * C4 = (threadgroup float4 *) C;
   299	
   300	        int i = tiisg;
   301	        for (; i < nr0/4; i += 32) {
   302	            *(D4 + i) = *(C4 + i);
   303	        }
   304	
   305	        i = (4*(nr0/4)) + tiisg;
   306	        for (; i < nr0; i += 32) {
   307	            *(D + i) = *(C + i);
   308	        }
   309	    }
   310	}
   311	
   312	//
   313	// matrix-matrix multiplication
   314	//
   315	
   316	typedef decltype(kernel_mul_mm<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_t;
   317	
   318	template [[host_name("kernel_mul_mm_f32_f32")]]     kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  float, float2x4>;
   319	template [[host_name("kernel_mul_mm_f16_f32")]]     kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   float, float2x4>;
   320	#if defined(GGML_METAL_HAS_BF16)
   321	template [[host_name("kernel_mul_mm_bf16_f32")]]    kernel mul_mm_t kernel_mul_mm<bfloat, bfloat4x4, simdgroup_bfloat8x8, bfloat, bfloat2x4, simdgroup_bfloat8x8, bfloat4x4,     1,     dequantize_bf16,    bfloat, bfloat4x4, float, float2x4>;
   322	#endif
   323	template [[host_name("kernel_mul_mm_q1_0_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q1_0,    8,     dequantize_q1_0,    float,  float4x4,  float, float2x4>;
   324	template [[host_name("kernel_mul_mm_q2_0_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_0,    4,     dequantize_q2_0,    float,  float4x4,  float, float2x4>;
   325	template [[host_name("kernel_mul_mm_q4_0_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_0,    2,     dequantize_q4_0,    float,  float4x4,  float, float2x4>;
   326	template [[host_name("kernel_mul_mm_q4_1_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_1,    2,     dequantize_q4_1,    float,  float4x4,  float, float2x4>;
   327	template [[host_name("kernel_mul_mm_q5_0_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_0,    2,     dequantize_q5_0,    float,  float4x4,  float, float2x4>;
   328	template [[host_name("kernel_mul_mm_q5_1_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_1,    2,     dequantize_q5_1,    float,  float4x4,  float, float2x4>;
   329	template [[host_name("kernel_mul_mm_q8_0_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q8_0,    2,     dequantize_q8_0,    float,  float4x4,  float, float2x4>;
   330	template [[host_name("kernel_mul_mm_mxfp4_f32")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_mxfp4,   2,     dequantize_mxfp4,   float,  float4x4,  float, float2x4>;
   331	template [[host_name("kernel_mul_mm_q2_K_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_K,    QK_NL, dequantize_q2_K,    float,  float4x4,  float, float2x4>;
   332	template [[host_name("kernel_mul_mm_q3_K_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q3_K,    QK_NL, dequantize_q3_K,    float,  float4x4,  float, float2x4>;
   333	template [[host_name("kernel_mul_mm_q4_K_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_K,    QK_NL, dequantize_q4_K,    float,  float4x4,  float, float2x4>;
   334	template [[host_name("kernel_mul_mm_q5_K_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_K,    QK_NL, dequantize_q5_K,    float,  float4x4,  float, float2x4>;
   335	template [[host_name("kernel_mul_mm_q6_K_f32")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q6_K,    QK_NL, dequantize_q6_K,    float,  float4x4,  float, float2x4>;
   336	template [[host_name("kernel_mul_mm_iq2_xxs_f32")]] kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xxs, QK_NL, dequantize_iq2_xxs, float,  float4x4,  float, float2x4>;
   337	template [[host_name("kernel_mul_mm_iq2_xs_f32")]]  kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xs,  QK_NL, dequantize_iq2_xs,  float,  float4x4,  float, float2x4>;
   338	template [[host_name("kernel_mul_mm_iq3_xxs_f32")]] kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_xxs, QK_NL, dequantize_iq3_xxs, float,  float4x4,  float, float2x4>;
   339	template [[host_name("kernel_mul_mm_iq3_s_f32")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_s,   QK_NL, dequantize_iq3_s,   float,  float4x4,  float, float2x4>;
   340	template [[host_name("kernel_mul_mm_iq2_s_f32")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_s,   QK_NL, dequantize_iq2_s,   float,  float4x4,  float, float2x4>;
   341	template [[host_name("kernel_mul_mm_iq1_s_f32")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_s,   QK_NL, dequantize_iq1_s,   float,  float4x4,  float, float2x4>;
   342	template [[host_name("kernel_mul_mm_iq1_m_f32")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_m,   QK_NL, dequantize_iq1_m,   float,  float4x4,  float, float2x4>;
   343	template [[host_name("kernel_mul_mm_iq4_nl_f32")]]  kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_nl,  2,     dequantize_iq4_nl,  float,  float4x4,  float, float2x4>;
   344	template [[host_name("kernel_mul_mm_iq4_xs_f32")]]  kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_xs,  QK_NL, dequantize_iq4_xs,  float,  float4x4,  float, float2x4>;
   345	
   346	template [[host_name("kernel_mul_mm_f32_f16")]]     kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  half, half2x4>;
   347	template [[host_name("kernel_mul_mm_f16_f16")]]     kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   half, half2x4>;
   348	template [[host_name("kernel_mul_mm_q1_0_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q1_0,    8,     dequantize_q1_0,    float,  float4x4,  half, half2x4>;
   349	template [[host_name("kernel_mul_mm_q2_0_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_0,    4,     dequantize_q2_0,    float,  float4x4,  half, half2x4>;
   350	template [[host_name("kernel_mul_mm_q4_0_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_0,    2,     dequantize_q4_0,    float,  float4x4,  half, half2x4>;
   351	template [[host_name("kernel_mul_mm_q4_1_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_1,    2,     dequantize_q4_1,    float,  float4x4,  half, half2x4>;
   352	template [[host_name("kernel_mul_mm_q5_0_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_0,    2,     dequantize_q5_0,    float,  float4x4,  half, half2x4>;
   353	template [[host_name("kernel_mul_mm_q5_1_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_1,    2,     dequantize_q5_1,    float,  float4x4,  half, half2x4>;
   354	template [[host_name("kernel_mul_mm_q8_0_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q8_0,    2,     dequantize_q8_0,    float,  float4x4,  half, half2x4>;
   355	template [[host_name("kernel_mul_mm_mxfp4_f16")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_mxfp4,   2,     dequantize_mxfp4,   float,  float4x4,  half, half2x4>;
   356	template [[host_name("kernel_mul_mm_q2_K_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q2_K,    QK_NL, dequantize_q2_K,    float,  float4x4,  half, half2x4>;
   357	template [[host_name("kernel_mul_mm_q3_K_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q3_K,    QK_NL, dequantize_q3_K,    float,  float4x4,  half, half2x4>;
   358	template [[host_name("kernel_mul_mm_q4_K_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q4_K,    QK_NL, dequantize_q4_K,    float,  float4x4,  half, half2x4>;
   359	template [[host_name("kernel_mul_mm_q5_K_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q5_K,    QK_NL, dequantize_q5_K,    float,  float4x4,  half, half2x4>;
   360	template [[host_name("kernel_mul_mm_q6_K_f16")]]    kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_q6_K,    QK_NL, dequantize_q6_K,    float,  float4x4,  half, half2x4>;
   361	template [[host_name("kernel_mul_mm_iq2_xxs_f16")]] kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xxs, QK_NL, dequantize_iq2_xxs, float,  float4x4,  half, half2x4>;
   362	template [[host_name("kernel_mul_mm_iq2_xs_f16")]]  kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_xs,  QK_NL, dequantize_iq2_xs,  float,  float4x4,  half, half2x4>;
   363	template [[host_name("kernel_mul_mm_iq3_xxs_f16")]] kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_xxs, QK_NL, dequantize_iq3_xxs, float,  float4x4,  half, half2x4>;
   364	template [[host_name("kernel_mul_mm_iq3_s_f16")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq3_s,   QK_NL, dequantize_iq3_s,   float,  float4x4,  half, half2x4>;
   365	template [[host_name("kernel_mul_mm_iq2_s_f16")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq2_s,   QK_NL, dequantize_iq2_s,   float,  float4x4,  half, half2x4>;
   366	template [[host_name("kernel_mul_mm_iq1_s_f16")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_s,   QK_NL, dequantize_iq1_s,   float,  float4x4,  half, half2x4>;
   367	template [[host_name("kernel_mul_mm_iq1_m_f16")]]   kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq1_m,   QK_NL, dequantize_iq1_m,   float,  float4x4,  half, half2x4>;
   368	template [[host_name("kernel_mul_mm_iq4_nl_f16")]]  kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_nl,  2,     dequantize_iq4_nl,  float,  float4x4,  half, half2x4>;
   369	template [[host_name("kernel_mul_mm_iq4_xs_f16")]]  kernel mul_mm_t kernel_mul_mm<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   block_iq4_xs,  QK_NL, dequantize_iq4_xs,  float,  float4x4,  half, half2x4>;
   370	
   371	//
   372	// indirect matrix-matrix multiplication
   373	//
   374	
   375	typedef decltype(kernel_mul_mm_id<half, half4x4, simdgroup_half8x8, half, half2x4, simdgroup_half8x8, float4x4, 1, dequantize_f32, float, float4x4, float, float2x4>) mul_mm_id;
   376	
   377	template [[host_name("kernel_mul_mm_id_f32_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   float4x4,      1,     dequantize_f32,     float,  float4x4,  float, float2x4>;
   378	template [[host_name("kernel_mul_mm_id_f16_f32")]]     kernel mul_mm_id kernel_mul_mm_id<half,   half4x4,   simdgroup_half8x8,   half,   half2x4,   simdgroup_half8x8,   half4x4,       1,     dequantize_f16,     half,   half4x4,   float, float2x4>;
   379	#if defined(GGML_METAL_HAS_BF16)
   380	template [[host_name("kernel_mul_mm_id_bf16_f32")]]    kernel mul_mm_id kernel_mul_mm_id<bfloat, bfloat4x4, simdgroup_bfloat8x8, bfloat, bfloat2x4, simdgroup_bfloat8x8, bfloat4x4,     1,     dequantize_bf16,    bfloat, bfloat4x4, float, float2x4>;
   381	#endif

--- dispatch ---
     1	        } else {
     2	            ggml_metal_encoder_dispatch_threadgroups(enc, ((ne01 + nr0*nsg - 1)/(nr0*nsg)), ((ne11 + nr1 - 1)/nr1), ne12*ne13, 32, nsg, 1);
     3	        }
     4	    }
     5	
     6	    return 1;
     7	}
     8	
     9	size_t ggml_metal_op_mul_mat_id_extra_tpe(const ggml_tensor * op) {
    10	    assert(op->op == GGML_OP_MUL_MAT_ID);
    11	
    12	    const int64_t ne02 = op->src[0]->ne[2]; // n_expert
    13	
    14	    return ggml_type_size(GGML_TYPE_I32)*ne02;
    15	}
    16	
    17	size_t ggml_metal_op_mul_mat_id_extra_ids(const ggml_tensor * op) {
    18	    assert(op->op == GGML_OP_MUL_MAT_ID);
    19	
    20	    const int64_t ne02 = op->src[0]->ne[2]; // n_expert
    21	    const int64_t ne21 = op->src[2]->ne[1]; // n_token
    22	
    23	    return ggml_type_size(GGML_TYPE_I32)*ne02*ne21;
    24	}
    25	
    26	int ggml_metal_op_mul_mat_id(ggml_metal_op_t ctx, int idx) {
    27	    ggml_tensor * op = ctx->node(idx);
    28	
    29	    ggml_metal_library_t lib = ctx->lib;
    30	    ggml_metal_encoder_t enc = ctx->enc;
    31	
    32	    const ggml_metal_device_props * props_dev = ggml_metal_device_get_props(ctx->dev);
    33	
    34	    GGML_TENSOR_LOCALS( int32_t, ne0, op->src[0], ne);
    35	    GGML_TENSOR_LOCALS(uint64_t, nb0, op->src[0], nb);
    36	    GGML_TENSOR_LOCALS( int32_t, ne1, op->src[1], ne);
    37	    GGML_TENSOR_LOCALS(uint64_t, nb1, op->src[1], nb);
    38	    GGML_TENSOR_LOCALS( int32_t, ne2, op->src[2], ne);
    39	    GGML_TENSOR_LOCALS(uint64_t, nb2, op->src[2], nb);
    40	    GGML_TENSOR_LOCALS( int32_t, ne,  op,         ne);
    41	    GGML_TENSOR_LOCALS(uint64_t, nb,  op,         nb);
    42	
    43	    // src2 = ids
    44	    GGML_ASSERT(op->src[2]->type == GGML_TYPE_I32);
    45	
    46	    GGML_ASSERT(!ggml_is_transposed(op->src[0]));
    47	    GGML_ASSERT(!ggml_is_transposed(op->src[1]));
    48	
    49	    GGML_ASSERT(ne03 == 1);
    50	    GGML_ASSERT(ne13 == 1);
    51	
    52	    ggml_metal_buffer_id bid_src0 = ggml_metal_get_buffer_id(op->src[0]);
    53	    ggml_metal_buffer_id bid_src1 = ggml_metal_get_buffer_id(op->src[1]);
    54	    ggml_metal_buffer_id bid_src2 = ggml_metal_get_buffer_id(op->src[2]);
    55	    ggml_metal_buffer_id bid_dst  = ggml_metal_get_buffer_id(op);
    56	
    57	    const uint32_t r2 = 1;
    58	    const uint32_t r3 = 1;
    59	
    60	    // find the break-even point where the matrix-matrix kernel becomes more efficient compared
    61	    // to the matrix-vector kernel
    62	    // ne20 = n_used_experts
    63	    // ne21 = n_rows (batch size)
    64	    const int ne21_mm_id_min = 32;
    65	
    66	    if (props_dev->has_simdgroup_mm && ne00 >= 64 && (ne21 >= ne21_mm_id_min)) {
    67	        // some Metal matrix data types require aligned pointers
    68	        // ref: https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf (Table 2.5)
    69	        //switch (op->src[0]->type) {
    70	        //    case GGML_TYPE_F32:  GGML_ASSERT(nb01 % 16 == 0); break;
    71	        //    case GGML_TYPE_F16:  GGML_ASSERT(nb01 % 8  == 0); break;
    72	        //    case GGML_TYPE_BF16: GGML_ASSERT(nb01 % 8  == 0); break;
    73	        //    default: break;
    74	        //}
    75	
    76	        // extra buffers for intermediate id mapping
    77	        ggml_metal_buffer_id bid_tpe = bid_dst;
    78	        bid_tpe.offs += ggml_nbytes(op);
    79	
    80	        ggml_metal_buffer_id bid_ids = bid_tpe;
    81	        bid_ids.offs += ggml_metal_op_mul_mat_id_extra_tpe(op);
    82	
    83	        {
    84	            ggml_metal_kargs_mul_mm_id_map0 args = {
    85	                ne02,
    86	                ne10,
    87	                ne11, // n_expert_used (bcast)
    88	                nb11,
    89	                nb12,
    90	                ne21, // n_tokens
    91	                ne20, // n_expert_used
    92	                nb21,
    93	            };
    94	
    95	            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id_map0(lib, ne02, ne20);
    96	
    97	            const size_t smem = pipeline.smem;
    98	
    99	            GGML_ASSERT(ne02 <= ggml_metal_pipeline_max_theads_per_threadgroup(pipeline));
   100	
   101	            GGML_ASSERT(smem <= props_dev->max_theadgroup_memory_size);
   102	
   103	            ggml_metal_encoder_set_pipeline(enc, pipeline);
   104	            ggml_metal_encoder_set_bytes   (enc, &args, sizeof(args), 0);
   105	            ggml_metal_encoder_set_buffer  (enc, bid_src2, 1);
   106	            ggml_metal_encoder_set_buffer  (enc, bid_tpe,  2);
   107	            ggml_metal_encoder_set_buffer  (enc, bid_ids,  3);
   108	
   109	            ggml_metal_encoder_set_threadgroup_memory_size(enc, smem, 0);
   110	
   111	            ggml_metal_encoder_dispatch_threadgroups(enc, 1, 1, 1, ne02, 1, 1);
   112	        }
   113	
   114	        // this barrier is always needed because the next kernel has to wait for the id maps to be computed
   115	        ggml_metal_op_concurrency_reset(ctx);
   116	
   117	        {
   118	            auto pipeline = ggml_metal_library_get_pipeline_mul_mm_id(lib, op);
   119	
   120	            ggml_metal_kargs_mul_mm_id args = {
   121	                /*.ne00  =*/ ne00,
   122	                /*.ne02  =*/ ne02,
   123	                /*.nb01  =*/ nb01,
   124	                /*.nb02  =*/ nb02,
   125	                /*.nb03  =*/ nb03,
   126	                /*.ne11  =*/ ne11, // n_expert_used (bcast)
   127	                /*.nb10  =*/ nb10,
   128	                /*.nb11  =*/ nb11,
   129	                /*.nb12  =*/ nb12,
   130	                /*.nb13  =*/ nb13,
   131	                /*.ne20  =*/ ne20, // n_expert_used
   132	                /*.ne21  =*/ ne21, // n_tokens
   133	                /*.ne0   =*/ ne0,
   134	                /*.ne1   =*/ ne1,
   135	                /*.r2    =*/ r2,
   136	                /*.r3    =*/ r3,
   137	            };
   138	
   139	            ggml_metal_encoder_set_pipeline(enc, pipeline);
   140	            ggml_metal_encoder_set_bytes   (enc, &args, sizeof(args), 0);
   141	            ggml_metal_encoder_set_buffer  (enc, bid_src0, 1);

exec
/bin/zsh -lc "sed -n '2660,2745p' ggml/src/ggml-metal/ggml-metal-ops.cpp | nl -ba && grep -n \"maxTotalThreadsPerThreadgroup\\|nsg\" -n ggml/src/ggml-metal/ggml-metal-ops.cpp | grep -A2 -B2 mul_mm_id | head" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 0ms:
     1	                /*.ne20  =*/ ne20, // n_expert_used
     2	                /*.ne21  =*/ ne21, // n_tokens
     3	                /*.ne0   =*/ ne0,
     4	                /*.ne1   =*/ ne1,
     5	                /*.r2    =*/ r2,
     6	                /*.r3    =*/ r3,
     7	            };
     8	
     9	            ggml_metal_encoder_set_pipeline(enc, pipeline);
    10	            ggml_metal_encoder_set_bytes   (enc, &args, sizeof(args), 0);
    11	            ggml_metal_encoder_set_buffer  (enc, bid_src0, 1);
    12	            ggml_metal_encoder_set_buffer  (enc, bid_src1, 2);
    13	            ggml_metal_encoder_set_buffer  (enc, bid_tpe,  3);
    14	            ggml_metal_encoder_set_buffer  (enc, bid_ids,  4);
    15	            ggml_metal_encoder_set_buffer  (enc, bid_dst,  5);
    16	
    17	            const size_t smem = pipeline.smem;
    18	
    19	            ggml_metal_encoder_set_threadgroup_memory_size(enc, smem, 0);
    20	
    21	            ggml_metal_encoder_dispatch_threadgroups(enc, (ne21 + 31)/32, (ne01 + 63)/64, ne02, 128, 1, 1);
    22	        }
    23	    } else {
    24	        auto pipeline = ggml_metal_library_get_pipeline_mul_mv_id(lib, op);
    25	
    26	        const int nr0 = pipeline.nr0;
    27	        const int nr1 = pipeline.nr1;
    28	        const int nsg = pipeline.nsg;
    29	
    30	        const size_t smem = pipeline.smem;
    31	
    32	        ggml_metal_kargs_mul_mv_id args = {
    33	            /*.nei0 =*/ ne20,
    34	            /*.nei1 =*/ ne21,
    35	            /*.nbi1 =*/ nb21,
    36	            /*.ne00 =*/ ne00,
    37	            /*.ne01 =*/ ne01,
    38	            /*.ne02 =*/ ne02,
    39	            /*.nb00 =*/ nb00,
    40	            /*.nb01 =*/ nb01,
    41	            /*.nb02 =*/ nb02,
    42	            /*.ne10 =*/ ne10,
    43	            /*.ne11 =*/ ne11,
    44	            /*.ne12 =*/ ne12,
    45	            /*.ne13 =*/ ne13,
    46	            /*.nb10 =*/ nb10,
    47	            /*.nb11 =*/ nb11,
    48	            /*.nb12 =*/ nb12,
    49	            /*.ne0  =*/ ne0,
    50	            /*.ne1  =*/ ne1,
    51	            /*.nb1  =*/ nb1,
    52	            /*.nr0  =*/ nr0,
    53	        };
    54	
    55	        if (ggml_is_quantized(op->src[0]->type)) {
    56	            GGML_ASSERT(ne00 >= nsg*nr0);
    57	        }
    58	
    59	        ggml_metal_encoder_set_pipeline(enc, pipeline);
    60	        ggml_metal_encoder_set_bytes(enc, &args, sizeof(args), 0);
    61	        ggml_metal_encoder_set_buffer(enc, bid_src0, 1);
    62	        ggml_metal_encoder_set_buffer(enc, bid_src1, 2);
    63	        ggml_metal_encoder_set_buffer(enc, bid_dst,  3);
    64	        ggml_metal_encoder_set_buffer(enc, bid_src2, 4);
    65	
    66	        const int64_t _ne1 = 1;
    67	        const int64_t ne123 = ne20*ne21;
    68	
    69	        ggml_metal_encoder_set_threadgroup_memory_size(enc, smem, 0);
    70	
    71	        if (op->src[0]->type == GGML_TYPE_F32 ||
    72	            op->src[0]->type == GGML_TYPE_F16 ||
    73	            op->src[0]->type == GGML_TYPE_BF16 ||
    74	            op->src[0]->type == GGML_TYPE_Q8_0) {
    75	            ggml_metal_encoder_dispatch_threadgroups(enc, (ne01 + nr0 - 1)/(nr0), (_ne1 + nr1 - 1)/nr1, ne123, 32, nsg, 1);
    76	        } else {
    77	            ggml_metal_encoder_dispatch_threadgroups(enc, (ne01 + nr0*nsg - 1)/(nr0*nsg), (_ne1 + nr1 - 1)/nr1, ne123, 32, nsg, 1);
    78	        }
    79	    }
    80	
    81	    return 1;
    82	}
    83	
    84	int ggml_metal_op_add_id(ggml_metal_op_t ctx, int idx) {
    85	    ggml_tensor * op = ctx->node(idx);
    86	

exec
/bin/zsh -lc "nl -ba AGENTS.md | sed -n '35,100p'; printf '\\n--- git diff zero context ---\\n'; git diff --unified=0 a40fc08983f436db1ad28039db72ca165ae8e5d2 -- scripts/sol-review.sh KERNEL-PROJECT.md REVIEWS/.last-reviewed | sed -n '1,240p'; printf '\\n--- current head/status ---\\n'; git rev-parse HEAD; cat REVIEWS/.last-reviewed; git status --short" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 4186ms:
2026-08-14 19:54:37.185 xcodebuild[99026:7419623]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:54:37.303 xcodebuild[99026:7419622] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-2jPMtKRN' (errno=Operation not permitted)
2026-08-14 19:54:37.667 xcodebuild[99028:7419634]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:54:37.777 xcodebuild[99028:7419633] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
diff --git a/KERNEL-PROJECT.md b/KERNEL-PROJECT.md
index 0799fa5ca..eba902c57 100644
--- a/KERNEL-PROJECT.md
+++ b/KERNEL-PROJECT.md
@@ -93,2 +93,4 @@ across 8 tokens instead of 1 (PR #25377 evidence on plain mul_mat:
-   block; wire pipeline lookup in ggml-metal.m (mirror
-   ggml_metal_library_get_pipeline_mul_mm_id).
+   block; wire pipeline lookup in ggml-metal-device.cpp (mirror
+   ggml_metal_library_get_pipeline_mul_mm_id at ggml-metal-device.cpp:1009
+   - corrected 2026-08-14 per Sol review; the .metal file is the shader
+   source, the registry lives in ggml-metal-device.cpp).
diff --git a/REVIEWS/.last-reviewed b/REVIEWS/.last-reviewed
new file mode 100644
index 000000000..e54e5218f
--- /dev/null
+++ b/REVIEWS/.last-reviewed
@@ -0,0 +1 @@
+a40fc08983f436db1ad28039db72ca165ae8e5d2
diff --git a/scripts/sol-review.sh b/scripts/sol-review.sh
index 6b36444db..7b19c55cf 100755
--- a/scripts/sol-review.sh
+++ b/scripts/sol-review.sh
@@ -9,4 +9,8 @@
-# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
-# short (empty-ish output on no-new-commits would be ideal but a nightly
-# review of even 1 commit is the point - this is a review loop, not a
-# change-detector).
+# Cron: no_agent watchdog - stdout delivered verbatim; silent when no new
+# commits; non-zero exit on review failure (cron alerts).
+#
+# v2 fixes (from Sol's own review of v1, REVIEWS/2026-08-14.md):
+# - review --base <marker> so each run covers ONLY new commits
+# - marker advances only on codex success; failures exit nonzero
+# - git log -25 (no SIGPIPE via head)
+# - missing base ref = error, not silent success
@@ -23,2 +27,2 @@ mkdir -p "$REVIEWS_DIR"
-# Locate the base ref (local branch or remote)
-if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
+# Resolve the base ref; a missing base is a config error, not "no work".
+if git rev-parse --verify --quiet "$BASE" >/dev/null; then
@@ -26 +30 @@ if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
-else
+elif git rev-parse --verify --quiet "origin/$BASE" >/dev/null; then
@@ -27,0 +32,3 @@ else
+else
+    echo "sol-review: base '$BASE' not found locally or on origin" >&2
+    exit 2
@@ -29,0 +37 @@ fi
+# The ref to diff against: the last-reviewed marker, else the base branch.
@@ -47 +55 @@ OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
-    git log --oneline "$LAST..HEAD" | head -25
+    git log --oneline -25 "$LAST..HEAD"
@@ -58 +66,2 @@ REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-mo
-if ! codex review --base "$BASE_REF" \
+REVIEW_OK=0
+if codex review --base "$LAST" \
@@ -61,0 +71,2 @@ if ! codex review --base "$BASE_REF" \
+    REVIEW_OK=1
+else
@@ -63,4 +74,11 @@ if ! codex review --base "$BASE_REF" \
-    codex review --base "$BASE_REF" \
-        -c model="gpt-5.6-sol" \
-        -c model_reasoning_effort="high" \
-        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
+    if codex review --base "$LAST" \
+            -c model="gpt-5.6-sol" \
+            -c model_reasoning_effort="high" \
+            >> "$OUT" 2>&1; then
+        REVIEW_OK=1
+    fi
+fi
+
+if [ "$REVIEW_OK" != "1" ]; then
+    echo "sol-review FAILED (codex review error) - marker NOT advanced; see $OUT" >&2
+    exit 1

--- current head/status ---
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-QNTkdQrd' (errno=Operation not permitted)
2026-08-14 19:54:38.593 xcodebuild[99033:7419666]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:54:38.705 xcodebuild[99033:7419665] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-trx9NOg4' (errno=Operation not permitted)
2026-08-14 19:54:39.066 xcodebuild[99035:7419677]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:54:39.178 xcodebuild[99035:7419676] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
e213b154c30966836c2d576bf72423819442e38b
a40fc08983f436db1ad28039db72ca165ae8e5d2
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Sim3DlZ8' (errno=Operation not permitted)
2026-08-14 19:54:40.005 xcodebuild[99040:7419701]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:54:40.123 xcodebuild[99040:7419700] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-wD6plbD6' (errno=Operation not permitted)
2026-08-14 19:54:40.487 xcodebuild[99042:7419711]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:54:40.627 xcodebuild[99042:7419710] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
 M REVIEWS/2026-08-14.md

2026-08-14T23:54:41.338968Z ERROR codex_models_manager::manager: failed to renew cache TTL: missing field `base_instructions` at line 95 column 5
exec
/bin/zsh -lc "nl -ba ggml/src/ggml-metal/ggml-metal-device.cpp | sed -n '985,1030p'; nl -ba KERNEL-PROJECT.md | sed -n '88,104p'; git diff --check a40fc08983f436db1ad28039db72ca165ae8e5d2" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 exited 2 in 1391ms:
2026-08-14 19:55:42.840 xcodebuild[99075:7420911]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:55:42.969 xcodebuild[99075:7420910] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-wBwRdfA3' (errno=Operation not permitted)
2026-08-14 19:55:43.349 xcodebuild[99077:7420923]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:55:43.474 xcodebuild[99077:7420922] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
REVIEWS/2026-08-14.md:99: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:101: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:115: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:117: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:124: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:126: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:133: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:135: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:150: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:152: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:159: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:188: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:314: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:317: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:322: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:335: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:338: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:345: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:361: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:363: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:391: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:393: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:395: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:426: trailing whitespace.
+    14	
REVIEWS/2026-08-14.md:429: trailing whitespace.
+    17	
REVIEWS/2026-08-14.md:434: trailing whitespace.
+    22	
REVIEWS/2026-08-14.md:441: trailing whitespace.
+    29	
REVIEWS/2026-08-14.md:443: trailing whitespace.
+    31	
REVIEWS/2026-08-14.md:450: trailing whitespace.
+    38	
REVIEWS/2026-08-14.md:465: trailing whitespace.
+    53	
REVIEWS/2026-08-14.md:467: trailing whitespace.
+    55	
REVIEWS/2026-08-14.md:480: trailing whitespace.
+    68	
REVIEWS/2026-08-14.md:482: trailing whitespace.
+    70	
REVIEWS/2026-08-14.md:484: trailing whitespace.
+    72	
REVIEWS/2026-08-14.md:511: trailing whitespace.
+    18	
REVIEWS/2026-08-14.md:514: trailing whitespace.
+    21	
REVIEWS/2026-08-14.md:519: trailing whitespace.
+    26	
REVIEWS/2026-08-14.md:529: trailing whitespace.
+    36	
REVIEWS/2026-08-14.md:532: trailing whitespace.
+    39	
REVIEWS/2026-08-14.md:539: trailing whitespace.
+    46	
REVIEWS/2026-08-14.md:554: trailing whitespace.
+    61	
REVIEWS/2026-08-14.md:556: trailing whitespace.
+    63	
REVIEWS/2026-08-14.md:574: trailing whitespace.
+    81	
REVIEWS/2026-08-14.md:579: trailing whitespace.
+    86	
REVIEWS/2026-08-14.md:581: trailing whitespace.
+    88	
REVIEWS/2026-08-14.md:583: trailing whitespace.
+    90	
REVIEWS/2026-08-14.md:984: trailing whitespace.
++          
REVIEWS/2026-08-14.md:1027: trailing whitespace.
++    66	
REVIEWS/2026-08-14.md:1029: trailing whitespace.
++    68	
REVIEWS/2026-08-14.md:1031: trailing whitespace.
++    70	
REVIEWS/2026-08-14.md:1033: trailing whitespace.
++    72	
REVIEWS/2026-08-14.md:1044: trailing whitespace.
++    83	
REVIEWS/2026-08-14.md:1048: trailing whitespace.
++    87	
REVIEWS/2026-08-14.md:1050: trailing whitespace.
++    89	
REVIEWS/2026-08-14.md:1056: trailing whitespace.
++    95	
REVIEWS/2026-08-14.md:1058: trailing whitespace.
++    97	
REVIEWS/2026-08-14.md:1060: trailing whitespace.
++    99	
REVIEWS/2026-08-14.md:1063: trailing whitespace.
++   102	
REVIEWS/2026-08-14.md:1065: trailing whitespace.
++   104	
REVIEWS/2026-08-14.md:1067: trailing whitespace.
++   106	
REVIEWS/2026-08-14.md:1070: trailing whitespace.
++   109	
REVIEWS/2026-08-14.md:1107: trailing whitespace.
+++# Sol review 
REVIEWS/2026-08-14.md:2059: trailing whitespace.
++    72	
REVIEWS/2026-08-14.md:2061: trailing whitespace.
++    74	
REVIEWS/2026-08-14.md:2068: trailing whitespace.
++    81	
REVIEWS/2026-08-14.md:2432: trailing whitespace.
++    37	
REVIEWS/2026-08-14.md:2434: trailing whitespace.
++    39	
REVIEWS/2026-08-14.md:2441: trailing whitespace.
++    46	
REVIEWS/2026-08-14.md:2443: trailing whitespace.
++    48	
REVIEWS/2026-08-14.md:2460: trailing whitespace.
++   106	
REVIEWS/2026-08-14.md:2511: trailing whitespace.
++    48	
REVIEWS/2026-08-14.md:2518: trailing whitespace.
++    55	
REVIEWS/2026-08-14.md:2520: trailing whitespace.
++    57	
REVIEWS/2026-08-14.md:2535: trailing whitespace.
++    72	
REVIEWS/2026-08-14.md:2642: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2644: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2651: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2680: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2715: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2718: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2723: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2736: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2739: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2746: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2762: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2764: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2792: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2794: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2796: trailing whitespace.
+ 
REVIEWS/2026-08-14.md:2905: trailing whitespace.
+   123	
REVIEWS/2026-08-14.md:2913: trailing whitespace.
+   131	
REVIEWS/2026-08-14.md:2920: trailing whitespace.
+   138	
REVIEWS/2026-08-14.md:2922: trailing whitespace.
+   140	
REVIEWS/2026-08-14.md:2926: trailing whitespace.
+   144	
REVIEWS/2026-08-14.md:2928: trailing whitespace.
+   146	
REVIEWS/2026-08-14.md:2934: trailing whitespace.
+   152	
REVIEWS/2026-08-14.md:2936: trailing whitespace.
+   154	
REVIEWS/2026-08-14.md:2938: trailing whitespace.
+   156	
REVIEWS/2026-08-14.md:2945: trailing whitespace.
+   163	
REVIEWS/2026-08-14.md:2947: trailing whitespace.
+   165	
REVIEWS/2026-08-14.md:2949: trailing whitespace.
+   167	
REVIEWS/2026-08-14.md:2954: trailing whitespace.
+   172	
REVIEWS/2026-08-14.md:2968: trailing whitespace.
+   186	
REVIEWS/2026-08-14.md:2970: trailing whitespace.
+   188	
REVIEWS/2026-08-14.md:2977: trailing whitespace.
+   195	
REVIEWS/2026-08-14.md:2979: trailing whitespace.
+   197	
REVIEWS/2026-08-14.md:2993: trailing whitespace.
+   211	
REVIEWS/2026-08-14.md:2997: trailing whitespace.
+   215	
REVIEWS/2026-08-14.md:3006: trailing whitespace.
+   224	
REVIEWS/2026-08-14.md:3008: trailing whitespace.
+   226	
REVIEWS/2026-08-14.md:3010: trailing whitespace.
+   228	
REVIEWS/2026-08-14.md:3020: trailing whitespace.
+     6	
REVIEWS/2026-08-14.md:3036: trailing whitespace.
+    22	
REVIEWS/2026-08-14.md:3040: trailing whitespace.
+    26	
REVIEWS/2026-08-14.md:3043: trailing whitespace.
+    29	
REVIEWS/2026-08-14.md:3047: trailing whitespace.
+    33	
REVIEWS/2026-08-14.md:3051: trailing whitespace.
+    37	
REVIEWS/2026-08-14.md:3054: trailing whitespace.
+    40	
REVIEWS/2026-08-14.md:3056: trailing whitespace.
+    42	
REVIEWS/2026-08-14.md:3060: trailing whitespace.
+    46	
REVIEWS/2026-08-14.md:3064: trailing whitespace.
+    50	
REVIEWS/2026-08-14.md:3068: trailing whitespace.
+    54	
REVIEWS/2026-08-14.md:3070: trailing whitespace.
+    56	
REVIEWS/2026-08-14.md:3072: trailing whitespace.
+    58	
REVIEWS/2026-08-14.md:3074: trailing whitespace.
+    60	
REVIEWS/2026-08-14.md:3078: trailing whitespace.
+    64	
REVIEWS/2026-08-14.md:3081: trailing whitespace.
+    67	
REVIEWS/2026-08-14.md:3083: trailing whitespace.
+    69	
REVIEWS/2026-08-14.md:3085: trailing whitespace.
+    71	
REVIEWS/2026-08-14.md:3091: trailing whitespace.
+    77	
REVIEWS/2026-08-14.md:3095: trailing whitespace.
+    81	
REVIEWS/2026-08-14.md:3097: trailing whitespace.
+    83	
REVIEWS/2026-08-14.md:3104: trailing whitespace.
+    90	
REVIEWS/2026-08-14.md:3108: trailing whitespace.
+    94	
REVIEWS/2026-08-14.md:3111: trailing whitespace.
+    97	
REVIEWS/2026-08-14.md:3117: trailing whitespace.
+   103	
REVIEWS/2026-08-14.md:3122: trailing whitespace.
+   108	
REVIEWS/2026-08-14.md:3127: trailing whitespace.
+   113	
REVIEWS/2026-08-14.md:3129: trailing whitespace.
+   115	
REVIEWS/2026-08-14.md:3135: trailing whitespace.
+   121	
REVIEWS/2026-08-14.md:3137: trailing whitespace.
+   123	
REVIEWS/2026-08-14.md:3141: trailing whitespace.
+   127	
REVIEWS/2026-08-14.md:3146: trailing whitespace.
+   132	
REVIEWS/2026-08-14.md:3148: trailing whitespace.
+   134	
REVIEWS/2026-08-14.md:3151: trailing whitespace.
+   137	
REVIEWS/2026-08-14.md:3155: trailing whitespace.
+   141	
REVIEWS/2026-08-14.md:3160: trailing whitespace.
+   146	
REVIEWS/2026-08-14.md:3165: trailing whitespace.
+   151	
REVIEWS/2026-08-14.md:3167: trailing whitespace.
+   153	
REVIEWS/2026-08-14.md:3173: trailing whitespace.
+   159	
REVIEWS/2026-08-14.md:3176: trailing whitespace.
+   162	
REVIEWS/2026-08-14.md:3178: trailing whitespace.
+   164	
REVIEWS/2026-08-14.md:3180: trailing whitespace.
+   166	
REVIEWS/2026-08-14.md:3187: trailing whitespace.
+   173	
REVIEWS/2026-08-14.md:3192: trailing whitespace.
+   178	
REVIEWS/2026-08-14.md:3197: trailing whitespace.
+   183	
REVIEWS/2026-08-14.md:3203: trailing whitespace.
+   189	
REVIEWS/2026-08-14.md:3205: trailing whitespace.
+   191	
REVIEWS/2026-08-14.md:3209: trailing whitespace.
+   195	
REVIEWS/2026-08-14.md:3214: trailing whitespace.
+   200	
REVIEWS/2026-08-14.md:3218: trailing whitespace.
+   204	
REVIEWS/2026-08-14.md:3223: trailing whitespace.
+   209	
REVIEWS/2026-08-14.md:3228: trailing whitespace.
+   214	
REVIEWS/2026-08-14.md:3234: trailing whitespace.
+   220	
REVIEWS/2026-08-14.md:3239: trailing whitespace.
+   225	
REVIEWS/2026-08-14.md:3243: trailing whitespace.
+   229	
REVIEWS/2026-08-14.md:3246: trailing whitespace.
+   232	
REVIEWS/2026-08-14.md:3248: trailing whitespace.
+   234	
REVIEWS/2026-08-14.md:3250: trailing whitespace.
+   236	
REVIEWS/2026-08-14.md:3255: trailing whitespace.
+   241	
REVIEWS/2026-08-14.md:3258: trailing whitespace.
+   244	
REVIEWS/2026-08-14.md:3262: trailing whitespace.
+   248	
REVIEWS/2026-08-14.md:3264: trailing whitespace.
+   250	
REVIEWS/2026-08-14.md:3268: trailing whitespace.
+   254	
REVIEWS/2026-08-14.md:3270: trailing whitespace.
+   256	
REVIEWS/2026-08-14.md:3274: trailing whitespace.
+   260	
REVIEWS/2026-08-14.md:3281: trailing whitespace.
+   267	
REVIEWS/2026-08-14.md:3285: trailing whitespace.
+   271	
REVIEWS/2026-08-14.md:3288: trailing whitespace.
+   274	
REVIEWS/2026-08-14.md:3294: trailing whitespace.
+   280	
REVIEWS/2026-08-14.md:3299: trailing whitespace.
+   285	
REVIEWS/2026-08-14.md:3301: trailing whitespace.
+   287	
REVIEWS/2026-08-14.md:3304: trailing whitespace.
+   290	
REVIEWS/2026-08-14.md:3307: trailing whitespace.
+   293	
REVIEWS/2026-08-14.md:3310: trailing whitespace.
+   296	
REVIEWS/2026-08-14.md:3313: trailing whitespace.
+   299	
REVIEWS/2026-08-14.md:3318: trailing whitespace.
+   304	
REVIEWS/2026-08-14.md:3325: trailing whitespace.
+   311	
REVIEWS/2026-08-14.md:3329: trailing whitespace.
+   315	
REVIEWS/2026-08-14.md:3331: trailing whitespace.
+   317	
REVIEWS/2026-08-14.md:3359: trailing whitespace.
+   345	
REVIEWS/2026-08-14.md:3384: trailing whitespace.
+   370	
REVIEWS/2026-08-14.md:3388: trailing whitespace.
+   374	
REVIEWS/2026-08-14.md:3390: trailing whitespace.
+   376	
REVIEWS/2026-08-14.md:3402: trailing whitespace.
+     5	
REVIEWS/2026-08-14.md:3405: trailing whitespace.
+     8	
REVIEWS/2026-08-14.md:3408: trailing whitespace.
+    11	
REVIEWS/2026-08-14.md:3410: trailing whitespace.
+    13	
REVIEWS/2026-08-14.md:3413: trailing whitespace.
+    16	
REVIEWS/2026-08-14.md:3416: trailing whitespace.
+    19	
REVIEWS/2026-08-14.md:3419: trailing whitespace.
+    22	
REVIEWS/2026-08-14.md:3422: trailing whitespace.
+    25	
REVIEWS/2026-08-14.md:3425: trailing whitespace.
+    28	
REVIEWS/2026-08-14.md:3428: trailing whitespace.
+    31	
REVIEWS/2026-08-14.md:3430: trailing whitespace.
+    33	
REVIEWS/2026-08-14.md:3439: trailing whitespace.
+    42	
REVIEWS/2026-08-14.md:3442: trailing whitespace.
+    45	
REVIEWS/2026-08-14.md:3445: trailing whitespace.
+    48	
REVIEWS/2026-08-14.md:3448: trailing whitespace.
+    51	
REVIEWS/2026-08-14.md:3453: trailing whitespace.
+    56	
REVIEWS/2026-08-14.md:3456: trailing whitespace.
+    59	
REVIEWS/2026-08-14.md:3462: trailing whitespace.
+    65	
REVIEWS/2026-08-14.md:3472: trailing whitespace.
+    75	
REVIEWS/2026-08-14.md:3476: trailing whitespace.
+    79	
REVIEWS/2026-08-14.md:3479: trailing whitespace.
+    82	
REVIEWS/2026-08-14.md:3491: trailing whitespace.
+    94	
REVIEWS/2026-08-14.md:3493: trailing whitespace.
+    96	
REVIEWS/2026-08-14.md:3495: trailing whitespace.
+    98	
REVIEWS/2026-08-14.md:3497: trailing whitespace.
+   100	
REVIEWS/2026-08-14.md:3499: trailing whitespace.
+   102	
REVIEWS/2026-08-14.md:3505: trailing whitespace.
+   108	
REVIEWS/2026-08-14.md:3507: trailing whitespace.
+   110	
REVIEWS/2026-08-14.md:3510: trailing whitespace.
+   113	
REVIEWS/2026-08-14.md:3513: trailing whitespace.
+   116	
REVIEWS/2026-08-14.md:3516: trailing whitespace.
+   119	
REVIEWS/2026-08-14.md:3535: trailing whitespace.
+   138	
REVIEWS/2026-08-14.md:3550: trailing whitespace.
+     8	
REVIEWS/2026-08-14.md:3558: trailing whitespace.
+    16	
REVIEWS/2026-08-14.md:3560: trailing whitespace.
+    18	
REVIEWS/2026-08-14.md:3562: trailing whitespace.
+    20	
REVIEWS/2026-08-14.md:3567: trailing whitespace.
+    25	
REVIEWS/2026-08-14.md:3571: trailing whitespace.
+    29	
REVIEWS/2026-08-14.md:3573: trailing whitespace.
+    31	
REVIEWS/2026-08-14.md:3596: trailing whitespace.
+    54	
REVIEWS/2026-08-14.md:3600: trailing whitespace.
+    58	
REVIEWS/2026-08-14.md:3607: trailing whitespace.
+    65	
REVIEWS/2026-08-14.md:3610: trailing whitespace.
+    68	
REVIEWS/2026-08-14.md:3612: trailing whitespace.
+    70	
REVIEWS/2026-08-14.md:3622: trailing whitespace.
+    80	
REVIEWS/2026-08-14.md:3625: trailing whitespace.
+    83	
REVIEWS/2026-08-14.md:3628: trailing whitespace.
+    86	

exec
/bin/zsh -lc "command -v shellcheck >/dev/null && shellcheck scripts/sol-review.sh || true; codex review --help | sed -n '1,180p'" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 0ms:
WARNING: proceeding, even though we could not create PATH aliases: Operation not permitted (os error 1)
Run a code review non-interactively

Usage: codex review [OPTIONS] [PROMPT]

Arguments:
  [PROMPT]
          Custom review instructions. If `-` is used, read from stdin

Options:
  -c, --config <key=value>
          Override a configuration value that would otherwise be loaded from `~/.codex/config.toml`.
          Use a dotted path (`foo.bar.baz`) to override nested values. The `value` portion is parsed
          as TOML. If it fails to parse as TOML, the raw string is used as a literal.
          
          Examples: - `-c model="o3"` - `-c 'sandbox_permissions=["disk-full-read-access"]'` - `-c
          shell_environment_policy.inherit=all`

      --strict-config
          Error out when config.toml contains fields that are not recognized by this version of
          Codex

      --enable <FEATURE>
          Enable a feature (repeatable). Equivalent to `-c features.<name>=true`

      --uncommitted
          Review staged, unstaged, and untracked changes

      --base <BRANCH>
          Review changes against the given base branch

      --disable <FEATURE>
          Disable a feature (repeatable). Equivalent to `-c features.<name>=false`

      --commit <SHA>
          Review the changes introduced by a commit

      --title <TITLE>
          Optional commit title to display in the review summary

  -h, --help
          Print help (see a summary with '-h')

exec
/bin/zsh -lc "git show-ref --heads --dereference | rg 'champion-speedup|metal-moe-kernel' && git merge-base champion-speedup a40fc08983f436db1ad28039db72ca165ae8e5d2 && git log --oneline --decorate --graph --all -12 | sed -n '1,80p'" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 5395ms:
2026-08-14 19:56:48.343 xcodebuild[99128:7422163]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:56:48.463 xcodebuild[99128:7422161] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-boMMLowm' (errno=Operation not permitted)
2026-08-14 19:56:48.874 xcodebuild[99133:7422227]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:56:48.992 xcodebuild[99133:7422223] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
fc11ab9beaba6639fd7973198dd2522a348357f7 refs/heads/champion-speedup
e213b154c30966836c2d576bf72423819442e38b refs/heads/metal-moe-kernel
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-9q59JLoU' (errno=Operation not permitted)
2026-08-14 19:56:49.910 xcodebuild[99149:7422337]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:56:50.031 xcodebuild[99149:7422336] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Gazx2ot9' (errno=Operation not permitted)
2026-08-14 19:56:50.432 xcodebuild[99152:7422363]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:56:50.557 xcodebuild[99152:7422362] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
fc11ab9beaba6639fd7973198dd2522a348357f7
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-j8Iq8d80' (errno=Operation not permitted)
2026-08-14 19:56:51.448 xcodebuild[99158:7422392]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:56:51.562 xcodebuild[99158:7422390] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Zx6O5AT8' (errno=Operation not permitted)
2026-08-14 19:56:51.983 xcodebuild[99160:7422403]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:56:52.101 xcodebuild[99160:7422402] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
* e213b154c (HEAD -> metal-moe-kernel, river/metal-moe-kernel) sol-review: v2 fixes from Sol's first review (marker scope, failure handling, SIGPIPE, base validation) + kernel-plan registry path correction
* 954dc712d review: Sol pass 2026-08-14
* a40fc0898 champion-speedup: remove failed smoke review artifact
* 78034fda1 review: Sol pass 2026-08-14
* f996933d6 metal-moe-kernel: E0 result (dispatch-only dead) + E1 skinny-tile design
* 5f07b0b72 metal-moe-kernel: project scaffold + M0 findings
* fc11ab9be (river/champion-speedup, champion-speedup) champion-speedup: VERDICT + budget-256 daily-driver default
* 2b5f4cd19 champion-speedup: journal runaway-thinking discovery + budget cap verification
* 3d83b74ae champion-speedup: journal baseline2 + candidates; budget256 neutral, nothink running
* c52fe656d champion-speedup: authoritative baseline2 (all gates green)
* 6ceb8fcff champion-speedup: THINK_ARGS knob for server-side reasoning control
* 7715a8eba champion-speedup: pin system clang in t2 gate (Homebrew TSan segfaults on ARM64)

exec
/bin/zsh -lc "git diff --stat a40fc08983f436db1ad28039db72ca165ae8e5d2..HEAD && git diff --unified=3 a40fc08983f436db1ad28039db72ca165ae8e5d2..HEAD -- scripts/sol-review.sh KERNEL-PROJECT.md REVIEWS/.last-reviewed | sed -n '1,240p'; printf '\\nCommitted review tail:\\n'; git show HEAD:REVIEWS/2026-08-14.md | tail -80" in /Users/davidfeliperiveraguerra/dev/llama.cpp
 succeeded in 4592ms:
2026-08-14 19:57:54.068 xcodebuild[99239:7424368]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:57:54.201 xcodebuild[99239:7424367] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-YY3VBqqG' (errno=Operation not permitted)
2026-08-14 19:57:54.604 xcodebuild[99242:7424443]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:57:54.721 xcodebuild[99242:7424438] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
 KERNEL-PROJECT.md      |    6 +-
 REVIEWS/.last-reviewed |    1 +
 REVIEWS/2026-08-14.md  | 1948 ++++++++++++++++++++++++++++++++++++++++++++++++
 scripts/sol-review.sh  |   44 +-
 4 files changed, 1984 insertions(+), 15 deletions(-)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-yOq5WUQ9' (errno=Operation not permitted)
2026-08-14 19:57:55.553 xcodebuild[99248:7424478]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:57:55.668 xcodebuild[99248:7424477] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-coyaWD5R' (errno=Operation not permitted)
2026-08-14 19:57:56.032 xcodebuild[99250:7424488]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:57:56.158 xcodebuild[99250:7424487] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
diff --git a/KERNEL-PROJECT.md b/KERNEL-PROJECT.md
index 0799fa5ca..eba902c57 100644
--- a/KERNEL-PROJECT.md
+++ b/KERNEL-PROJECT.md
@@ -90,8 +90,10 @@ across 8 tokens instead of 1 (PR #25377 evidence on plain mul_mat:
    - careful with lr0/lr1 clamping + id lookup (ids_i32[im*ne21 + r1 + lr1])
 2. Instantiate for block_iq3_s (QK_NL, dequantize_iq3_s) + block_q4_0 +
    block_q8_0 (validation quants). Register host_name in the template
-   block; wire pipeline lookup in ggml-metal.m (mirror
-   ggml_metal_library_get_pipeline_mul_mm_id).
+   block; wire pipeline lookup in ggml-metal-device.cpp (mirror
+   ggml_metal_library_get_pipeline_mul_mm_id at ggml-metal-device.cpp:1009
+   - corrected 2026-08-14 per Sol review; the .metal file is the shader
+   source, the registry lives in ggml-metal-device.cpp).
 3. Dispatch (ggml-metal-ops.cpp MUL_MAT_ID case): new branch
    (ne21 >= 2 && ne21 <= 16 && quant in {iq3_s, q4_0, q8_0}) ->
    nr8 pipeline; keep mv at ne21==1; keep existing mm at ne21>=32.
diff --git a/REVIEWS/.last-reviewed b/REVIEWS/.last-reviewed
new file mode 100644
index 000000000..e54e5218f
--- /dev/null
+++ b/REVIEWS/.last-reviewed
@@ -0,0 +1 @@
+a40fc08983f436db1ad28039db72ca165ae8e5d2
diff --git a/scripts/sol-review.sh b/scripts/sol-review.sh
index 6b36444db..7b19c55cf 100755
--- a/scripts/sol-review.sh
+++ b/scripts/sol-review.sh
@@ -6,10 +6,14 @@
 #
 # Usage: scripts/sol-review.sh [base-branch]   (default: champion-speedup)
 #
-# Cron: no_agent watchdog - stdout is delivered verbatim; keep the digest
-# short (empty-ish output on no-new-commits would be ideal but a nightly
-# review of even 1 commit is the point - this is a review loop, not a
-# change-detector).
+# Cron: no_agent watchdog - stdout delivered verbatim; silent when no new
+# commits; non-zero exit on review failure (cron alerts).
+#
+# v2 fixes (from Sol's own review of v1, REVIEWS/2026-08-14.md):
+# - review --base <marker> so each run covers ONLY new commits
+# - marker advances only on codex success; failures exit nonzero
+# - git log -25 (no SIGPIPE via head)
+# - missing base ref = error, not silent success
 set -euo pipefail
 
 ROOT="$(cd "$(dirname "$0")/.." && pwd)"
@@ -20,13 +24,17 @@ REVIEWS_DIR="$ROOT/REVIEWS"
 MARKER="$REVIEWS_DIR/.last-reviewed"
 mkdir -p "$REVIEWS_DIR"
 
-# Locate the base ref (local branch or remote)
-if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
+# Resolve the base ref; a missing base is a config error, not "no work".
+if git rev-parse --verify --quiet "$BASE" >/dev/null; then
     BASE_REF="$BASE"
-else
+elif git rev-parse --verify --quiet "origin/$BASE" >/dev/null; then
     BASE_REF="origin/$BASE"
+else
+    echo "sol-review: base '$BASE' not found locally or on origin" >&2
+    exit 2
 fi
 
+# The ref to diff against: the last-reviewed marker, else the base branch.
 LAST="$(cat "$MARKER" 2>/dev/null || echo "$BASE_REF")"
 
 # Number of new commits since last review
@@ -44,7 +52,7 @@ OUT="$REVIEWS_DIR/$(date +%Y-%m-%d).md"
     echo "- Model: gpt-5.6-sol (codex review, ChatGPT plan)"
     echo
     echo '```'
-    git log --oneline "$LAST..HEAD" | head -25
+    git log --oneline -25 "$LAST..HEAD"
     echo '```'
     echo
     echo "## Findings"
@@ -55,15 +63,25 @@ REVIEW_PROMPT="Adversarial code review of the Metal MoE kernel changes (metal-mo
 
 # codex 0.146: --base cannot be combined with a positional [PROMPT]; the
 # stdin form ('-') is the workaround. Fall back to the built-in prompt.
-if ! codex review --base "$BASE_REF" \
+REVIEW_OK=0
+if codex review --base "$LAST" \
         -c model="gpt-5.6-sol" \
         -c model_reasoning_effort="high" \
         - <<< "$REVIEW_PROMPT" >> "$OUT" 2>&1; then
+    REVIEW_OK=1
+else
     echo "(stdin prompt form rejected - retrying with built-in review prompt)" >> "$OUT"
-    codex review --base "$BASE_REF" \
-        -c model="gpt-5.6-sol" \
-        -c model_reasoning_effort="high" \
-        >> "$OUT" 2>&1 || echo "(codex review failed)" >> "$OUT"
+    if codex review --base "$LAST" \
+            -c model="gpt-5.6-sol" \
+            -c model_reasoning_effort="high" \
+            >> "$OUT" 2>&1; then
+        REVIEW_OK=1
+    fi
+fi
+
+if [ "$REVIEW_OK" != "1" ]; then
+    echo "sol-review FAILED (codex review error) - marker NOT advanced; see $OUT" >&2
+    exit 1
 fi
 
 echo "$(git rev-parse HEAD)" > "$MARKER"

Committed review tail:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-9ixJGXaJ' (errno=Operation not permitted)
2026-08-14 19:57:57.089 xcodebuild[99257:7424521]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:57:57.237 xcodebuild[99257:7424520] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-ZMzgXCpG' (errno=Operation not permitted)
2026-08-14 19:57:57.665 xcodebuild[99259:7424541]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-08-14 19:57:57.802 xcodebuild[99259:7424540] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
    55	
    56	## Progress log
    57	
    58	- 2026-08-14: M0 done. E0 dispatched: baseline sweep running.
    59	- 2026-08-14 E0 RESULT: DISPATCH-ONLY IS DEAD. Threshold 32->2 routes
    60	  everything to the existing 64x32 mm kernel; measured REGRESSION at every
    61	  batch (throttle-noise-corrected, interleaved pairs recommended for E1):
    62	    baseline (mv<32 / mm@32): b=2 29.67, b=8 29.79, b=32 30.13 t/s decode
    63	    patched (mm everywhere):  b=1 24.92, b=2 24.38, b=4 25.84, b=8 24.88,
    64	    b=16 25.78, b=32 26.42
    65	  The 64x32 tile zero-pads below 32 tokens (~1/32 utilization at b=1).
    66	  Prefill also regressed at small batches (S_PP b=8: 61.6 -> 39.9).
    67	  REVERTED (ops.cpp restored to threshold 32, rebuilt).
    68	  NOTE: consecutive bench runs throttle (~10-15% drift, power governor) -
    69	  E1 A/Bs MUST be interleaved pairs, not sequential sweeps.
    70	  Bench gotcha: llama-batched-bench needs -npl explicitly (empty n_pl =
    71	  zero combos, header-only, exit 0).
    72	

codex
The prescribed benchmark never exercises the dispatch being evaluated, and the watchdog has multiple reliability failures, including an endless self-generated commit loop and an invalid custom-prompt invocation. These issues prevent the changes from fulfilling their stated purpose.

Full review comments:

- [P1] Sweep parallel sequences to exercise nr8 dispatch — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:98-99
  With `-npl 1`, the TG loop submits exactly one token per `llama_decode`, while `-b` only caps the decode chunk size. Consequently `ne21` remains 1 and the proposed `ne21 >= 2` branch is never measured, making the A/B and kill decision invalid; sweep `-npl` and keep `-b` sufficiently large instead.

- [P1] Stop the watchdog from committing its own marker — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:69-71
  The marker records the pre-review `HEAD`, then the commit advances `HEAD`, so the next cron run sees that generated review commit as new work and repeats indefinitely even without source changes. This also violates the repository prohibition on automated commits (`AGENTS.md:49`); keep review state outside branch history or require explicit approval.

- [P2] Correct the SIMDgroup thread count — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:86-87
  Apple Metal SIMDgroups contain 32 threads, so a 64-thread threadgroup consists of two SIMDgroups, not one. Implementing accumulator or shared-memory ownership from this contradictory specification risks duplicated or missing work; specify either one 32-thread SIMDgroup or an explicit two-SIMDgroup partition.

- [P2] Ensure the custom prompt reaches Codex — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:58-61
  In Codex 0.146, `-` still occupies the positional `[PROMPT]`, so combining it with `--base` exits with status 2. The first invocation therefore fails every run and the fallback performs a generic review without `REVIEW_PROMPT`, meaning the targeted Metal checks are never applied.

- [P2] Review from the last-reviewed commit — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:63-66
  After the marker exists, the report claims the range is `$LAST..HEAD`, but the successful review invocation still uses `--base "$BASE_REF"`. Every run therefore re-reviews the complete branch since `champion-speedup`, duplicating old findings and attributing them to only the latest commits.

- [P2] Do not mark failed reviews complete — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:66-69
  If the fallback review fails because of authentication, network, or model errors, `|| echo` converts the failure into success and execution advances the marker and prints that the commits were reviewed. This gives the watchdog and audit trail a false success state; only update the marker after a successful Codex exit.

- [P2] Avoid SIGPIPE when truncating the log — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:47-47
  When the range contains more than 25 commits, `head` closes the pipe and `git log` exits with SIGPIPE status 141. Because `pipefail` and `set -e` are enabled, this aborts the script before the review; use Git's own maximum-count option instead.

- [P2] Reject missing base references — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:30-33
  If neither the local base nor `origin/$BASE` exists, `git rev-list` fails but `|| echo "0"` converts the configuration error into zero new commits, causing a silent successful exit. A misspelled argument or unfetched remote therefore disables the watchdog without warning.

- [P3] Point the plan at the actual pipeline registry — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:92-94
  The referenced `ggml-metal.m` file does not exist in this tree. `ggml_metal_library_get_pipeline_mul_mm_id` is implemented in `ggml/src/ggml-metal/ggml-metal-device.cpp` and declared in `ggml-metal-device.h`, so the implementation plan currently directs step 2 to a nonexistent registration site.
The prescribed benchmark never exercises the dispatch being evaluated, and the watchdog has multiple reliability failures, including an endless self-generated commit loop and an invalid custom-prompt invocation. These issues prevent the changes from fulfilling their stated purpose.

Full review comments:

- [P1] Sweep parallel sequences to exercise nr8 dispatch — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:98-99
  With `-npl 1`, the TG loop submits exactly one token per `llama_decode`, while `-b` only caps the decode chunk size. Consequently `ne21` remains 1 and the proposed `ne21 >= 2` branch is never measured, making the A/B and kill decision invalid; sweep `-npl` and keep `-b` sufficiently large instead.

- [P1] Stop the watchdog from committing its own marker — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:69-71
  The marker records the pre-review `HEAD`, then the commit advances `HEAD`, so the next cron run sees that generated review commit as new work and repeats indefinitely even without source changes. This also violates the repository prohibition on automated commits (`AGENTS.md:49`); keep review state outside branch history or require explicit approval.

- [P2] Correct the SIMDgroup thread count — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:86-87
  Apple Metal SIMDgroups contain 32 threads, so a 64-thread threadgroup consists of two SIMDgroups, not one. Implementing accumulator or shared-memory ownership from this contradictory specification risks duplicated or missing work; specify either one 32-thread SIMDgroup or an explicit two-SIMDgroup partition.

- [P2] Ensure the custom prompt reaches Codex — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:58-61
  In Codex 0.146, `-` still occupies the positional `[PROMPT]`, so combining it with `--base` exits with status 2. The first invocation therefore fails every run and the fallback performs a generic review without `REVIEW_PROMPT`, meaning the targeted Metal checks are never applied.

- [P2] Review from the last-reviewed commit — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:63-66
  After the marker exists, the report claims the range is `$LAST..HEAD`, but the successful review invocation still uses `--base "$BASE_REF"`. Every run therefore re-reviews the complete branch since `champion-speedup`, duplicating old findings and attributing them to only the latest commits.

- [P2] Do not mark failed reviews complete — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:66-69
  If the fallback review fails because of authentication, network, or model errors, `|| echo` converts the failure into success and execution advances the marker and prints that the commits were reviewed. This gives the watchdog and audit trail a false success state; only update the marker after a successful Codex exit.

- [P2] Avoid SIGPIPE when truncating the log — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:47-47
  When the range contains more than 25 commits, `head` closes the pipe and `git log` exits with SIGPIPE status 141. Because `pipefail` and `set -e` are enabled, this aborts the script before the review; use Git's own maximum-count option instead.

- [P2] Reject missing base references — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:30-33
  If neither the local base nor `origin/$BASE` exists, `git rev-list` fails but `|| echo "0"` converts the configuration error into zero new commits, causing a silent successful exit. A misspelled argument or unfetched remote therefore disables the watchdog without warning.

- [P3] Point the plan at the actual pipeline registry — /Users/davidfeliperiveraguerra/dev/llama.cpp/KERNEL-PROJECT.md:92-94
  The referenced `ggml-metal.m` file does not exist in this tree. `ggml_metal_library_get_pipeline_mul_mm_id` is implemented in `ggml/src/ggml-metal/ggml-metal-device.cpp` and declared in `ggml-metal-device.h`, so the implementation plan currently directs step 2 to a nonexistent registration site.

codex
The watchdog remains trapped in a review-only commit cycle, and same-day reruns can recursively review their own output. Both defects undermine the reliability of the automated review process.

Full review comments:

- [P1] Do not commit the last-reviewed marker — /Users/davidfeliperiveraguerra/dev/llama.cpp/REVIEWS/.last-reviewed:1-1
  After a successful run, this tracked marker records the pre-review `HEAD`, and the subsequent `git commit` advances `HEAD` past it. Every later cron tick therefore treats the generated review commit as new work and creates another review commit even without source changes. Keep review state outside the committed history; the automatic commit also conflicts with `AGENTS.md:49`.

- [P1] Write the live report outside the reviewed worktree — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:67-67
  On a second run on the same date, `$OUT` is already tracked and has been truncated before this invocation. Because `codex review --base` includes tracked worktree changes, it reviews the same report to which its output is being appended, recursively copying diagnostics and diffs into the report and rapidly exhausting context. Generate the report in a temporary path outside the worktree and move it into `REVIEWS/` after Codex exits.
The watchdog remains trapped in a review-only commit cycle, and same-day reruns can recursively review their own output. Both defects undermine the reliability of the automated review process.

Full review comments:

- [P1] Do not commit the last-reviewed marker — /Users/davidfeliperiveraguerra/dev/llama.cpp/REVIEWS/.last-reviewed:1-1
  After a successful run, this tracked marker records the pre-review `HEAD`, and the subsequent `git commit` advances `HEAD` past it. Every later cron tick therefore treats the generated review commit as new work and creates another review commit even without source changes. Keep review state outside the committed history; the automatic commit also conflicts with `AGENTS.md:49`.

- [P1] Write the live report outside the reviewed worktree — /Users/davidfeliperiveraguerra/dev/llama.cpp/scripts/sol-review.sh:67-67
  On a second run on the same date, `$OUT` is already tracked and has been truncated before this invocation. Because `codex review --base` includes tracked worktree changes, it reviews the same report to which its output is being appended, recursively copying diagnostics and diffs into the report and rapidly exhausting context. Generate the report in a temporary path outside the worktree and move it into `REVIEWS/` after Codex exits.
